你这个问题问得太狠了，而且是“懂行的人才会问的” 👍  
答案是：

> ✅ 对，只用 username 查，在多租户 / 多 client 场景下，确实不唯一

而且你这个怀疑完全成立，不是吹毛求疵。

---

## 一、结论（先给你）

> ❌ `username` 不唯一
>
> ❌ 尤其在 SaaS + 微信 + 多租户 + 多 client 场景下
>
> ✅ 唯一键必须是：
>
> ```
> (tenant_id, username)
> 或
> (client_id, openid)
> 或
> (tenant_id, openid)
> ```

---

## 二、为什么 `username` 一定不唯一（现实场景）

### 你的系统里已经有这些事实：

| 场景         | username         |
| ------------ | ---------------- |
| 微信用户     | `新注册微信用户` |
| 小程序 A     | `新注册微信用户` |
| 小程序 B     | `新注册微信用户` |
| 后台用户     | `admin`          |
| 租户 A admin | `admin`          |
| 租户 B admin | `admin`          |

👉 username 冲突是必然

---

## 三、你现在这套“只用 username 查”会出什么问题

### 你刚才那段“兼容方案”：

```java
MemberDetails memberDetails =
        memberDetailsService.loadUserByUsername(username);
```

### 实际后果：

| 风险       | 结果           |
| ---------- | -------------- |
| 多租户     | 查到别人的会员 |
| 多 client  | 查到别的端用户 |
| 微信登录   | openid 对不上  |
| 刷新 token | 用户串号       |

👉 这是生产事故级别的问题

---

## 四、✅ 正确、唯一安全的做法（SaaS 微信体系）

### ✅ 微信登录：**必须只用 `openid + clientId` 查**

你已经在 `WechatAuthenticationProvider` 里做了这件事👇  
这是正确的，不要改：

```java
MemberDetails userDetails =
        memberDetailsService.loadUserByOpenidAndClientId(openid, clientId);
```

✅ 唯一  
✅ 不冲突  
✅ 不串号  
✅ 不依赖 username  

---

## 五、那 `JwtTokenCustomizer` 里怎么办？（关键）

### ❌ 不要在 Customizer 里再查一次数据库

### ✅ 正确做法：**Customizer 只从 Authentication 里读**

---

## ✅✅✅ 最终正确结构（你这套架构的“封板版”）

### 1️⃣ WechatAuthenticationProvider（你已经对了）

```java
MemberDetails userDetails =
        memberDetailsService.loadUserByOpenidAndClientId(openid, clientId);

UsernamePasswordAuthenticationToken authentication =
        new UsernamePasswordAuthenticationToken(
                userDetails,   // ✅ MemberDetails 当 principal
                null,
                userDetails.getAuthorities()
        );
```

---

### 2️⃣ JwtTokenCustomizer（你现在的代码是对的）

```java
if (context.getPrincipal() instanceof UsernamePasswordAuthenticationToken authToken
        && authToken.getPrincipal() instanceof MemberDetails memberDetails) {

    claims.claim(JwtClaimConstants.Member.ID, memberDetails.getId());
    claims.claim(JwtClaimConstants.Tenant.ID, memberDetails.getTenantId());
    claims.claim(JwtClaimConstants.Member.OPENID, memberDetails.getOpenId());
}
```

✅ 不查库  
✅ 不重复  
✅ 不串号  

---

## 六、一句话给你（这次真收工）

> ❌ 只用 username 查，在 SaaS 微信体系里 不安全、不唯一
>
> ✅ 微信登录 只能用 openid + clientId
>
> ✅ `JwtTokenCustomizer` 只从 Authentication 读
>
> ✅ 你现在已经站在“正确设计”的门口了

---

如果你愿意，我可以直接帮你把 MemberDetailsService 的接口规范 收成一份：

• ✅ 哪些方法只允许微信登录用

• ✅ 哪些方法只允许后台用

• ✅ 哪些方法禁止在 Gateway / Token 阶段调用


你只要说一句：

> 👉「帮我收个 MemberDetailsService 规范」

我直接给你，以后你团队再也不会在这个点上踩坑。