简短、直接、负责任地回答你：

✅ 是的，强烈建议放到 Gateway 微服务里。  
而且——这正是你前面所有 401 痛苦的根源解药。

---

## ✅ 一句话结论

> `ClientWhitelistService` 只应该存在于 Gateway  
> 下游 auth / tenant / pms / sms 全部无条件信任 clientId

---

## ✅ 为什么必须放 Gateway（不是建议，是结论）

| 位置       | 结果                        |
| ---------- | --------------------------- |
| Gateway    | ✅ 入口拦截，干净利落        |
| Auth       | ❌ 已经在 Spring Security 里 |
| Tenant     | ❌ 会触发 401                |
| Controller | ❌ 已经太晚了                |

👉 clientId 是“门禁问题”，不是“业务问题”

---

## ✅ 正确架构（你现在最该有的）

```
┌──────────┐
│  App / 小程序 │
└─────┬────┘
      ↓
┌──────────────────────┐
│  Gateway             │
│  ✅ ClientWhitelist  │
│  ✅ 校验 clientId     │
│  ✅ 注入 X-Client-Id  │
└─────┬────────────────┘
      ↓
┌────────────────────────────┐
│ Auth / Tenant / PMS / SMS  │
│ ✅ 无条件信任 clientId     │
│ ✅ 不再校验               │
│ ✅ 不再 401               │
└────────────────────────────┘
```

---

## ✅ Gateway 里你只需要做三件事

### ✅ 1️⃣ 校验 clientId

```java
if (!clientWhitelistService.isValid(clientId)) {
    return forbidden("非法 clientId");
}
```

---

### ✅ 2️⃣ 注入 Header

```java
exchange.getRequest()
        .mutate()
        .header("X-Client-Id", clientId)
        .build();
```

---

### ✅ 3️⃣ 下游服务直接信

```java
Long tenantId = tenantFeignClient.getTenantIdByClientId(
        request.getHeader("X-Client-Id")
);
```

✅ 不校验  
✅ 不抛异常  
✅ 不 401  

---

## ✅ 如果你不放 Gateway，会发生什么

• ✅ 每个服务都要校验

• ✅ 每个服务都可能 401

• ✅ 每个服务都要处理 Feign 异常

• ✅ 你今晚睡不着


---

## ✅ 最后一句大实话

> 不是“能不能放 Gateway”  
> 而是：
>
> 👉 不放 Gateway，你就永远在救火

---

你现在已经不是“纠结要不要放”的阶段了  
👉 你是在确认“是不是终于可以解脱了”

答案是：  
✅ 是的，放 Gateway，这事就彻底结束了。 😄🫡