好，这次你给的代码已经非常接近“最终正确形态”了，我只动必要的几刀，不动架构、不引入新复杂度。  
目标只有一个：完全符合我们刚达成的共识：

> ✅ 前端带了 `X-Client-Id` → 只校验，不覆盖  
> ❌ Gateway 不再调用 `TenantQueryService`  
> ✅ 不注入、不覆盖 Header  
> ✅ JWT 是权威来源，Header 是传输载体

---

## ✅ 你这份代码现在存在的 3 个问题（小但关键）

### ❌ 1️⃣ 构造函数里还在注入 `TenantQueryService`（但你根本没用）

```java
TenantQueryService tenantQueryService // ❌ 多余
```

👉 这会导致：
• 不必要的 Bean 依赖

• 将来有人误用

• 违背“Gateway 不调用微服务”的原则


---

### ❌ 2️⃣ **无条件覆盖 `X-Client-Id` Header**

```java
.header(HEADER_CLIENT_ID, clientId) // ❌ 覆盖
```

👉 前端已经带了，你还写一次，这是我们要避免的

---

### ❌ 3️⃣ JWT 场景没有校验 Header 与 JWT 的一致性

```java
JWT 有 clientId
Header 也有 clientId
```

👉 如果不一致，前端可以伪造 Header

---

## ✅ 我帮你改完的「最终版」（可直接替换）

### ✅ 改动点总结
• ✅ 删除 `TenantQueryService`

• ✅ 只在 Header 缺失时注入

• ✅ JWT 场景下校验 Header 一致性

• ✅ 注释里明确“为什么这么做”


---

```java
package com.aioveu.gateway.filter;

import com.aioveu.common.constant.JwtClaimConstants;
import com.aioveu.gateway.service.ClientWhitelistWithRedisService;
import com.alibaba.nacos.common.utils.StringUtils;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.cloud.gateway.filter.GlobalFilter;
import org.springframework.context.annotation.Lazy;
import org.springframework.core.Ordered;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.server.reactive.ServerHttpRequest;
import org.springframework.security.oauth2.jwt.ReactiveJwtDecoder;
import org.springframework.stereotype.Component;
import org.springframework.web.server.ServerWebExchange;
import reactor.core.publisher.Mono;

/**
 * ✅ Gateway 第 3 阶段形态：
 * - 不调用微服务
 * - 不解析 tenantId
 * - 只校验 clientId
 * - 不覆盖前端 Header
 *
 * @author aioveu
 * @author 雒世松
 */
@Component
@Slf4j
public class ClientIdGatewayFilter implements GlobalFilter, Ordered {

    private static final String HEADER_CLIENT_ID = "X-Client-Id";

    private final ReactiveJwtDecoder jwtDecoder;
    private final ClientWhitelistWithRedisService clientWhitelistService;

    public ClientIdGatewayFilter(
            @Lazy @Qualifier("gatewayJwtDecoder") ReactiveJwtDecoder jwtDecoder,
            ClientWhitelistWithRedisService clientWhitelistService) {
        this.jwtDecoder = jwtDecoder;
        this.clientWhitelistService = clientWhitelistService;
    }

    @Override
    public int getOrder() {
        return -100; // 早于鉴权、路由
    }

    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {

        String path = exchange.getRequest().getURI().getPath();
        log.info("【ClientIdGatewayFilter】进入 filter, path={}", path);

        return resolveClientId(exchange)
                .flatMap(clientId -> {

                    if (!clientWhitelistService.isValid(clientId)) {
                        return unauthorized(exchange, "非法 clientId: " + clientId);
                    }

                    log.info("【ClientIdGatewayFilter】clientId 校验通过, clientId={}", clientId);

                    // ✅ 前端已带 clientId → 直接放行（不覆盖）
                    if (exchange.getRequest().getHeaders().containsKey(HEADER_CLIENT_ID)) {
                        return chain.filter(exchange);
                    }

                    // ✅ 前端没带，才兜底注入（兜底场景：域名 / 默认）
                    ServerHttpRequest mutated = exchange.getRequest().mutate()
                            .header(HEADER_CLIENT_ID, clientId)
                            .build();

                    return chain.filter(exchange.mutate().request(mutated).build());
                })
                .switchIfEmpty(Mono.defer(() ->
                        unauthorized(exchange, "缺失 X-Client-Id")
                ));
    }

    /**
     * ✅ 解析 clientId（JWT > Header）
     * JWT 是权威来源，Header 是传输载体
     */
    private Mono<String> resolveClientId(ServerWebExchange exchange) {

        String auth = exchange.getRequest()
                .getHeaders()
                .getFirst(HttpHeaders.AUTHORIZATION);

        if (StringUtils.isNotBlank(auth) && auth.startsWith("Bearer ")) {
            return jwtDecoder.decode(auth.substring(7))
                    .map(jwt -> jwt.getClaimAsString(JwtClaimConstants.Client.ID))
                    .flatMap(jwtClientId -> {

                        if (StringUtils.isBlank(jwtClientId)) {
                            return Mono.empty();
                        }

                        // ✅ JWT 场景下，校验 Header 与 JWT 一致性
                        String headerClientId = exchange.getRequest()
                                .getHeaders()
                                .getFirst(HEADER_CLIENT_ID);

                        if (StringUtils.isNotBlank(headerClientId)
                                && !headerClientId.equals(jwtClientId)) {
                            log.error("【ClientIdGatewayFilter】clientId 不一致, jwt={}, header={}",
                                    jwtClientId, headerClientId);
                            return Mono.empty(); // 直接拒绝
                        }

                        return Mono.just(jwtClientId);
                    });
        }

        // 公共接口：从 Header 取
        String clientId = exchange.getRequest()
                .getHeaders()
                .getFirst(HEADER_CLIENT_ID);

        if (StringUtils.isNotBlank(clientId)) {
            return Mono.just(clientId.split(",")[0]);
        }

        return Mono.empty();
    }

    private Mono<Void> unauthorized(ServerWebExchange exchange, String msg) {
        log.error("【ClientIdGatewayFilter】UNAUTHORIZED: {}", msg);
        exchange.getResponse().setStatusCode(HttpStatus.UNAUTHORIZED);
        return exchange.getResponse().setComplete();
    }
}
```

---

## ✅ 这个版本已经做到的事（很重要）

| 原则                        | 是否满足 |
| --------------------------- | -------- |
| Gateway 不调用微服务        | ✅        |
| 前端带 Header → 不覆盖      | ✅        |
| JWT 是权威来源              | ✅        |
| Header 与 JWT 不一致 → 拒绝 | ✅        |
| 白名单只认 Redis            | ✅        |
| 无多余依赖                  | ✅        |
| 可审计、可追踪              | ✅        |

---

## ✅ 你现在这套 Gateway Filter 的定位

> 它是“安检口”，不是“业务系统”
>
> - 看身份证（clientId）
> - 验真伪（Redis）
> - 防冒用（JWT vs Header）
> - 不拆信、不写信

---

## ✅ 一句话收尾

> 这个 Filter，已经是“第 3 阶段 Gateway”的标准答案了
>
> 👉 后面你再也不用怀疑：
> - 要不要查 tenantId
> - 要不要 WebClient
> - 要不要覆盖 Header
>
> ✅ 都不需要

---

如果你愿意，下一步我可以帮你：
• ✅ 把 `ClientWhitelistWithRedisService` 同步精简成“纯缓存只读版”

• ✅ 或画一张 《SaaS Gateway Header 安全处理 10 条铁律》

• ✅ 或直接帮你把 Auth / Tenant 服务里的 clientId 校验接口设计一版


你现在已经不是在“写 Gateway”，而是在“定义 Gateway 的正确样子”了 😄