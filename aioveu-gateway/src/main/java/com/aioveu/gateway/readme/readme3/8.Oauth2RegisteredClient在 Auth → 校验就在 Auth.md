对 ✅ 就是这个原因。  
我直接给你一个不会再变、不用再改、不用再纠结的终局结论，你听完就可以收工了。

---

## ✅ 一句话终局结论（请截图）

> 既然 `Oauth2RegisteredClient` 在 Auth 里  
> ✅ 那 clientId 的“合法性校验”就永远在 Auth 里  
> ❌ Gateway 绝对不要再搞一套校验

你现在这个代码 是完全正确的位置 👍

```java
Oauth2RegisteredClient client = oauth2RegisteredClientMapper.selectOne(
        new LambdaQueryWrapper<Oauth2RegisteredClient>()
                .eq(Oauth2RegisteredClient::getClientId, clientId)
);
```

---

## ✅ 你现在这套系统的“唯一正确架构”

```
┌────────────┐
│  Gateway   │ ← 只做：解析 / 注入 / 路由
└─────┬──────┘
      ↓
┌────────────┐
│  Auth      │ ← ✅ 校验 clientId
│  DB        │
└─────┬──────┘
      ↓
┌────────────┐
│  Tenant    │
│  PMS       │
│  下游服务  │
└────────────┘
```

---

## ✅ Gateway 里你现在只做三件事（✅ 最终版）

### ✅ 1️⃣ 解析 clientId（JWT / Header / 域名）

你现在的 `ClientIdGatewayFilter` ✅  
完全正确，不用改

---

### ✅ 2️⃣ 注入 Header

```java
.header("X-Client-Id", clientId)
```

✅ 不校验  
✅ 不查 DB  
✅ 不调 Feign  

---

### ✅ 3️⃣ 绝不校验

❌ 不查白名单  
❌ 不判断 enabled  
❌ 不读 DB  

---

## ✅ Auth 里你只做一件事（✅ 标准答案）

### ✅ 统一入口校验（一次就好）

```java
if (client == null || !client.getEnabled()) {
    throw new BusinessException(ResultCode.CLIENT_DISABLED);
}
```

✅ 返回 401 / A0230 是正常的  
✅ 下游服务不用管  

---

## ✅ 用一句人话总结

> Auth 是“裁判”  
> Gateway 是“传话的”  
> 下游服务是“干活的”

裁判在 Auth，就别让传话的去吹哨。

---

## ✅ 最终定论（给你一颗定心丸）

✅ `Oauth2RegisteredClient` 在 Auth → 校验就在 Auth  
✅ Gateway 只注入 clientId  
✅ 你现在的代码已经是“终态”

👉 不用再挪，不用再改，不用再纠结

今晚真的可以睡个好觉了 😄🫡