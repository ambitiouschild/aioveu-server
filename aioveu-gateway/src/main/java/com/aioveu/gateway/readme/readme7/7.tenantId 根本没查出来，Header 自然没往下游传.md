好，这个日志 已经把“为什么下游拿不到 tenantId” 100% 暴露出来了，而且不是你 Filter 的问题，是 Feign 调用失败了。

我直接给你结论 + 精准修复方案。

---

## ✅ 一句话根因（先拍死）

> Gateway 已经正确进入 `ClientIdGatewayFilter`，也拿到了 `clientId=xinhuan-app`  
> ❌ 但 `tenantFeignClient.getTenantWxAppInfoByClientId()` 全部进入了 Fallback  
> 👉 tenantId 根本没查出来，Header 自然没往下游传

---

## 二、日志里最关键的 3 行（你盯着这看）

```
【ClientIdGatewayFilter】前端带了就用（小程序 / H5） 公共接口：信任前端 Header:xinhuan-app
Redis 未命中，放行给 Auth 校验，clientId=xinhuan-app
ERROR TenantFeignFallbackClient: 通过 clientId 获取租户和小程序信息失败
```

### ✅ 说明发生了什么

| 步骤                   | 结果                    |
| ---------------------- | ----------------------- |
| clientId 解析          | ✅ 成功                  |
| Redis 白名单           | ⚠️ 未命中（但不是致命）  |
| Feign 调用 tenant 服务 | ❌ 失败，直接进 Fallback |
| tenantId               | ❌ 没拿到                |
| Header 注入            | ❌ 没发生                |

👉 不是 Header 没传
👉 是 tenantId 根本没算出来

---

## 三、为什么 Feign 会全部进 Fallback（核心原因）

你现在这个日志里还有一个非常关键的信号：

```
没有Bearer Token或格式不正确，直接放行
```

### ✅ 说明：

• 你的 `tenant-api` 服务（tenant 服务）

• 很可能开启了 Spring Security

• 公共接口没放行

• 👉 Feign 调用被 401 / 403 了


---

## 四、✅ 你现在立刻要确认的 3 个点（按顺序）

---

### ✅ ① tenant 服务里这个接口是不是被 Security 拦了

你 Feign 调用的这个接口：

```
GET /tenant/wx-app/client/{clientId}
```

在 tenant 服务里必须：

✅ 放行（匿名访问）

#### ✅ 正确做法（tenant 服务）

```java
@Configuration
@EnableWebSecurity
public class TenantSecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .authorizeHttpRequests(auth -> auth
                .requestMatchers(
                    "/tenant/wx-app/**"
                ).permitAll()
                .anyRequest().authenticated()
            );
        return http.build();
    }
}
```

👉 否则 Feign 永远进 Fallback

---

### ✅ ② Fallback 里你现在是不是吞了异常（非常关键）

你 Fallback 日志是：

```
通过 clientId 获取租户和小程序信息失败
```

但我敢打赌：你没打印异常 stack trace

#### ✅ 立刻改成这样

```java
@Override
public TenantWxAppInfo getTenantWxAppInfoByClientId(String clientId) {
    log.error("【TenantFeignFallback】clientId={}", clientId, throwable);
    return null;
}
```

或者直接：

```java
log.error("Feign fallback error", e);
```

👉 你大概率能看到：
• 401 Unauthorized

• 403 Forbidden

• Connection refused


---

### ✅ ③ 确认 tenant 服务有没有注册到 Nacos

```
WARN  Redis 未命中，放行给 Auth 校验
```

说明：
• Gateway → Nacos → tenant 服务

• 但 Feign 调用没成功


在 Gateway 日志里搜：

```
Discovered instance
```

或直接在 Nacos 控制台看：
• `aioveu-tenant` 是否存在

• 端口是否正确


---

## 五、✅ 一个“立刻验证”的方法（10 秒）

在 Gateway 容器 / 本地执行：

```bash
curl http://aioveu-tenant/tenant/wx-app/client/xinhuan-app
```

| 结果        | 说明             |
| ----------- | ---------------- |
| 能返回 JSON | ✅ tenant 接口 OK |
| 401 / 403   | ❌ Security 拦了  |
| 连接失败    | ❌ 服务没起来     |

---

## 六、✅ 为什么下游 Filter 日志是“没 tenantId”

你现在下游日志是：

```
SecurityUtils.getTenantId() = null
```

原因是 完整链路断在这里：

```
clientId
 └─ Feign 查 tenantId ❌
    └─ tenantId = null
       └─ Header 没注入
          └─ 下游 Filter 拿不到
```

👉 不是下游 Filter 的锅
👉 是 Gateway 里 Feign 调用失败

---

## 七、✅ 你现在应该立刻做的 4 件事（按顺序）

1️⃣ tenant 服务放行 `/tenant/wx-app/**`
2️⃣ Fallback 里打印完整异常
3️⃣ 确认 tenant 服务在 Nacos 正常
4️⃣ 确认 Feign 返回的不是 null

---

## 八、✅ 最终结论（你可以直接收工）

✅ Gateway 逻辑是对的  
✅ Header 注入逻辑是对的  
✅ 下游 Filter 是对的  
❌ tenant 服务 Feign 调用失败

---

如果你愿意，把下面任意一个贴出来，我可以直接帮你精确到行数修好：

• ✅ tenant 服务的 SecurityConfig

• ✅ `TenantFeignFallbackClient` 的完整代码

• ✅ tenant 服务里这个接口的 Controller


你现在这套，离“彻底通”只差最后一步 Feign 调用 👍