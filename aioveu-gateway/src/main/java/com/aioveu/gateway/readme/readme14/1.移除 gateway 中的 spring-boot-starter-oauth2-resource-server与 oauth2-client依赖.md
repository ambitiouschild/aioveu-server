好，这次不用猜了，POM 就是铁证。  
我直接给你一个“你现在就该做”的最终行动清单，不绕弯子，不考古。

---

## ✅ 先给你一个“死刑判决”

> 这个 POM 本身就是“错的”
>
> ❌ Gateway 不应该依赖：
> - `spring-boot-starter-oauth2-resource-server`
> - `spring-boot-starter-oauth2-client`
>
> ✅ 你现在遇到的所有 401、Security 拦截、Filter 不执行，都是这俩依赖造成的

你不是“配置错了”，你是一开始就把不该进来的东西引进来了。

---

## ✅ 你现在面临的选择（只有两条路）

### ✅ 路线 A（强烈推荐，架构正确）
> 让 Gateway 回归“纯粹转发”

### ❌ 路线 B（继续挣扎）
> 继续让 Gateway 当 Resource Server

👉 我直接建议你：选 A

---

## ✅ 你“现在立刻”要做的 3 件事（按顺序）

---

### ✅ 第 1 步：删掉这两个依赖（必须删）

```xml
<!-- ❌ 删掉 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
</dependency>

<!-- ❌ 删掉 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-oauth2-client</artifactId>
</dependency>
```

✅ 删完 `mvn clean compile`  
✅ 启动后 401 会立刻消失

---

### ✅ 第 2 步：只保留“JWT 解析工具”（不是 Security）

你现在的 JWT 用法是：

```java
ReactiveJwtDecoder jwtDecoder;
jwtDecoder.decode(token)
```

✅ 你只是解析 claim
❌ 你不是在“校验合法性”

所以你只需要这两个（不是 starter）：

```xml
<!-- ✅ 保留 -->
<dependency>
    <groupId>org.springframework.security</groupId>
    <artifactId>spring-security-oauth2-core</artifactId>
</dependency>

<!-- ✅ 保留 -->
<dependency>
    <groupId>org.springframework.security</groupId>
    <artifactId>spring-security-oauth2-jose</artifactId>
</dependency>
```

⚠️ 但注意：  
这两个只是工具库，不会自动启用 Security。

---

### ✅ 第 3 步（强烈建议）：换成“零 Security”的 JWT 解析

如果你愿意，把 `ReactiveJwtDecoder` 彻底干掉，换成：

```xml
<dependency>
    <groupId>com.nimbusds</groupId>
    <artifactId>nimbus-jose-jwt</artifactId>
    <version>9.37</version>
</dependency>
```

然后这样写：

```java
SignedJWT jwt = (SignedJWT) JWTParser.parse(token);
Long tenantId = jwt.getJWTClaimsSet().getLongClaim("tenant_id");
```

✅ 零 Spring Security
✅ 零自动配置
✅ 零 401 惊喜

---

## ✅ 删完后，你的 Gateway POM 应该长这样（干净版）

```xml
<!-- Gateway -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-gateway</artifactId>
</dependency>

<!-- 服务发现 -->
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
</dependency>

<!-- 负载均衡 -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-loadbalancer</artifactId>
</dependency>

<!-- 配置中心 -->
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
</dependency>

<!-- Redis -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis-reactive</artifactId>
</dependency>

<!-- JWT 解析（工具级） -->
<dependency>
    <groupId>com.nimbusds</groupId>
    <artifactId>nimbus-jose-jwt</artifactId>
    <version>9.37</version>
</dependency>

<!-- Sentinel -->
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-alibaba-sentinel-gateway</artifactId>
</dependency>

<!-- Actuator -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>

<!-- Common -->
<dependency>
    <groupId>com.aioveu</groupId>
    <artifactId>common-core</artifactId>
</dependency>

<dependency>
    <groupId>com.aioveu</groupId>
    <artifactId>common-redis</artifactId>
</dependency>
```

✅ 没有 Security
✅ 没有 OAuth2
✅ 没有 Resource Server
✅ 没有 Client

---

## ✅ 如果你**暂时不敢删**（过渡方案）

那至少加这个配置类（之前给你过的）：

```java
@Configuration
@EnableWebFluxSecurity
public class GatewaySecurityConfig {

    @Bean
    public SecurityWebFilterChain springSecurityFilterChain(ServerHttpSecurity http) {
        http
            .csrf(ServerHttpSecurity.CsrfSpec::disable)
            .authorizeExchange(e -> e.anyExchange().permitAll())
            .oauth2ResourceServer(oauth2 -> oauth2.disable());
        return http.build();
    }
}
```

⚠️ 这只是止血，不是根治

---

## ✅ 给你一句“能写在 Git Commit 里”的话

> 移除 gateway 中的 `spring-boot-starter-oauth2-resource-server` 与 `oauth2-client` 依赖，避免 Spring Security 自动配置在网关层拦截请求；网关仅保留 JWT 解析能力，认证与鉴权职责下沉至资源服务器。

---

## ✅ 最后说一句心里话

你这几天踩的坑，本质上是：

> “我想让 Gateway 解析 JWT，结果不小心把它变成了 Resource Server”

这不是低级错误，这是Spring Security 自动配置设计得“太聪明”导致的陷阱。

你现在做的事，已经是中台架构师级别的纠偏了。

要我帮你把 `ClientIdGatewayFilter` 改成 nimbus 版本吗？  
改完之后，你这个 Gateway 就是“教科书级干净网关”了。