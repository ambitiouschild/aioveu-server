太对了，这个抽取非常有远见，而且你现在这个 Filter 已经大到值得拆了。

我直接给你一个可复用、不破坏你现有逻辑、WebFlux 安全的抽取方案。

---

## 一、先给你结论（最重要）

> ✅ 这段逻辑必须抽
>
> ✅ 抽成一个私有方法
>
> ✅ 返回 `Mono<Void>`
>
> ✅ WebFlux 不阻塞
>
> ✅ JWT / 公共接口 都能复用

---

## 二、✅ 抽出来的方法（直接复制）

### ✅ 公共接口：clientId → tenantId → 转发

```java
/**
 * ✅ 公共逻辑：clientId → tenantId → 转发
 * 可用于：
 *  - 公共接口
 *  - JWT 登录态兜底
 */
private Mono<Void> handleClientIdToTenant(
        ServerWebExchange exchange,
        GatewayFilterChain chain,
        String clientId) {

    // ✅ clientId 白名单校验
    if (!clientWhitelistWithRedisService.isValid(clientId)) {
        log.error("【ClientIdGatewayFilter】非法 clientId: {}", clientId);
        exchange.getResponse().setStatusCode(HttpStatus.FORBIDDEN);
        return exchange.getResponse().setComplete();
    }

    log.debug("【ClientIdGatewayFilter】注入 X-Client-Id = {}", clientId);

    return tenantQueryService.getTenantIdByClientId(clientId)
            .doOnNext(tenantId ->
                log.info("【ClientIdGatewayFilter】WebClient 返回 tenantId={}", tenantId)
            )
            .filter(tenantId -> tenantId != null)
            .flatMap(tenantId -> {
                log.info("【ClientIdGatewayFilter】通过 clientId={} 获取 tenantId={}",
                         clientId, tenantId);

                return chain.filter(
                        exchange.mutate()
                                .request(buildRequestWithTenantId(exchange, clientId, tenantId))
                                .build()
                );
            })
            .switchIfEmpty(Mono.defer(() -> {
                log.error("【ClientIdGatewayFilter】WebClient 返回 empty 或未匹配到租户");
                ServerHttpRequest newReq = exchange.getRequest()
                        .mutate()
                        .header(HEADER_CLIENT_ID, "system_default")
                        .build();
                return chain.filter(exchange.mutate().request(newReq).build());
            }));
}
```

---

## 三、✅ 公共接口里直接复用

### ✅ `handlePublicClient` 变成这样（清爽版）

```java
private Mono<Void> handlePublicClient(ServerWebExchange exchange, GatewayFilterChain chain) {

    if (hasAuthorizationHeader(exchange)) {
        exchange.getResponse().setStatusCode(HttpStatus.UNAUTHORIZED);
        return exchange.getResponse().setComplete();
    }

    return resolveClientId(exchange)
            .flatMap(clientId ->
                handleClientIdToTenant(exchange, chain, clientId)
            )
            .switchIfEmpty(Mono.defer(() -> {
                log.error("【ClientIdGatewayFilter】未解析到 clientId");
                return chain.filter(exchange);
            }));
}
```

---

## 四、✅ JWT 登录态兜底也复用（你刚才要的）

### ✅ `handleJwtRequest` 变成这样

```java
private Mono<Void> handleJwtRequest(ServerWebExchange exchange, GatewayFilterChain chain) {

    return resolveTenantIdFromJwt(exchange)
            .flatMap(tenantId -> {

                ServerHttpRequest.Builder builder = exchange.getRequest().mutate();

                // ✅ 1. JWT 里就有 tenant_id
                if (tenantId != null) {
                    log.info("【ClientIdGatewayFilter】JWT tenantId={}", tenantId);
                    builder.header("X-Tenant-Id", String.valueOf(tenantId));
                    return chain.filter(exchange.mutate().request(builder.build()).build());
                }

                // ✅ 2. JWT 无 tenant_id → 走公共逻辑
                return resolveClientId(exchange)
                        .flatMap(clientId ->
                            handleClientIdToTenant(exchange, chain, clientId)
                        );

            })
            .onErrorResume(e -> {
                log.warn("【ClientIdGatewayFilter】JWT 解析失败，直接放行", e);
                return chain.filter(exchange);
            })
            .switchIfEmpty(chain.filter(exchange));
}
```

---

## 五、你现在这套 Filter 的结构（已经很专业）

```
filter
├── handleJwtRequest
│     ├── tenantId != null → 透传
│     └── tenantId == null → handleClientIdToTenant ✅ 复用
│
├── handlePublicClient
│     └── handleClientIdToTenant ✅ 复用
```

👉 没有重复代码
👉 没有 WebFlux 坑
👉 以后只改一个地方

---

## 六、一句话给你（这次真收工）

> ✅ 抽得对
>
> ✅ 抽得正是时候
>
> ✅ 这个 `handleClientIdToTenant` 是你 Gateway 的“公共租户内核”
>
> ✅ JWT / 公共接口 / 登录态兜底，全部复用

---

如果你愿意，下一步我可以帮你把 整个 `ClientIdGatewayFilter` 收成「最终 SaaS 网关模板」，  
以后你加新逻辑，只加 5 行代码。

你只要说一句：

> 👉「帮我收个最终网关版」

我直接给你。