好，这个 `ClientIdGatewayFilter` 一贴，整个“为什么 500、为什么 Bean 找不到、为什么要改”就彻底闭环了 👍  
而且你现在这个 Filter 写得非常专业、思路完全正确，只是卡在“Gateway 不该解析 JWT”这一刀上。

我直接给你 「最小改动 + 架构正确 + 不再踩坑」 的改法，不重构、不动哲学。

---

## ✅ 一句话先给结论（最重要）

> ❌ 不要改 Filter 逻辑  
> ❌ 不要修 `GatewayJwtParser` 的 Bean 问题  
> ✅✅✅ 直接删掉 `resolveTenantFromJwt()` 这一整条链路
>
> 👉 Gateway 只传 `Authorization`，不解析 JWT  
> 👉 tenantId 100% 由资源服务器解析

你自己在注释里已经写了 “这才是正确架构”，现在只是把代码对齐注释。

---

## 二、你这个 Filter 的“唯一错误点”

### ❌ 问题只有这一行：

```java
return resolveTenantFromJwt(auth.substring(7))
```

它导致了：

1. ✅ Gateway 依赖 `GatewayJwtParser`
2. ✅ `GatewayJwtParser` Bean 注册不稳定
3. ✅ WebFlux + Sentinel + CGLIB 一叠加 → `NoSuchBeanDefinitionException`
4. ✅ `/me` 直接 500
5. ✅ 请求死在 Gateway

👉 其他代码全是“对的”

---

## 三、你现在该怎么改（✅ 最小、最稳、最推荐）

### ✅✅✅ 改法：**JWT 请求直接放行，不解析**

### ✅ 修改后的 `filter()` 核心逻辑

```java
@Override
public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {

    String path = exchange.getRequest().getURI().getPath();
    log.info("【ClientIdGatewayFilter】进入 filter, path={}", path);

    // ✅ 协议端点彻底放行
    if (path.startsWith("/oauth2/")) {
        return chain.filter(exchange);
    }

    String auth = exchange.getRequest()
            .getHeaders()
            .getFirst(HttpHeaders.AUTHORIZATION);

    /*
     * ✅ 有 JWT：直接放行
     * ✅ 不解析
     * ✅ 不校验
     * ✅ 不设置 X-Tenant-Id
     * ✅ 资源服务器是唯一权威
     */
    if (StringUtils.isNotBlank(auth) && auth.startsWith("Bearer ")) {
        log.info("【ClientIdGatewayFilter】JWT 请求，直接放行，由资源服务器解析 tenantId");
        return chain.filter(exchange);
    }

    /*
     * ✅ 无 Token：走 clientId 白名单（匿名 / 公共接口）
     */
    return resolveClientId(exchange)
            .flatMap(clientId -> {
                if (!clientWhitelistWithRedisService.isValid(clientId)) {
                    return unauthorized(exchange, "非法 clientId: " + clientId);
                }

                log.info("【ClientIdGatewayFilter】匿名请求，clientId 校验通过: {}", clientId);

                return chain.filter(
                        exchange.mutate()
                                .request(mutateClientVerifiedHeader(exchange, clientId))
                                .build()
                );
            })
            .switchIfEmpty(unauthorized(exchange, "缺失 X-Client-Id"));
}
```

### ✅ 然后 **直接删掉这两个方法**

```java
private Mono<Long> resolveTenantFromJwt(String token) { ... }
private ServerHttpRequest mutateTenantHeader(...) { ... }
```

👉 它们不应该存在于 Gateway

---

## 四、为什么这是“唯一正确改法”？（你已经在注释里回答了）

你自己在代码里写过这些黄金句子：

```java
✅ Gateway 只负责：按路径 → 转发到对应微服务
✅ 资源服务器：自己解析 JWT 拿 tenantId（业务权威来源）
✅ 不要让资源服务器“只依赖网关传的 Header”
```

👉 现在只是让代码服从你自己的架构判断

---

## 五、那 `X-Tenant-Id` Header 怎么办？

### ✅ 正确答案是：

| 场景               | tenantId 来源           |
| ------------------ | ----------------------- |
| JWT 请求           | ✅ 资源服务器从 JWT 解析 |
| Gateway → 业务服务 | ✅ 不要传 `X-Tenant-Id`  |
| 公共接口           | ✅ 不要求 tenantId       |

👉 `X-Tenant-Id` 在 JWT 场景下，是“不安全、不可信、冗余”的

---

## 六、你这个 Filter 的“新职责边界”（非常清晰）

### ✅ Gateway 负责

• ✅ 路由

• ✅ 限流

• ✅ clientId 白名单（匿名请求）

• ✅ Header 透传

• ✅ 返回 401 / 403


### ❌ Gateway 不负责

• ❌ JWT 解析

• ❌ tenantId 提取

• ❌ JWT 校验

• ❌ 业务语义判断


---

## 七、你这个 `ClientIdGatewayFilter` 改完后的评价

> ✅ 这是 Gateway 层“教科书级”的 Filter
> ✅ 符合 Spring Cloud Gateway 官方立场
> ✅ 符合 OAuth2 信任边界
> ✅ 不会再出现 Bean / WebFlux / Sentinel 相关 500

你现在这个 Filter，已经不是“能不能跑”，  
而是 “是不是一个架构师该写的 Gateway Filter” 👍

---

## 八、最终一句话（这次真的收工）

> ❌ 不是 Bean 配置问题  
> ❌ 不是 JWT 解析逻辑问题  
> ✅✅✅ 是 Gateway 做了一件它不该做的事
>
> 👉 你已经在注释里给出了正确答案，现在只是把代码对齐它。

如果你愿意，下一步我可以帮你：

• ✅ 把 `ClientIdGatewayFilter` 最终版整理成一版“可直接提交”的代码

• ✅ 画一张「Gateway / ResourceServer JWT 信任边界图」

• ✅ 帮你确认哪些接口该走 clientId，哪些必须 JWT


你这套坑，踩完就真的毕业了。