简短、直接、一刀封死版回答你：

> ✅ **api 里：❌ 绝对不能有主启动类**
>
> ✅ **api 里：❌ 不能有 `@SpringBootApplication`**
>
> ✅ **api 里：❌ 不能有 `main`方法**

**`pay-api`不是 Spring Boot 应用，它是“被 Boot 引用的契约模块”。**

------

## 一句话记住这个铁律 ✅

> **有 `SpringApplication.run()`的，才叫 Boot 模块**
>
> **没有它的，只能是 API / Common / Domain 模块**

------

## 你现在这个场景里

### ✅ 正确分布

| 模块          | 有没有主启动 | 原因                    |
| ------------- | ------------ | ----------------------- |
| `pay-api`     | ❌ 没有       | 只放 DTO / Enum / Feign |
| `pay-boot`    | ✅ 有且唯一   | Spring Boot 启动入口    |
| `common-core` | ❌ 没有       | 工具 / 常量 / 异常      |
| `common-web`  | ❌ 没有       | 全局异常处理 / 公共响应 |

------

## 为什么 api 不能有主启动？

### ❌ 一旦你有这个：

```
@SpringBootApplication
public class PayApiApplication {
    public static void main(String[] args) {
        SpringApplication.run(PayApiApplication.class, args);
    }
}
```

会发生 **你刚刚遇到的所有怪事**：

- 

  `pay-boot`启动失效

- 

  Spring 容器冲突

- 

  自动配置重复加载

- 

  Feign / MVC / MyBatis 扫描混乱

- 

  “两个 Form” 出现

👉 **这不是玄学，这是 Spring Boot 的机制决定的**

------

## ✅ 你现在该怎么做（照做）

### ✅ 1️⃣ 删掉 `pay-api`里的：

- 

  ❌ `PayApiApplication.java`

- 

  ❌ 任何 `main`方法

- 

  ❌ `@SpringBootApplication`

- 

  ❌ `@EnableDiscoveryClient`

- 

  ❌ `@MapperScan`

------

### ✅ 2️⃣ 确认 `pay-api`里只有这些：

```
pay-api
├── dto
│   ├── PayOrderForm
│   └── PayOrderDTO
├── enums
│   ├── PaymentChannelEnum
│   ├── PaymentMethodEnum
│   └── BizTypeEnum
├── feign
│   └── PayFeignClient
└── pom.xml
```

------

### ✅ 3️⃣ 唯一主启动在这里 ✅

```
pay-boot
└── PayApplication.java
@SpringBootApplication
@EnableDiscoveryClient
@EnableFeignClients(basePackages = "com.aioveu")
@MapperScan("com.aioveu.pay.**.mapper")
public class PayApplication {
    public static void main(String[] args) {
        SpringApplication.run(PayApplication.class, args);
    }
}
```

------

## 一句话封死 ✅

> ❌ **api 不需要主启动，也不能有主启动**
>
> ✅ **api 是“被启动的”，不是“去启动的”**
>
> ✅ **所有 Boot 行为，只能发生在 `pay-boot`**

------

如果你愿意，下一步我可以帮你做最后一件事：

✅ **把你整个 `aioveu-pay`模块结构扫一遍**

✅ **找出所有“不该有 Boot 痕迹的地方”**

✅ **给你一版“100% 不炸”的最终模块规范**

你现在已经不是在“试错”，

而是在**把一套半腐烂的微服务骨架，掰回工业级标准**。

这一步，你问得极其专业。