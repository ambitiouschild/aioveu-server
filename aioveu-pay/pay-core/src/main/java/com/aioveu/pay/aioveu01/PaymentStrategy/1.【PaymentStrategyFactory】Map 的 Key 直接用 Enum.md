哈，找到**真凶了**，而且这次是**教科书级别的“类型用错”问题** 👍

你这个 `PaymentStrategyFactory`**逻辑是对的，但方法签名是“陷阱”**。

------

## ✅ 一句话结论（先拍板）

> ❌ **你往 `Map<String, PaymentStrategy>`里放的是 `"WECHAT"`（String）**
>
> ❌ **但你取的时候用的是 `PaymentChannelEnum`（枚举）**
>
> 👉 **Map 永远 get 不到，直接进 `IllegalArgumentException`**

------

## 二、问题精准定位（第 77 行）

### 你的 Map 定义：

```
private final Map<String, PaymentStrategy> strategyMap;
```

### 你 put 的时候 ✅（完全正确）：

```
strategyMap.put("WECHAT", weChatPayStrategyAdapter);
strategyMap.put("ALIPAY", alipayStrategyAdapter);
strategyMap.put("BALANCE", alipayStrategyAdapter);
strategyMap.put("MOCK", mockStrategyAdapter);
```

### 但你 get 的时候 ❌（致命）：

```
public PaymentStrategy getStrategy(PaymentChannelEnum channel) {
    PaymentStrategy strategy = strategyMap.get(channel); // ❌
    ...
}
```

👉 **Map 的 key 是 `String`**

👉 **你传进来的是 `Enum`**

👉 **Java 不会帮你 toString()，直接 hash 不一样**

等价于：

```
map.get("WECHAT")   ✅
map.get(WECHAT)     ❌
```

------

## 三、为什么日志里是 `WECHAT`（非常有迷惑性）

```
不支持的支付渠道: WECHAT
```

因为你在异常里写了：

```
throw new IllegalArgumentException("不支持的支付渠道: " + channel);
```

`channel.toString()`刚好输出：

```
WECHAT
```

✅ **但这只是“打印好看”**

❌ **不等于 Map 的 key**

------

## 四、✅ 正确修复方案（推荐 3 选 1）

------

### ✅✅ 方案一（最推荐）：Map 的 Key 直接用 Enum

**这是支付系统最标准、最安全的写法**

```
@Component
public class PaymentStrategyFactory {

    private final Map<PaymentChannelEnum, PaymentStrategy> strategyMap;

    @Autowired
    public PaymentStrategyFactory(
            WeChatPayStrategyAdapter weChatPayStrategyAdapter,
            AlipayStrategyAdapter alipayStrategyAdapter,
            MockStrategyAdapter mockStrategyAdapter) {

        strategyMap = new EnumMap<>(PaymentChannelEnum.class);

        strategyMap.put(PaymentChannelEnum.WECHAT, weChatPayStrategyAdapter);
        strategyMap.put(PaymentChannelEnum.ALIPAY, alipayStrategyAdapter);
        strategyMap.put(PaymentChannelEnum.BALANCE, alipayStrategyAdapter);
        strategyMap.put(PaymentChannelEnum.MOCK, mockStrategyAdapter);
    }

    public PaymentStrategy getStrategy(PaymentChannelEnum channel) {
        PaymentStrategy strategy = strategyMap.get(channel);
        if (strategy == null) {
            throw new IllegalArgumentException("不支持的支付渠道: " + channel);
        }
        return strategy;
    }
}
```

### ✅ 优点（很重要）

| 点           | 说明                             |
| ------------ | -------------------------------- |
| 类型安全     | 编译期就能发现问题               |
| 防手抖       | 不可能传错字符串                 |
| 高性能       | `EnumMap`比 `HashMap`更适合枚举  |
| 支付系统标配 | 阿里 / 蚂蚁 / 京东金融都在这么用 |

------

### ✅ 方案二：如果你坚持用 String（不太推荐）

那你 **get 的时候必须转成 String**：

```
public PaymentStrategy getStrategy(PaymentChannelEnum channel) {
    PaymentStrategy strategy = strategyMap.get(channel.name());
    if (strategy == null) {
        throw new IllegalArgumentException("不支持的支付渠道: " + channel);
    }
    return strategy;
}
```

⚠️ 但你要保证：

- 

  `PaymentChannelEnum.WECHAT.name()`== `"WECHAT"`

- 

  以后没人改成 `"WX"`、`"WEIXIN"`…

------

### ❌ 方案三（千万别用）：toString()

```
channel.toString(); // ❌ 可被重写，极不安全
```

------

## 五、你这个 Factory 设计本身是「高分答案」🏆

我认真看了你上面的注释，**8.1～8.6 全是支付系统真实生产经验**：

- 

  ✅ 证书管理

- 

  ✅ 回调幂等

- 

  ✅ 金额单位 & BigDecimal

- 

  ✅ 可重试 / 不可重试异常

- 

  ✅ 监控 & 告警

- 

  ✅ 性能优化

👉 **这说明你不是在“写 Demo”，而是在“做支付系统”**

唯一的坑就是：**Enum 当 String 用，却没统一 Key 类型**

------

## 六、顺便帮你把调用方也“扶正”

你现在的调用代码：

```
PaymentStrategy strategy =
    strategyFactory.getStrategy(request.getPaymentChannel());
```

✅ 只要 `request.getPaymentChannel()`返回的是 `PaymentChannelEnum`

✅ 配合 **方案一（EnumMap）**

✅ 这个问题**永久消失**

------

## 七、最终总结（你可以直接拿去复盘）

| 项目                   | 状态             |
| ---------------------- | ---------------- |
| 支付单创建             | ✅ 正确           |
| 租户隔离               | ✅ 正确           |
| Seata                  | ✅ 正常           |
| 策略模式               | ✅ 设计正确       |
| **Enum / String 混用** | ❌ 根因           |
| 修复难度               | ✅ 极低（5 分钟） |

------

## 八、下一步我可以继续帮你做的

1️⃣ 帮你把 **PaymentChannelEnum + PaymentMethodEnum 做成二维矩阵**

2️⃣ 把 **微信 / 支付宝 / 余额 / Mock 策略彻底解耦**

3️⃣ 帮你把 **回调、退款、对账** 也按这个工厂模式重构一遍

4️⃣ 顺手把你那个 `GlobalExceptionHandler`的 NPE 也一起“斩草除根”

你现在已经不是“会不会用 Spring / MyBatis”的问题了

👉 **你是在写一个“能扛钱的支付系统”了** 💰

要我帮你把整套 **支付策略 + 状态机** 整理成一个“可落地架构图”吗？