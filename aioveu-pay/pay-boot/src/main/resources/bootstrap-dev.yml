server:
  port: 8888

spring:
  main:
    allow-circular-references: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  cloud:
    nacos:
      # 配置中心  # 配置中心配置  先获取配置，后执行服务发现”
      config:
        enabled: true
        server-addr: ${NACOS_SERVER:localhost:8848} # 使用服务名而非localhost
        file-extension: yaml
        namespace: ${NACOS_NAMESPACE:a0e9ab31-f2c7-4c10-94a4-9f2f53e5a5ef} #aioveu-prod  # 生产环境命名空间
        group: ${NACOS_GROUP:AIOVEU_GROUP}
        username: ${NACOS_USER:aioveu}  # 从环境变量读取
        password: ${NACOS_PASSWORD:775825}   # 从环境变量读取
        #name: ${spring.application.name}  # 默认情况下，应用会使用spring.application.name作为数据ID的前缀，所以这里可以不用配置，但可以显式配置
        shared-configs:
          - data-id: aioveu-common.yaml  # 公共共享配置
            group: ${NACOS_GROUP:AIOVEU_GROUP} # 使用相同的分组
            refresh: true  # 启用动态刷新
          - data-id: aioveu-WeChatPay.yaml # 公共共享配置
            group: AIOVEU_PAY_GROUP  # 使用相同的分组
            refresh: true

          - data-id: aioveu-AliPay.yaml # 公共共享配置
            group: AIOVEU_PAY_GROUP  # 使用相同的分组
            refresh: true

          - data-id: aioveu-MockPay.yaml # 公共共享配置
            group: AIOVEU_PAY_GROUP  # 使用相同的分组
            refresh: true
      # 注册中心 # 服务发现配置

      discovery:
        enabled: true
        server-addr: ${spring.cloud.nacos.config.server-addr}
        namespace: ${spring.cloud.nacos.config.namespace}
        group: ${spring.cloud.nacos.config.group}
        username: ${spring.cloud.nacos.config.username}  # 复用config的认证
        password: ${spring.cloud.nacos.config.password}
        metadata:
          version: 1.0
          env: prod
          zone: zone1



# application.yml
alipay:
  # 基础配置
  app-id: 2021000116699999
  app-private-key: "MIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC3wN7c..."
  alipay-public-key: "MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEAuZ7K..."
  server-url: https://openapi.alipay.com/gateway.do

  # 证书模式配置（二选一）
  use-cert: false
  app-cert-path: /path/to/appCertPublicKey.crt
  alipay-cert-path: /path/to/alipayCertPublicKey.crt
  alipay-root-cert-path: /path/to/alipayRootCert.crt

  # 回调地址
  notify-url: https://your-domain.com/api/payment/alipay/notify
  return-url: https://your-domain.com/payment/return

  # 其他配置
  sign-type: RSA2
  charset: UTF-8
  format: json
  timeout: 10

  # 沙箱环境配置
  # server-url: https://openapi.alipaydev.com/gateway.do
  # app-id: 2016092700609999

  # application.properties
  # 支付宝配置
#  alipay.app-id=2021000116699999
#  alipay.app-private-key=MIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC3wN7c...
#  alipay.alipay-public-key=MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEAuZ7K...
#  alipay.server-url=https://openapi.alipay.com/gateway.do
#  alipay.notify-url=https://your-domain.com/api/payment/alipay/notify
#  alipay.return-url=https://your-domain.com/payment/return
#  alipay.sign-type=RSA2
#  alipay.charset=UTF-8
#  alipay.format=json