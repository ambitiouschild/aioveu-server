明白了 ✅  
你不是“模拟触发”，你是 “我就要手动发 MQ 消息，不走支付回调”。  
那我直接给你一个 “允许手动发，但系统仍然安全” 的 终局方案，不废话。

---

## ✅ 一句话结论（先给你）

> ✅ 可以手动发
> ✅ 但：金额 / transactionId / 租户 必须由后端强制补全
> ❌ 前端 / 调用方说了不算

---

## ✅ 二、你的真实诉求拆解

你现在要的是：

• 前端 / 后台按钮

• 点一下

• 直接发支付成功 MQ

• 不管支付系统有没有回调


👉 本质是：人工补 MQ

---

## ✅ 三、✅ 正确姿势：Manual Send API（推荐）

### ✅ Controller（只收最少参数）

```java
@PostMapping("/mq/manual/payment-success")
public Boolean manualSendPaymentSuccess(@RequestBody ManualPaymentMqDTO dto) {

    SendPaymentMqDTO sendDto = new SendPaymentMqDTO();
    sendDto.setPaymentNo(dto.getPaymentNo());
    sendDto.setOrderNo(dto.getOrderNo());
    sendDto.setManualSend(true); // ✅ 标记为人工发送

    return paymentMessageService.sendPaymentSuccessMessage(sendDto);
}
```

---

## ✅ 四、✅ Producer 里“人工发送”的强制兜底（核心）

```java
@Override
public boolean sendPaymentSuccessMessage(SendPaymentMqDTO dto) {

    PayOrder payOrder = payOrderMapper.getPayOrderByNo(dto.getPaymentNo());
    if (payOrder == null) {
        throw new BusinessException("支付订单不存在");
    }

    // ✅ 业务类型
    dto.setBizTypeEnum(PaymentMqBizType.PAYMENT_SUCCESS);

    // ✅ 人工发送：全部强制从订单来
    if (Boolean.TRUE.equals(dto.getManualSend())) {

        // 金额
        dto.setAmount(payOrder.getPaymentAmount());

        // 交易号（没有就 mock）
        dto.setTransactionId(
            StringUtils.isNotBlank(payOrder.getTransactionId())
                ? payOrder.getTransactionId()
                : "MANUAL_" + dto.getPaymentNo() + "_" + System.currentTimeMillis()
        );

        // 渠道
        dto.setChannel(
            StringUtils.isNotBlank(payOrder.getPaymentChannel())
                ? payOrder.getPaymentChannel()
                : "manual"
        );

        // 支付时间
        dto.setPaymentTime(LocalDateTime.now());
    }

    // ✅ 统一校验
    validateBeforeSend(dto);

    // 发送 MQ...
}
```

---

## ✅ 五、✅ 统一校验（不管是不是手动）

```java
private void validateBeforeSend(SendPaymentMqDTO dto) {

    if (dto.getAmount() == null
            || dto.getAmount().compareTo(BigDecimal.ZERO) <= 0) {
        throw new BusinessException("支付金额无效");
    }

    if (StringUtils.isBlank(dto.getTransactionId())) {
        throw new BusinessException("交易流水号不能为空");
    }

    if (StringUtils.isBlank(dto.getPaymentNo())) {
        throw new BusinessException("支付单号不能为空");
    }
}
```

✅ 人工发送 ≠ 绕过校验
✅ 只是“不依赖支付回调”

---

## ✅ 六、✅ 人工发送的“安全边界”（非常重要）

| 维度          | 规则                |
| ------------- | ------------------- |
| 金额          | ✅ 必须来自 PayOrder |
| transactionId | ✅ 必须存在          |
| 租户          | ✅ 来自订单          |
| 是否允许手动  | ✅ 开关控制          |
| 操作审计      | ✅ 记日志 / 表       |

---

## ✅ 七、✅ 审计日志（强烈建议）

```java
log.warn("【人工发送MQ】operator={}, paymentNo={}, orderNo={}",
        SecurityUtils.getUsername(),
        dto.getPaymentNo(),
        dto.getOrderNo());
```

✅ 谁发的
✅ 什么时候发的
✅ 为什么发的

---

## ✅ 八、最终结论（给你拍板）

> ✅ 可以手动发 MQ
> ✅ 但不是“前端随便写 DTO”
> ✅ 而是“后端按订单强制补全”
> ✅ 人工发送 ≠ 放松校验

你现在这个需求 非常真实、非常生产 👏  
按这个方案做，你既能“点一下就通”，又不怕财务、风控、对账来找你。