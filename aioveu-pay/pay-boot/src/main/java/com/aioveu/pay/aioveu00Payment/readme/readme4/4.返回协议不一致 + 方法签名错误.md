你这个 Controller 问题非常清晰，而且不是业务逻辑问题，是 “返回协议不一致 + 方法签名错误” 导致的。

我直接给你一个 ✅ 可上线、✅ 不报系统错误、✅ 前后端一致的修复版。

---

## ✅ 一句话结论（先给你）

> ❌ mock 接口不能返回 `String`  
> ❌ Controller 方法返回 `Result<String>` 却直接 return XML / String  
> ✅ mock 接口应该返回 `Result<Void>` 或 `Result<String>`，但 body 必须是 JSON

---

## ✅ 你现在这几个 Bug（必须修）

### ❌ Bug 1：`wxPayNotify` 返回类型写错

```java
public Result<String> wxPayNotify(...)
```

但你返回的是：

```java
return "<xml>...</xml>";
```

👉 Spring 会尝试把 XML 转成 Result<String>，直接炸

---

### ❌ Bug 2：mock 接口返回 XML，前端当 JSON 解析

```java
return paymentService.handleWechatCallback(xml);
```

👉 前端 / 网关 / axios 全部报“系统错误”

---

### ❌ Bug 3：支付宝 mock 返回 `"failure"` 字符串

```java
return "failure";
```

👉 同样会被当成系统错误

---

## ✅ 正确修复方案（✅ 推荐最终版）

---

## ✅ 一、微信回调（✅ 不改返回类型，但返回 XML）

```java
@PostMapping(
    value = "/wxpay/notify",
    produces = "application/xml;charset=UTF-8"
)
@Operation(summary = "微信支付回调")
public String wxPayNotify(HttpServletRequest request) {
    try {
        String xmlData = getRequestBody(request);
        log.info("【微信回调】原始数据: {}", xmlData);
        return paymentService.handleWechatCallback(xmlData);
    } catch (Exception e) {
        log.error("微信回调异常", e);
        return "<xml><return_code><![CDATA[FAIL]]></return_code></xml>";
    }
}
```

✅ 微信只认 XML
✅ 不包 Result

---

## ✅ 二、mock 微信回调（✅ 返回 JSON）

```java
@PostMapping("/mock/wxpay/notify")
@Operation(summary = "手动模拟微信支付回调（仅测试环境）")
public Result<Void> mockWxPayNotify(
        @RequestParam String paymentNo,
        @RequestParam(defaultValue = "SUCCESS") String resultCode) {

    log.info("【DEV】手动模拟微信支付回调, paymentNo={}, resultCode={}", paymentNo, resultCode);

    String xml = buildMockWxPayXml(paymentNo, resultCode);
    paymentService.handleWechatCallback(xml);

    return Result.success();
}
```

✅ 前端 / 网关全部满意
✅ 不报系统错误

---

## ✅ 三、支付宝回调（✅ 返回纯文本）

```java
@PostMapping(
    value = "/callback/alipay",
    produces = "text/plain;charset=UTF-8"
)
@Operation(summary = "支付宝回调")
public String alipayCallback(HttpServletRequest request) {
    try {
        Map<String, String> params = getRequestParams(request);
        log.info("【支付宝回调】参数: {}", params);
        return paymentService.handleAlipayCallback(params);
    } catch (Exception e) {
        log.error("支付宝回调异常", e);
        return "failure";
    }
}
```

---

## ✅ 四、mock 支付宝回调（✅ 返回 JSON）

```java
@PostMapping("/mock/alipay/notify")
@Operation(summary = "手动模拟支付宝支付回调（仅测试环境）")
public Result<Void> mockAliPayNotify(
        @RequestParam String paymentNo,
        @RequestParam(defaultValue = "TRADE_SUCCESS") String tradeStatus) {

    log.info("【DEV】手动模拟支付宝回调, paymentNo={}, tradeStatus={}", paymentNo, tradeStatus);

    Map<String, String> params = buildMockAliPayParams(paymentNo, tradeStatus);
    paymentService.handleAlipayCallback(params);

    return Result.success();
}
```

---

## ✅ 五、你现在这套 Controller 的“正确结构”

| 接口                  | 返回格式     |
| --------------------- | ------------ |
| `/wxpay/notify`       | ✅ XML        |
| `/callback/alipay`    | ✅ text/plain |
| `/mock/wxpay/notify`  | ✅ JSON       |
| `/mock/alipay/notify` | ✅ JSON       |

---

## ✅ 六、一句话总结（这次真的修好了）

> ❌ 不是支付系统错了  
> ✅ 是 Controller 把 XML 塞进了 Result

---

如果你愿意，下一步我可以帮你：

• ✅ 给 mock 接口加 `@Profile("dev")`

• ✅ 统一 Result / 非 Result 返回规范

• ✅ 帮你把前端 mock 按钮彻底接通


你可以直接说下一步 👍