这是一个非常关键、也非常专业的问题 👍  
我直接给你 结论 + 原因 + 你这套代码里「正确的做法」。

---

## ✅ 一句话结论（先给你）

> ✅ 发送记录必须在「发送前」保存  
> ❌ 绝不能在发送成功后保存

你现在这段代码 位置是对的（发送前） ✅  
但 `messageId` 的赋值顺序还有一点点隐患，我一并帮你收口。

---

## ✅ 二、为什么必须在「发送前」保存？

### ❌ 如果是「发送后保存」，会发生什么？

| 场景                  | 后果         |
| --------------------- | ------------ |
| MQ 发送成功，应用宕机 | 发送记录没了 |
| MQ 成功，DB 事务回滚  | 记录丢失     |
| 补偿任务扫描          | 找不到消息   |
| 对账                  | 永远对不上   |

👉 这是支付系统的大忌

---

## ✅ 三、✅ 正确的顺序（标准支付中台）

```
1️⃣ 生成 messageId
2️⃣ 保存发送记录（status = INIT / SENDING）
3️⃣ 发送 MQ
4️⃣ 更新发送状态（SUCCESS / FAILED）
```

✅ 哪怕 MQ 发了，应用挂了
✅ 补偿任务也能扫到

---

## ✅ 四、你现在这段代码「对在哪里，错在哪里」

### ✅ 对的地方

```java
// 保存发送记录
messageId = saveMqSendRecord(request);

// 发送消息
RabbitSendResult result = sendSingleMessage(request);
```

✅ 发送前保存 ✅

---

### ❌ 错的地方（很隐蔽）

```java
messageId = saveMqSendRecord(request);
```

👉 `saveMqSendRecord()` 不应该再返回 `messageId`

因为：
• `messageId` 已经在 `request` 里了

• 返回容易让人误以为“重新生成了”


---

## ✅ 五、✅ 我建议你改成这样（最终版）

### ✅ 1️⃣ 发送前：保存 INIT 状态

```java
mqSendRecordService.saveSendingRecord(request);
```

```java
public void saveSendingRecord(RabbitSendRequest request) {
    MqSendRecord record = new MqSendRecord();
    record.setMessageId(request.getMessageId());
    record.setExchange(request.getExchange());
    record.setRoutingKey(request.getRoutingKey());
    record.setBizId(request.getBizId());
    record.setStatus(SendStatus.SENDING.name());
    record.setCreateTime(LocalDateTime.now());
    save(record);
}
```

---

### ✅ 2️⃣ 发送后：只更新状态

```java
mqSendRecordService.updateSendStatus(
        request.getMessageId(),
        success ? SendStatus.SUCCESS : SendStatus.FAILED,
        success ? null : result.getErrorMessage()
);
```

---

## ✅ 六、你现在这个异常分支是 **对的 ✅**

```java
catch (Exception e) {
    if (messageId != null) {
        mqSendRecordService.updateSendStatus(
            messageId,
            SendStatus.FAILED,
            e.getMessage()
        );
    }
}
```

✅ 这是生产级写法

---

## ✅ 七、最终结论（你现在这一步非常关键）

> ✅ 发送记录在发送前保存  
> ❌ 绝不能在发送后保存  
> ✅ messageId 一旦生成，永不改变

你现在这套代码  
👉 只要把 `saveMqSendRecord` 改成「只保存，不返 ID」  
👉 就已经是标准支付中台实现了 👍

---

## 🚀 下一步我可以帮你

✅ 帮你把 `mq_send_record` 表结构最终定稿  
✅ 帮你把 补偿任务 + 重试策略 设计完整  
✅ 帮你画一张 MQ 发送 → 消费 → 补偿 时序图  

你可以直接说一句：  
👉 “帮我定 mq_send_record 的最终表结构”