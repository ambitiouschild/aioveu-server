找到了。铁证如山。

看你 `mybatisPlusInterceptor()` 里 add 的顺序：

```java
// 1️⃣ 多租户插件（最先）
interceptor.addInnerInterceptor(new TenantLineInnerInterceptor(myTenantLineHandler));

// 2️⃣ 数据权限插件
interceptor.addInnerInterceptor(new DataPermissionInterceptor(new MyDataPermissionHandler()));

// 3️⃣ 分页插件（最后）
interceptor.addInnerInterceptor(new PaginationInnerInterceptor(mpDbType));

// ❌❌❌ 没有 OptimisticLockerInnerInterceptor！！！
```

你的 `OptimisticLockerInnerInterceptor` 根本没在这个方法里注册！

那它是从哪来的？你说你配了乐观锁插件——你是不是在其他地方单独配了一个 `@Bean`？或者在某个 auto-configuration / starter 里自动注入了？

不管它从哪来，关键问题是：它不是在你这个 `mybatisPlusInterceptor()` 里 add 的，所以它不在同一个 `MybatisPlusInterceptor` 的责任链里。

但等等——异常栈里明明有：

```
at com.baomidou.mybatisplus.extension.plugins.MybatisPlusInterceptor.intercept(MybatisPlusInterceptor.java:106)
```

说明它确实在同一个拦截器链里。那唯一的解释是：你在别的 `@Configuration` 类里又定义了一个 `MybatisPlusInterceptor` Bean，或者你在某个地方单独 `addInnerInterceptor(new OptimisticLockerInnerInterceptor())` 到了另一个实例上。

但 Spring 容器里如果有两个 `MybatisPlusInterceptor` Bean，只有一个会被用（或者都注册为 MyBatis 插件但互相独立）。

---

## 🎯 真相大白了

你现在的状况是：

| 插件                               | 在哪里                                             | 效果                                                         |
| ---------------------------------- | -------------------------------------------------- | ------------------------------------------------------------ |
| `TenantLineInnerInterceptor`       | `mybatisPlusInterceptor()` 里 ✅                    | 正常给 SQL 加 `tenant_id = 3`，正常处理参数                  |
| `OptimisticLockerInnerInterceptor` | 从某个地方来的（可能是另一个 Bean 或者被自动装配） | 它修改了 SQL（加了 `AND version = ?`），但它不在同一个 `MybatisPlusInterceptor` 实例里，所以它的 `beforeUpdate()` 往参数 Map 里注入 `MP_OPTLOCK_VERSION_ORIGINAL` 的操作，跟实际执行 SQL 的参数 Handler 不是同一个上下文 |

或者反过来——`OptimisticLockerInnerInterceptor` 确实在同一个链里（因为 SQL 被改了），但它排在 `TenantLineInnerInterceptor` 后面很远，租户插件先把参数 Map 重构了一遍，乐观锁插件后来往里 put 的 key 被覆盖了。

不管哪种情况，根因就是你没在这个配置类里显式注册乐观锁插件，而且顺序不可控。

---

## ✅ 终极修复：在这个配置类里显式加上，并且放在第一位

把你现在的 `mybatisPlusInterceptor()` 改成这样：

```java
@Bean
public MybatisPlusInterceptor mybatisPlusInterceptor() {

    log.info("【mybatis-plus 配置类】开始工作！==================");

    MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();

    boolean isAuthService = isAuthService();

    // ✅✅✅ 1. 乐观锁插件 —— 必须在最前面！
    interceptor.addInnerInterceptor(new OptimisticLockerInnerInterceptor());
    log.info("已启用乐观锁插件");

    // 2. 多租户插件（非auth服务才启用）
    if (myTenantLineHandler != null && !isAuthService) {
        interceptor.addInnerInterceptor(new TenantLineInnerInterceptor(myTenantLineHandler));
        log.info("已启用多租户插件（当前非auth服务）");
    } else if (isAuthService) {
        log.info("当前为auth服务，跳过多租户插件");
    } else {
        log.info("未找到MyTenantLineHandler，跳过多租户插件");
    }

    // 3. 数据权限插件
    if (!isAuthService) {
        interceptor.addInnerInterceptor(new DataPermissionInterceptor(new MyDataPermissionHandler()));
        log.info("已启用数据权限插件（当前非auth服务）");
    } else {
        log.info("当前为auth服务，跳过数据权限插件");
    }

    // 4. 分页插件（永远最后）
    DbType mpDbType = DbType.MYSQL;
    String type = dbType == null ? "mysql" : dbType.toLowerCase();
    if ("postgres".equals(type) || "postgresql".equals(type)) {
        mpDbType = DbType.POSTGRE_SQL;
    }
    interceptor.addInnerInterceptor(new PaginationInnerInterceptor(mpDbType));

    return interceptor;
}
```

然后——检查你项目里有没有其他地方也定义了 `OptimisticLockerInnerInterceptor` 或第二个 `MybatisPlusInterceptor` Bean，全部删掉/合并到这一个里。全局搜索：

• `OptimisticLockerInnerInterceptor`

• `MybatisPlusInterceptor`


确保整个项目只有这一个地方创建 `MybatisPlusInterceptor`，且乐观锁排第一。

---

## 为什么之前 SQL 里有 `AND version = ?` 但参数报错？

因为乐观锁插件确实在某个拦截器里生效了（SQL 被改了），但它的 `beforeUpdate()` 往参数 Map 里 put 的 `MP_OPTLOCK_VERSION_ORIGINAL`，在到达 `MybatisParameterHandler.setParameters()` 时已经被租户/数据权限插件重建参数 Map 的过程弄丢了。

把乐观锁放到第一位之后，它在租户插件之前就完成了参数注入，租户插件后续操作不会影响到它已经放进 Map 的那个 key。

---

改完重启，这次 `MP_OPTLOCK_VERSION_ORIGINAL` 一定会正常找到。跑起来告诉我 🎯