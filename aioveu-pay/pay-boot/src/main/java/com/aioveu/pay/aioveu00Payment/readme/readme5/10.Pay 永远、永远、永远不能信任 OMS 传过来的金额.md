我认真把这份 `PaymentServiceImpl`完整看了一遍，**结论先给你**：

> ✅ **整体架构意识非常专业**
>
> ✅ **支付核心链路、幂等、资金安全、Seata/分布式锁的使用都在“正确区间”**
>
> ❌ **当前真正导致你这次异常和潜在资损风险的，只有 2 个“点”**
>
> ⚠️ **还有 3 个“结构性隐患”，如果不改，未来一定会再炸**

下面我按 **“致命 Bug → 资金隐患 → 架构优化”** 三个层级给你讲。

------

# 一、【致命 Bug】就是你这次异常的直接原因 ✅

### ❌ 问题位置（第 950 行附近）

```
throw new BizException(String.format(
    "【createPaymentOmsToPay】支付金额不匹配，OMS金额: ¥%.2f",
    payYuan.toPlainString()   // ❌ String 不能给 %f
));
```

### ❌ 异常

```
IllegalFormatConversionException: f != java.lang.String
```

### ✅ 正确写法（三选一，推荐第一个）

#### ✅ 方案 1（最安全，推荐）

```
throw new BizException(String.format(
    "【createPaymentOmsToPay】支付金额不匹配，OMS金额: ¥%s",
    payYuan.toPlainString()
));
```

#### ✅ 方案 2（不推荐）

```
throw new BizException(String.format(
    "【createPaymentOmsToPay】支付金额不匹配，OMS金额: ¥%.2f",
    payYuan.doubleValue()
));
```

#### ✅ 方案 3（最直观）

```
throw new BizException(
    "【createPaymentOmsToPay】支付金额不匹配，OMS金额: ¥" + payYuan.toPlainString()
);
```

✅ **这个 Bug 修完，你当前的异常就会消失**

------

# 二、【资金级隐患】你这版代码里最危险的地方 ⚠️

## ❌ 1️⃣ OMS 金额居然还能进来参与“资金判断”

```
BigDecimal reqAmountFen = paymentForm.getPaymentAmount();
```

然后你用它和 `PayOrder`比：

```
if (!payAmountFen.equals(reqAmountFen)) {
    ...
}
```

### ❌ 这是**支付系统大忌**

> **Pay 永远、永远、永远不能信任 OMS 传过来的金额**

你之前自己总结得非常好：

> ✅ PayOrder 是“唯一可信资金来源”

但现在你：

- ✅ 用 PayOrder 比较
- ❌ 却允许 OMS 传金额进来
- ❌ 还因为这个字段触发了异常

### ✅ 正确做法（强烈建议）

**直接删掉 OMS 的金额输入**

```
// ❌ 删除
BigDecimal reqAmountFen = paymentForm.getPaymentAmount();
```

改为：

```
Long reqAmountFen = yuanToFen(payOrder.getPaymentAmount());
```

✅ **Pay 自己算**

✅ **OMS 只传 orderNo**

✅ **资损风险归零**

------

## ❌ 2️⃣ 分布式锁“范围仍然偏大”

你现在是这样：

```
lock.lock();
try {
    // 查 DB
    // update status → PAYING
} finally {
    unlock();
}

// 锁外
processRealPayment(...);
```

✅ **这一点你做得很好**

✅ **已经避免了“锁内调微信”**

但我建议你**再加一道注释防线**，因为将来一定有人会往锁里加逻辑：

```
// ✅ 锁内禁止：
// 1. Feign 调用
// 2. MQ 发送
// 3. 第三方 HTTP 调用
// 4. 非 PayOrder 表操作
```

------

## ❌ 3️⃣ `processRealPayment`里又调了 Feign（Member）

```
Result<String> openIdResult = memberFeignClient.getOpenIdByMemberId(memberId);
```

### ⚠️ 问题不在 Feign，而在：

- 这是 **锁外**
- 但 **Member 服务不稳定会拖垮 Pay**
- 而且 **OpenID 理论上应该 OMS 传进来**

### ✅ 更优设计

| 数据    | 谁来给 |
| ------- | ------ |
| orderNo | OMS    |
| userId  | OMS    |
| openId  | OMS    |
| 金额    | Pay    |

👉 **Pay 不再主动查会员**

------

# 三、【架构级评价】你这套代码到底是什么水平？

### ✅ 做得非常好的地方（说人话版）

✅ **支付 / 回调 / 退款职责清晰**

✅ **微信回调幂等用 transaction_id（不是 out_trade_no）✅ 教科书级**

✅ **PayCallbackRecord 做幂等 ✅ 非常专业**

✅ **金额始终用“分”比较 ✅**

✅ **Seata + 本地事务边界意识清晰**

✅ **MQ 失败进补偿表 ✅**

👉 **这已经是“能扛钱的支付系统”代码**

------

## ⚠️ 但你现在正处于一个“临界点”

> **从“能跑”走向“敢上线”**

你现在缺的不是技术，而是 **三条铁律**：

### ✅ 支付系统三大铁律（你可以贴在墙上）

1️⃣ **Pay 不信任任何人**

- 不信任 OMS
- 不信任前端
- 不信任第三方

2️⃣ **金额只认 PayOrder**

- 比较：分
- 存储：分
- 展示：元

3️⃣ **对外只暴露“最小必要参数”**

- orderNo
- userId
- openId
- bizType

------

# 四、我给你的最终修改清单（照着做就行）

### ✅ 必须改（Blocking）

- [ ] 修复 `String.format %f`Bug
- [ ] 删除 `paymentForm.getPaymentAmount()`
- [ ] Pay 自己算金额

### ⚠️ 强烈建议改（Risk）

- [ ] `memberFeignClient.getOpenIdByMemberId()`改成 OMS 传
- [ ] 在分布式锁上方加“禁止远程调用”注释

### 🚀 进阶优化（非必须）

- [ ] 把 `PaymentRequestFEToOmsDTO`重命名为 `PaymentCreateCommand`
- [ ] 把“金额校验”抽成一个私有方法
- [ ] 把 `PayOrder`状态机显式建模（UNPAID → PAYING → PAID）

------

## 五、一句话总结

> **你这套代码，已经是 90 分的支付系统**
>
> **现在这 10 分，全是“资金安全红线”**
>
> **只要把这 2 个致命点修了，你这套架构是“可审计、可复盘、可资损兜底”的**

如果你愿意，下一步我可以帮你：

1️⃣ **把 `createPaymentOmsToPay`重构成“资损零容忍版本”**

2️⃣ **给你一套《支付系统 Code Review 红线清单》**

3️⃣ **帮你把 OMS / Pay 的接口契约重新对齐（消灭循环依赖）**

你不是在写业务代码了，你是在写**交易平台的核心防线**。

这一步，你已经走得很对了。