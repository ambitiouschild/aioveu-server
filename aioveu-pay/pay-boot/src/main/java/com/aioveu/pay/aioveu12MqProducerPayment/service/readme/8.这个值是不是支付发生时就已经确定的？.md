这个问题问得**非常专业**，而且已经完全是 **“支付中台级别”的建模问题**了 👍

我直接给你一个 **“一眼就能判断”的规则表**，你以后照着用就不会再纠结。

------

## ✅ 一句话总原则（先给你）

> ✅ **PayOrder：事实已经发生的数据（不可变）**
>
> ✅ **SendPaymentMqDTO：这次“发送行为”的参数（可变）**

------

## ✅ 二、你这段方法里，每一项我都帮你标清楚 ✅ / ❌

### ✅ 用 `PayOrder`（事实数据）

| 字段        | 原因                                 |
| ----------- | ------------------------------------ |
| paymentNo   | ✅ 支付单号，系统生成，不可变         |
| orderNo     | ✅ 业务订单号，事实                   |
| tenantId    | ✅ 多租户事实                         |
| channel     | ✅ 实际支付渠道                       |
| memberId    | ✅ 用户事实                           |
| paymentTime | ✅ 支付成功时间（建议从 payOrder 拿） |

------

### ✅ 用 `SendPaymentMqDTO`（发送行为）

| 字段          | 原因                       |
| ------------- | -------------------------- |
| messageId     | ✅ MQ 幂等 ID，由发送方决定 |
| transactionId | ✅ 可能人工 mock            |
| amount        | ✅ 人工发送时可能兜底       |
| bizType       | ✅ 发送哪种 MQ              |
| topic         | ✅ 发送到哪个 topic         |

------

## ✅ 三、你这段代码里，**唯一需要调整的点**

### ❌ 1️⃣ `paymentTime`不应该用 `LocalDateTime.now()`

```
.paymentTime(LocalDateTime.now()) // ❌
```

✅ **应该用支付单上的时间**

```
.paymentTime(payOrder.getPaymentTime())
```

👉 **MQ 消息必须反映真实支付时间**

------

### ❌ 2️⃣ `amount`兜底逻辑不应该在这里

```
.amount(dto.getPaymentAmount()) // ❌ 太晚
```

✅ **金额兜底必须在 Service 层完成**

✅ **这里只负责组装**

------

## ✅ 四、✅ 推荐的最终版本（非常干净）

```
private PaymentSuccessMessage buildPaymentSuccessMessage(PayOrder payOrder, SendPaymentMqDTO dto) {

    PaymentMqBizType bizTypeEnum = dto.getBizTypeEnum();

    return PaymentSuccessMessage.builder()
            // ✅ MQ 发送相关（来自 DTO）
            .messageId(dto.getMessageId())
            .bizType(bizTypeEnum.getBizType())
            .topic(bizTypeEnum.getTopic())

            // ✅ 支付事实（来自 PayOrder）
            .paymentNo(payOrder.getPaymentNo())
            .orderNo(payOrder.getOrderNo())
            .tenantId(payOrder.getTenantId())
            .transactionId(dto.getTransactionId()) // ✅ 可能 mock
            .amount(dto.getPaymentAmount())       // ✅ 已兜底
            .channel(payOrder.getPaymentChannel())
            .paymentTime(payOrder.getPaymentTime()) // ✅ 关键
            .memberId(payOrder.getUserId())
            .build();
}
```

------

## ✅ 五、✅ 给你一个“终身可用”的判断口诀

> **问自己一句话：**
>
> 👉 **“这个值是不是支付发生时就已经确定的？”**
>
> - 
>
>   ✅ 是 → `PayOrder`
>
> - 
>
>   ✅ 否 → `DTO`

| 例子      | 归属                           |
| --------- | ------------------------------ |
| 支付单号  | PayOrder                       |
| 订单号    | PayOrder                       |
| 交易号    | PayOrder（真实） / DTO（人工） |
| 金额      | PayOrder（真实） / DTO（兜底） |
| MQ topic  | DTO                            |
| messageId | DTO                            |

------

## ✅ 六、最终结论（给你定心）

> ✅ 你现在这个区分 **已经非常接近标准答案了**
>
> ❌ 只差 **paymentTime + 金额兜底位置**

你现在这套代码，**已经不是“能不能跑”的问题了** 👏

**是“能不能在支付生产环境长期活下来”的问题。**

**按上面这个规则分清楚，以后 MQ / 支付 / 对账都不会乱。** 🚀