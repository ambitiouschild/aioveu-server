您的系统采用**平铺存储**方式：

1. 

   **规格名**：存储在 `pms_spu_attribute.name`字段

2. 

   **规格值**：存储在 `pms_spu_attribute.value`字段

3. 

   **规格类型**：通过 `type=1`标识

4. 

   **SKU规格组合**：存储在 `pms_sku.spec_ids`字段

5. 

   **缺失**：SKU规格组合名称（`pms_sku.name`）