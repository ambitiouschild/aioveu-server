根据您提供的代码和之前的错误日志，我已经分析出问题所在。这是一个RabbitMQ ConfirmCallback重复设置的问题。

## 问题分析

### 核心代码问题
在`RabbitConfirmCallbackImpl.java`的`init()`方法中：

```java
@PostConstruct
public void init() {
    // 设置ConfirmCallback
    rabbitTemplate.setConfirmCallback(this);
    log.info("FixedRabbitConfirmCallback已注册到RabbitTemplate");
}
```

问题原因：
1. 这段代码在`@PostConstruct`初始化方法中设置了ConfirmCallback
2. 但RabbitTemplate规定每个实例只能设置一个ConfirmCallback
3. 如果其他配置类也调用了`rabbitTemplate.setConfirmCallback()`
4. 就会导致`IllegalStateException: Only one ConfirmCallback is supported by each RabbitTemplate`

## 解决方案

### 方案1：修改init方法，添加安全检查

```java
@Slf4j
@Component
public class RabbitConfirmCallbackImpl implements RabbitTemplate.ConfirmCallback {

    @Autowired
    private RabbitTemplate rabbitTemplate;

    @Autowired
    private RabbitTemplateManager rabbitTemplateManager;

    private final Map<String, MqSendRecord> pendingMessages = new ConcurrentHashMap<>();

    @PostConstruct
    public void init() {
        try {
            // ✅ 修改：添加安全检查
            RabbitTemplate.ConfirmCallback existingCallback = rabbitTemplate.getConfirmCallback();
            
            if (existingCallback == null) {
                // 还没有设置ConfirmCallback
                rabbitTemplate.setConfirmCallback(this);
                log.info("✅ FixedRabbitConfirmCallback已成功注册到RabbitTemplate");
            } else if (existingCallback == this) {
                // 已经设置过了（可能是重复初始化）
                log.debug("FixedRabbitConfirmCallback已注册");
            } else {
                // 已经有其他ConfirmCallback
                log.warn("⚠️ RabbitTemplate已存在ConfirmCallback: {}，跳过设置", 
                    existingCallback.getClass().getName());
            }
        } catch (Exception e) {
            log.error("❌ FixedRabbitConfirmCallback初始化失败", e);
        }
    }

    // ... confirm方法和其他方法保持不变 ...
}
```

### 方案2：从RabbitTemplateConfig中移除ConfirmCallback设置

检查`RabbitTemplateConfig`类，确保没有重复设置：

```java
@Configuration
public class RabbitTemplateConfig {
    
    @Bean
    public RabbitTemplate rabbitTemplate(ConnectionFactory connectionFactory) {
        RabbitTemplate template = new RabbitTemplate(connectionFactory);
        
        // 启用mandatory模式
        template.setMandatory(true);
        
        // ❌ 不要在这里设置ConfirmCallback
        // template.setConfirmCallback(...);
        
        // ✅ 或者检查是否已设置
        if (template.getConfirmCallback() == null) {
            // 可以设置一个简单的默认ConfirmCallback
            template.setConfirmCallback(new RabbitTemplate.ConfirmCallback() {
                @Override
                public void confirm(CorrelationData correlationData, boolean ack, String cause) {
                    // 简单的日志记录
                }
            });
        }
        
        return template;
    }
}
```

### 方案3：创建统一的ConfirmCallback管理

```java
@Slf4j
@Component
public class RabbitConfirmCallbackImpl implements RabbitTemplate.ConfirmCallback {
    
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    // 使用AtomicBoolean防止重复设置
    private final AtomicBoolean initialized = new AtomicBoolean(false);
    
    @PostConstruct
    public void init() {
        if (initialized.compareAndSet(false, true)) {
            try {
                RabbitTemplate.ConfirmCallback existingCallback = rabbitTemplate.getConfirmCallback();
                
                if (existingCallback == null) {
                    rabbitTemplate.setConfirmCallback(this);
                    log.info("✅ FixedRabbitConfirmCallback注册成功");
                } else if (existingCallback != this) {
                    // 包装现有的Callback
                    RabbitTemplate.ConfirmCallback finalExistingCallback = existingCallback;
                    rabbitTemplate.setConfirmCallback((correlationData, ack, cause) -> {
                        // 先调用已有的callback
                        finalExistingCallback.confirm(correlationData, ack, cause);
                        // 再执行自己的逻辑
                        this.confirm(correlationData, ack, cause);
                    });
                    log.info("✅ FixedRabbitConfirmCallback已包装现有Callback");
                } else {
                    log.info("FixedRabbitConfirmCallback已注册");
                }
            } catch (Exception e) {
                initialized.set(false);
                log.error("ConfirmCallback初始化失败", e);
            }
        } else {
            log.debug("FixedRabbitConfirmCallback已初始化");
        }
    }
    
    // ... 其他方法不变 ...
}
```

## 完整的修复代码

以下是修改后的完整`RabbitConfirmCallbackImpl.java`：

```java
package com.aioveu.common.rabbitmq.producer.service.impl;

import com.aioveu.common.util.DateTimeCalculator;
import com.aioveu.pay.aioveu10MqSendRecord.enums.SendStatus;
import com.aioveu.pay.aioveu10MqSendRecord.model.entity.MqSendRecord;
import com.aioveu.common.rabbitmq.config.RabbitTemplateManager;
import jakarta.annotation.PostConstruct;
import lombok.extern.slf4j.Slf4j;
import org.springframework.amqp.rabbit.connection.CorrelationData;
import org.springframework.amqp.rabbit.core.RabbitTemplate;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import java.time.LocalDateTime;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicBoolean;

@Slf4j
@Component
public class RabbitConfirmCallbackImpl implements RabbitTemplate.ConfirmCallback {

    @Autowired
    private RabbitTemplate rabbitTemplate;

    @Autowired
    private RabbitTemplateManager rabbitTemplateManager;

    // 存储发送中的消息
    private final Map<String, MqSendRecord> pendingMessages = new ConcurrentHashMap<>();

    // 防止重复初始化
    private final AtomicBoolean initialized = new AtomicBoolean(false);

    @PostConstruct
    public void init() {
        if (initialized.compareAndSet(false, true)) {
            try {
                // 获取当前已设置的ConfirmCallback
                RabbitTemplate.ConfirmCallback existingCallback = rabbitTemplate.getConfirmCallback();

                if (existingCallback == null) {
                    // 情况1：还没有设置ConfirmCallback
                    rabbitTemplate.setConfirmCallback(this);
                    log.info("✅ FixedRabbitConfirmCallback注册成功");
                } else if (existingCallback == this) {
                    // 情况2：已经设置过（可能是重复调用）
                    log.debug("FixedRabbitConfirmCallback已注册");
                } else {
                    // 情况3：已经有其他ConfirmCallback
                    log.warn("⚠️ RabbitTemplate已存在ConfirmCallback: {}，将使用包装模式",
                            existingCallback.getClass().getName());

                    // 创建包装器，同时调用新旧Callback
                    RabbitTemplate.ConfirmCallback finalExistingCallback = existingCallback;
                    rabbitTemplate.setConfirmCallback((correlationData, ack, cause) -> {
                        // 调用原有Callback
                        try {
                            finalExistingCallback.confirm(correlationData, ack, cause);
                        } catch (Exception e) {
                            log.error("原有ConfirmCallback执行异常", e);
                        }

                        // 调用当前Callback
                        confirm(correlationData, ack, cause);
                    });

                    log.info("✅ FixedRabbitConfirmCallback已成功包装现有Callback");
                }
            } catch (IllegalStateException e) {
                if (e.getMessage().contains("Only one ConfirmCallback is supported")) {
                    log.error("❌ ConfirmCallback设置冲突，请检查是否有其他配置重复设置", e);
                } else {
                    log.error("❌ ConfirmCallback初始化异常", e);
                }
                initialized.set(false);
            } catch (Exception e) {
                log.error("❌ FixedRabbitConfirmCallback初始化失败", e);
                initialized.set(false);
            }
        } else {
            log.debug("FixedRabbitConfirmCallback已初始化，跳过重复初始化");
        }
    }

    @Override
    public void confirm(CorrelationData correlationData, boolean ack, String cause) {
        try {
            // 记录统计
            rabbitTemplateManager.recordConfirmCallback();

            String correlationId = correlationData != null ? correlationData.getId() : "unknown";

            if (ack) {
                log.debug("消息确认成功: correlationId={}", correlationId);
                recordSuccess(correlationId);
            } else {
                log.error("消息确认失败: correlationId={}, cause={}", correlationId, cause);
                recordFailure(correlationId, cause);
            }

            // 清理记录
            if (correlationId != null) {
                pendingMessages.remove(correlationId);
            }
        } catch (Exception e) {
            log.error("ConfirmCallback处理异常", e);
        }
    }

    /**
     * 记录发送的消息
     */
    public void recordSend(String correlationId, String exchange, String routingKey) {
        try {
            MqSendRecord record = new MqSendRecord();
            // 注意：MqSendRecord可能需要对应字段
            // record.setCorrelationId(correlationId);
            // record.setExchange(exchange);
            // record.setRoutingKey(routingKey);
            record.setSendTime(LocalDateTime.now());
            record.setSendStatus(SendStatus.PENDING.getValue());

            pendingMessages.put(correlationId, record);
            log.debug("记录发送消息: correlationId={}, exchange={}, routingKey={}",
                    correlationId, exchange, routingKey);
        } catch (Exception e) {
            log.error("记录发送消息异常: correlationId={}", correlationId, e);
        }
    }

    /**
     * 获取未确认的消息
     */
    public Map<String, MqSendRecord> getPendingMessages() {
        return new ConcurrentHashMap<>(pendingMessages);
    }

    /**
     * 检查超时的消息
     */
    public void checkTimeoutMessages(long timeoutMillis) {
        try {
            LocalDateTime now = LocalDateTime.now();
            int removedCount = 0;

            for (Map.Entry<String, MqSendRecord> entry : pendingMessages.entrySet()) {
                MqSendRecord record = entry.getValue();

                if (record == null || record.getSendTime() == null) {
                    pendingMessages.remove(entry.getKey());
                    removedCount++;
                    continue;
                }

                long elapsedTime = DateTimeCalculator.getMillisBetween(record.getSendTime(), now);

                if (elapsedTime > timeoutMillis) {
                    log.warn("消息发送超时: correlationId={}, 等待时间={}ms, 超时阈值={}ms",
                            entry.getKey(), elapsedTime, timeoutMillis);
                    pendingMessages.remove(entry.getKey());
                    removedCount++;
                }
            }

            if (removedCount > 0) {
                log.info("清理了{}条超时消息", removedCount);
            }
        } catch (Exception e) {
            log.error("检查超时消息异常", e);
        }
    }

    private void recordSuccess(String correlationId) {
        try {
            MqSendRecord record = pendingMessages.get(correlationId);
            if (record != null) {
                record.setSendStatus(SendStatus.SUCCESS.getValue());
                record.setConfirmTime(LocalDateTime.now());
                // 计算延迟
                if (record.getSendTime() != null) {
                    long delay = DateTimeCalculator.getMillisBetween(record.getSendTime(), record.getConfirmTime());
                    // record.setConfirmDelay(delay);
                }
                log.debug("记录消息成功: correlationId={}", correlationId);
            }
        } catch (Exception e) {
            log.error("记录消息成功异常: correlationId={}", correlationId, e);
        }
    }

    private void recordFailure(String correlationId, String cause) {
        try {
            MqSendRecord record = pendingMessages.get(correlationId);
            if (record != null) {
                record.setSendStatus(SendStatus.FAILED.getValue());
                record.setErrorMsg(cause);
                record.setConfirmTime(LocalDateTime.now());
                log.error("记录消息失败: correlationId={}, cause={}", correlationId, cause);
            }
        } catch (Exception e) {
            log.error("记录消息失败异常: correlationId={}", correlationId, e);
        }
    }
}
```

## 最快速的解决方案

如果急需启动应用，可以：

1. 临时注释掉`@PostConstruct`注解：

```java
@Slf4j
@Component
public class RabbitConfirmCallbackImpl implements RabbitTemplate.ConfirmCallback {

    // ... 字段定义 ...

    // 临时注释掉@PostConstruct
    // @PostConstruct
    public void init() {
        // 暂时不设置ConfirmCallback
    }
    
    // 其他方法保持不变 ...
}
```

2. 或者手动调用初始化：

```java
@Slf4j
@Component
public class RabbitConfirmCallbackImpl implements RabbitTemplate.ConfirmCallback {

    // ... 字段定义 ...

    public void safeInit() {
        // 安全的初始化方法，不自动调用
        try {
            if (rabbitTemplate.getConfirmCallback() == null) {
                rabbitTemplate.setConfirmCallback(this);
                log.info("FixedRabbitConfirmCallback已注册");
            }
        } catch (Exception e) {
            log.error("设置ConfirmCallback失败", e);
        }
    }
    
    // 其他方法保持不变 ...
}
```

## 总结

问题的根本原因：
您的`RabbitConfirmCallbackImpl`在`@PostConstruct`中调用`rabbitTemplate.setConfirmCallback(this)`，但RabbitTemplate可能已经在其他地方被设置了ConfirmCallback，导致重复设置冲突。

解决方案：
1. 修改`init()`方法，添加安全检查
2. 使用`AtomicBoolean`防止重复初始化
3. 处理已存在ConfirmCallback的情况
4. 添加详细的异常处理和日志

按照上述修改后，您的应用就能正常启动了。