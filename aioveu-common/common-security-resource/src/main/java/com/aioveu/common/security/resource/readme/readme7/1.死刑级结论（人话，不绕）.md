兄弟，这回不用猜了——凶手直接自己报身份证了。我盯着你刚贴的异常栈，一字一句给你钉死。

---

## 一、死刑级结论（人话，不绕）

> ✅ JWT 验签成功（`JwtAuthenticationProvider: Authenticated token`）
> ✅ `BearerTokenAuthenticationFilter` 正确设了 `JwtAuthenticationToken [ROLE_USER]`
> ❌ 但 `JwtVersionFilter.doFilterInternal(JwtVersionFilter.java:89)` 手动抛了：
```
org.springframework.security.oauth2.core.OAuth2AuthenticationException: Token 非法
```
> ❌ `ExceptionTranslationFilter` 捕获 → 清 `SecurityContext` → 调 `MyAuthenticationEntryPoint` → 你看到 `auth=null` + “Token 非法”

不是 matcher 没写、不是链没加载、不是 Spring 疯了——是你的 `JwtVersionFilter` 在第 89 行，把“合法 JWT”亲手枪毙了。

---

## 二、证据链（你日志里赤裸裸的）

### 1. JWT 已过验签
```
DEBUG JwtAuthenticationProvider: Authenticated token
DEBUG BearerTokenAuthenticationFilter: Set SecurityContextHolder to JwtAuthenticationToken [...Granted Authorities=[ROLE_USER]]
```

### 2. DebugFilter 在它之后打印 Auth 还在
```
DEBUG FILTER ACTIVE, URI=/me
Authentication: JwtAuthenticationToken [...
```

### 3. 紧接着异常栈顶层
```
ERROR MyAuthenticationEntryPoint: 🔥 REAL AUTH EXCEPTION
org.springframework.security.oauth2.core.OAuth2AuthenticationException: Token 非法
	at com.aioveu.common.security.resource.filter.JwtVersionFilter.doFilterInternal(JwtVersionFilter.java:89)
```

调用链顺序（你自己的 FilterChain 打印过）：
```
BearerTokenAuthenticationFilter   ✅ 设 Auth
DebugFilter                       ✅ 打印 Auth
JwtVersionFilter                  ❌ 第89行抛异常 ← 这里
JwtBlacklistFilter
TenantFilter
ExceptionTranslationFilter        ❌ 清Context + EntryPoint
```

---

## 三、你现在立刻要做的（止血，30 秒）

打开 `JwtVersionFilter.java`，直奔 第 89 行。

我赌它是类似这种逻辑之一（你对照看）：

### 常见凶手模板
```java
// 比对 tokenVersion / jwt.getClaim("tokenVersion")
if (tokenVersion != expected) {
    throw new OAuth2AuthenticationException("Token 非法"); // ← 89行
}
```

或
```java
if (!jwt.getClaimAsString("tokenVersion").equals(cacheVersion)) {
    throw new OAuth2AuthenticationException("Token 非法");
}
```

或黑名单/版本校验没过。

---

## 四、正确改法（二选一）

### ✅ 方案 A：真坏了就别谎报（先止血）
把异常类型换成准确的，或直接打日志不抛：
```java
log.error("JwtVersionFilter reject, jwtVersion={}, stored={}",
          jwt.getClaim("tokenVersion"), expected);
// 暂时注释，确认业务
// throw new OAuth2AuthenticationException("Token 非法");
chain.doFilter(request, response);
```

### ✅ 方案 B：真要校验，抛对异常名
```java
throw new OAuth2AuthenticationException("token version mismatch");
```

---

## 五、为什么之前 /me 一直“精神分裂”

时间线复盘：
1. `BearerToken` 验签过 → Auth 有
2. `JwtVersionFilter` 说“不，你非法” → 抛
3. Spring 清 Context → `auth=null`
4. 你 `EntryPoint` 写死日志：“Token 非法”
5. 你以为 Spring 瞎了，其实是你自己 Filter 开的枪

---

## 六、终局一句话

> JWT 没罪，Spring 没疯，`JwtVersionFilter` 第 89 行是唯一的枪手。
> 改完那一行，这假 401 当场死透，不用再贴日志。

要我直接帮你把 `JwtVersionFilter` 的完整“正确校验 + 不吞异常”标准实现 写给你粘进去吗？这案子现在可以封卷了。

---
*判定依据：你 2026-08-19T16:09 日志中 `OAuth2AuthenticationException` 栈顶为 `JwtVersionFilter:89`，Spring Security 6.x FilterChain 顺序对照。*