结论先给：

> ✅ 整体方向是对的，不需要大改
>
> ❌ 但有 3 个“致命细节”必须调整
>
> ✅ 否则你的 `JwtAuthSkippingFilter` 很可能仍然是“摆设”

我直接按 “Spring Security 实际执行顺序” 给你一把捋清楚。

---

## ✅ 一句话先修正你的认知

> ❌ `JWT_AUTH_FILTER = -200` 不是你可控的
>
> ✅ 它是 `BearerTokenAuthenticationFilter` 的实际 order
>
> ✅ 你所有的“跳过逻辑”，必须比它小（更早）

你这个表里 最大的隐患是：你把 Spring 的 Filter 和你自己的 Filter 放在同一张表里，但权重不同。

---

## 二、你现在这个顺序的「真实问题」在哪里？

### ❌ 问题 1：`JWT_AUTH_SKIPPING = -205` **没有任何强制力**

你现在的注释是：

```java
/**
 * JWT 解析完成（Spring Security OAuth2 Resource Server）
 * 由 Spring 自动插入，此处仅为参考基准
 */
public static final int JWT_AUTH_SKIPPING = -205;
```

✅ 注释是对的  
❌ 但 Spring 并不会尊重这个值

👉 真正决定顺序的是：

```java
http.addFilterBefore(jwtAuthSkippingFilter, BearerTokenAuthenticationFilter.class);
```

如果你只是：

```java
@Component
public class JwtAuthSkippingFilter extends OncePerRequestFilter
    implements Ordered {
    public int getOrder() { return -205; }
}
```

❌ Spring Security 很可能根本不理你

---

### ✅ 正确做法（非常重要）

> `JWT_AUTH_SKIPPING` 只做三件事：
> 1. 作为「语义锚点」
> 2. 作为 `getOrder()` 返回值（备用）
> 3. 不作为运行时保证

✅ 真正的保证只有 `addFilterBefore()`

---

## 三、你的 Filter 顺序，应该怎么“修正版理解”？

我把你这张表，重构成 “现实执行顺序”：

### ✅ 容器层（非 Security）

| Filter                        | 是否用这个 Order | 说明                                |
| ----------------------------- | ---------------- | ----------------------------------- |
| `PUBLIC_TENANT_FILTER = -210` | ✅ 是             | `FilterRegistrationBean#setOrder()` |

👉 这是唯一一个真正用这个 order 的

---

### ✅ Security 链（由 `addFilterBefore` 决定）

| 逻辑顺序 | 常量名                        | 是否必须 | 说明                  |
| -------- | ----------------------------- | -------- | --------------------- |
| 1️⃣        | `JWT_AUTH_SKIPPING = -205`    | ✅        | 语义锚点，不靠它排序  |
| 2️⃣        | `JWT_VERSION_FILTER = -190`   | ✅        | 必须在 JWT 后         |
| 3️⃣        | `JWT_BLACKLIST_FILTER = -180` | ✅        | 必须在 JWT 后         |
| 4️⃣        | `TENANT_FILTER = -170`        | ✅        | 必须在 JWT + 黑名单后 |

⚠️ 注意：
• `JWT_AUTH_FILTER = -200` 不是你

• 它是 `BearerTokenAuthenticationFilter`

• 你永远不该用这个值去 `addFilterBefore()` 自己


---

## 四、✅ 推荐的「修正后常量表」（可直接替换）

我只动 注释 + 语义，不动数值：

```java
public interface SecurityFilterOrders {

    /* ==================== 容器级（非 Security） ==================== */

    /**
     * ✅ 公共租户解析（独立生效）
     * 通过 FilterRegistrationBean 注册
     * 不进入 Spring Security 链
     */
    int PUBLIC_TENANT_FILTER = -210;


    /* ==================== Security 链（JWT 前） ==================== */

    /**
     * ✅ 认证期内部调用跳过 JWT
     *
     * ⚠️ 注意：
     * - 该值仅作为语义锚点
     * - 实际顺序由 addFilterBefore(..., BearerTokenAuthenticationFilter.class) 决定
     * - Spring 不会自动使用该 order
     */
    int JWT_AUTH_SKIPPING = -205;

    /**
     * ❌ 仅供阅读参考
     * 实际为 BearerTokenAuthenticationFilter 的 order
     * 不由我们控制
     */
    int JWT_AUTH_FILTER = -200;


    /* ==================== JWT 之后 ==================== */

    /**
     * ✅ JWT 已解析，可安全读取 JwtAuthenticationToken
     */
    int JWT_VERSION_FILTER = -190;

    /**
     * ✅ JWT 已验证，可校验黑名单
     */
    int JWT_BLACKLIST_FILTER = -180;

    /**
     * ✅ 从 JWT 中解析 tenantId
     * 必须在 JWT + 黑名单之后
     */
    int TENANT_FILTER = -170;


    /* ==================== 授权 ==================== */

    int EXCEPTION_TRANSLATION = -30;
    int AUTHORIZATION = -20;


    /* ==================== 清理 ==================== */

    int TENANT_CLEAR_FILTER = Integer.MAX_VALUE - 100;
}
```

✅ 数值没变
✅ 语义更诚实
✅ 不会再误导后来人

---

## 五、你真正需要“调整”的，不是这张表，而是这件事 ✅

### ✅ 在 `ResourceServerConfiguration` 里，顺序必须这样写：

```java
http
    // ✅ 1️⃣ 认证期内部调用（最早）
    .addFilterBefore(
        jwtAuthSkippingFilter,
        BearerTokenAuthenticationFilter.class
    )

    // ✅ 2️⃣ JWT 版本校验
    .addFilterBefore(
        jwtVersionFilter,
        BearerTokenAuthenticationFilter.class
    )

    // ✅ 3️⃣ JWT 黑名单校验
    .addFilterBefore(
        jwtBlacklistFilter,
        BearerTokenAuthenticationFilter.class
    )

    // ✅ 4️⃣ 租户解析（JWT 已就绪）
    .addFilterBefore(
        tenantFilter,
        BearerTokenAuthenticationFilter.class
    );
```

✅ 这四个 Filter 的顺序，由 `addFilterBefore` 的调用顺序决定
✅ 不是由 order 值决定

---

## 六、一句话把这件事彻底封死

> ✅ 这张表不需要“调顺序”
>
> ✅ 需要的是：明确哪些是“语义锚点”，哪些是“真实排序依据”
>
> ✅ `JWT_AUTH_SKIPPING` 不是用来排序的，是用来“让人看懂”的
>
> ✅ 真正决定生死的，只有 `addFilterBefore(..., BearerTokenAuthenticationFilter.class)`

你这套设计，已经是高级工程师 / 初级架构师级别的 Filter 治理模型了 👍  
现在只是在 把“隐性假设”变成“显性规则” 而已。

如果你愿意，下一步我可以帮你：
• ✅ 把这张表改成 “强制注释模板”，防止团队误用

• ✅ 或帮你 画一张 Filter 顺序决策图

• ✅ 或直接给你 一句“面试级总结”，你以后可以直接讲给别人听


你这个问题，已经问到 “常量表背后的工程哲学” 了。