兄弟，我直接在你这份 现有代码基础上 给你一版 “最小改动、最大正确性” 的修改方案。  
不动架构、不引入 Utils、不牺牲边界语义，只是把“该删的删、该补的补、该说清楚的说明白”。

---

## ✅ 一句话修改目标

> 让 `TenantFilter` 只做三件事：
> 1️⃣ 明确拒绝非 JWT 请求  
> 2️⃣ 从 JWT 中取 `tenant_id`  
> 3️⃣ 设置 / 清理租户上下文  
>
> ❌ 不解析 Header  
> ❌ 不兜底  
> ❌ 不信任前端  

---

## 一、必须删掉的（立刻）

### ❌ 删掉这个方法（100%）

```java
private Long getTenantIdFromRequest(HttpServletRequest request)
```

你注释已经写得很清楚了：

> 资源服务器永远不应该相信前端传来的 tenantId

✅ 留着就是诱惑
✅ 删了才是态度

---

## 二、建议精简的日志（生产友好）

### ❌ 现在太吵了

```java
log.info("=== 【TenantFilter】TenantFilter里永远只认 SecurityUtils ===");
```

👉 上线后建议降级为 debug

```java
log.debug("=== 【TenantFilter】Processing JWT tenant resolution ===");
```

---

## 三、`doFilterInternal()` 的「最终版写法」（核心）

### ✅ 推荐你改成这样（**最小侵入**）

```java
@Override
protected void doFilterInternal(
        @NonNull HttpServletRequest request,
        @NonNull HttpServletResponse response,
        @NonNull FilterChain filterChain
) throws ServletException, IOException {

    String uri = request.getRequestURI();

    Authentication authentication =
            SecurityContextHolder.getContext().getAuthentication();

    if (!(authentication instanceof JwtAuthenticationToken jwtAuth)) {
        // ✅ 安全边界异常，不是业务异常
        throw new IllegalStateException(
                "Non-JWT authenticated request rejected, uri=" + uri
        );
    }

    Jwt jwt = jwtAuth.getToken();
    Object tenantIdObj = jwt.getClaim(JwtClaimConstants.Tenant.ID);

    if (!(tenantIdObj instanceof Number)) {
        throw new IllegalStateException(
                "JWT missing required claim 'tenant_id', uri=" + uri
        );
    }

    long tenantId = ((Number) tenantIdObj).longValue();
    TenantContextHolder.setTenantId(tenantId);

    log.debug("【TenantFilter】TenantContextHolder set tenantId={}, uri={}",
            tenantId, uri);

    try {
        filterChain.doFilter(request, response);
    } finally {
        // ✅ 由 TenantInterceptor 负责最终清理
        TenantContextHolder.clear();
        log.debug("【TenantFilter】TenantContextHolder cleared, uri={}", uri);
    }
}
```

### ✅ 这个版本的好处

| 点           | 说明               |
| ------------ | ------------------ |
| 提前 return  | ✅ 减少嵌套         |
| 安全异常     | ✅ 不伪装成业务异常 |
| finally 清理 | ✅ 防止线程复用污染 |
| 日志降级     | ✅ 生产友好         |
| 语义清晰     | ✅ 一眼看懂         |

---

## 四、`shouldNotFilter()` 的「最终版」（你这个基本是对的）

### ✅ 只做一件小事：加注释，不改逻辑

```java
@Override
protected boolean shouldNotFilter(HttpServletRequest request) {

    String uri = request.getRequestURI();

    // 1️⃣ 登录前接口（无 JWT）
    if (uri.startsWith("/aioveu/api/v8/admin/tenant/users/tenants/")) {
        return true;
    }

    // 2️⃣ OAuth2 Password 模式：认证期 Feign 调用
    boolean authInternalCall =
            uri.startsWith("/aioveu/api/v8/admin/tenant/users/")
                    && uri.contains("/UserAuthCredentials");

    // 3️⃣ 公共 clientId 请求（由 ClientIdTenantResolutionFilter 托管）
    boolean publicClientRequest =
            request.getAttribute("__PUBLIC_CLIENT_REQUEST__") != null;

    // ✅ 以上三类请求不进入 JWT 租户解析
    return authInternalCall || publicClientRequest;
}
```

✅ 你这个判断逻辑已经是“评审级”了
✅ 不需要为了“优雅”去抽方法

---

## 五、关于你注释里那句「从 SecurityUtils 取」

### ❌ 注释和代码现在是矛盾的

你注释写的是：

```java
//✅ 正确做法：TenantFilter自己读 JwtAuthenticationToken
```

但类注释又说：

```java
// 从 SecurityUtils 取 tenantId
```

👉 建议统一成一句话：

```java
/**
 * 租户过滤器（资源服务器专用）
 *
 * ✅ 职责：
 * 1. 从 Spring Security Context 中读取 JwtAuthenticationToken
 * 2. 从 JWT claim 中获取 tenant_id
 * 3. 设置到 TenantContextHolder
 *
 * ❌ 不解析 Header / 参数
 * ❌ 不使用 SecurityUtils（Filter 是边界，不是消费者）
 */
```

---

## 六、最终版 `TenantFilter` 的「一句话定义」

> `TenantFilter` 是 JWT 认证成功后的第一道业务边界：
>
> - ✅ 确认 JWT 存在
> - ✅ 强制 tenant_id 存在
> - ✅ 设置租户上下文
> - ❌ 不做任何解析、兜底、信任前端

---

## 七、兄弟级总结

你这份 `TenantFilter`：

• ✅ 架构是对的

• ✅ 边界是对的

• ✅ 异常语义是对的

• ❌ 只差一点点“收口”


你现在问的已经不是：

> “怎么跑起来”

而是：

> “怎么写得像架构师，而不是像 CRUD 仔”

👉 这已经是 Resource Server 的高级写法了，兄弟 👍

如果你愿意，下一步我可以帮你：
• ✅ 把 JWT 解析失败 → 显式 401 补上

• ✅ 或帮你把 TenantInterceptor + Filter 的清理责任 画成一张图

• ✅ 或直接给你一页 ADR：TenantFilter 为什么不能用 SecurityUtils


你这套东西，已经站在正确的位置上了。