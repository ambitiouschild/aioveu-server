这个红得**非常合理**，而且你现在刚好踩中了 **“模块职责边界的最后一道警戒线”** 👍

我直接给你一个**“为什么爆红 + 怎么修 + 怎么不复发”**的完整答案。

------

## ✅ 一句话结论（先止血）

> **`MyDataPermissionHandler`放在 `common-security-resource`是 ✅ 对的**
>
> ❌ **但它现在“越界”引用了 `common-mybatis`的东西**
>
> ✅ **解决办法：把 `DataPermission`注解和 `DataScopeEnum`移到 `common-security-core`**
>
> ✅ **`common-security-resource`只依赖 `core`，不依赖 `mybatis`**

------

## 二、为什么一搬进来就全爆红？（根因）

你现在这个类里引用了：

```
import com.ai.commom.mybatis.annotation.DataPermission;
import com.ai.commom.mybatis.enums.DataScopeEnum;
import com.baomidou.mybatisplus.extension.plugins.handler.DataPermissionHandler;
import com.baomidou.mybatisplus.core.toolkit.*;
import net.sf.jsqlparser.*;
```

而你的 `common-security-resource`**大概率没有这些依赖**：

| 依赖                 | 属于哪个模块      |
| -------------------- | ----------------- |
| `DataPermission`注解 | `common-mybatis`❌ |
| `DataScopeEnum`      | `common-mybatis`❌ |
| MyBatis‑Plus 类      | `common-mybatis`❌ |
| JSqlParser           | `common-mybatis`❌ |

👉 **你在 resource 模块里用了“mybatis 的实现细节”**

------

## 三、✅ 正确的依赖方向（必须画在脑子里）

```
common-security-core
├── DataPermission (注解)
├── DataScopeEnum (枚举)
└── IBaseEnum
        ↑
common-security-resource
├── MyDataPermissionHandler
├── MyTenantLineHandler
└── JwtSecurityUtils
        ↑
common-mybatis
├── MybatisPlusConfig
└── TenantProperties
```

✅ **箭头方向：只能从上往下**

❌ **不能 reverse**

------

## 四、✅ 一步到位的修复方案（推荐）

### ✅ 第一步：把“定义”挪到 `common-security-core`

#### 1️⃣ `DataPermission`注解 → `common-security-core`

```
package com.aioveu.common.security.core.annotation;

import java.lang.annotation.*;

@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface DataPermission {

    String deptAlias() default "";

    String deptIdColumnName() default "dept_id";

    String userAlias() default "";

    String userIdColumnName() default "user_id";
}
```

------

#### 2️⃣ `DataScopeEnum`→ `common-security-core`

```
package com.aioveu.common.security.core.enums;

import com.aioveu.common.core.base.IBaseEnum;
import lombok.Getter;

@Getter
public enum DataScopeEnum implements IBaseEnum<Integer> {

    ALL(1, "全部"),
    DEPT(2, "本部门"),
    DEPT_AND_CHILD(3, "本部门及子部门"),
    SELF(4, "仅本人"),
    CUSTOM(5, "自定义");

    private final Integer value;
    private final String label;

    DataScopeEnum(Integer value, String label) {
        this.value = value;
        this.label = label;
    }
}
```

------

### ✅ 第二步：`common-security-resource`依赖 `core`

```
<dependency>
    <groupId>com.aioveu</groupId>
    <artifactId>common-security-core</artifactId>
</dependency>
```

✅ **不要再依赖 `common-mybatis`**

------

### ✅ 第三步：`common-mybatis`依赖 `resource`

```
<dependency>
    <groupId>com.aioveu</groupId>
    <artifactId>common-security-resource</artifactId>
</dependency>
```

✅ MyBatis 配置类里注册 `MyDataPermissionHandler`

✅ Handler 本身不依赖 mybatis

------

## 五、✅ 修改后的 `MyDataPermissionHandler`（精简正确版）

```
package com.aioveu.common.security.resource.handler;

import cn.hutool.core.util.StrUtil;
import com.aioveu.common.security.core.annotation.DataPermission;
import com.aioveu.common.security.core.enums.DataScopeEnum;
import com.aioveu.common.security.resource.helper.JwtSecurityUtils;
import com.baomidou.mybatisplus.extension.plugins.handler.DataPermissionHandler;
import lombok.SneakyThrows;
import lombok.extern.slf4j.Slf4j;
import net.sf.jsqlparser.expression.Expression;
import net.sf.jsqlparser.expression.operators.conditional.AndExpression;
import net.sf.jsqlparser.parser.CCJSqlParserUtil;

import java.lang.reflect.Method;

@Slf4j
public class MyDataPermissionHandler implements DataPermissionHandler {

    @Override
    @SneakyThrows
    public Expression getSqlSegment(Expression where, String mappedStatementId) {
        Class<?> clazz = Class.forName(mappedStatementId.substring(0, mappedStatementId.lastIndexOf('.')));
        String methodName = mappedStatementId.substring(mappedStatementId.lastIndexOf('.') + 1);

        for (Method method : clazz.getDeclaredMethods()) {
            DataPermission annotation = method.getAnnotation(DataPermission.class);
            if (annotation == null) {
                continue;
            }

            if (JwtSecurityUtils.isRoot()) {
                return where;
            }

            if (method.getName().equals(methodName)
                    || (method.getName() + "_COUNT").equals(methodName)) {
                return dataScopeFilter(
                        annotation.deptAlias(),
                        annotation.deptIdColumnName(),
                        annotation.userAlias(),
                        annotation.userIdColumnName(),
                        where
                );
            }
        }
        return where;
    }

    @SneakyThrows
    private Expression dataScopeFilter(
            String deptAlias,
            String deptIdColumnName,
            String userAlias,
            String userIdColumnName,
            Expression where
    ) {
        String deptCol = StrUtil.isNotBlank(deptAlias)
                ? deptAlias + "." + deptIdColumnName
                : deptIdColumnName;

        String userCol = StrUtil.isNotBlank(userAlias)
                ? userAlias + "." + userIdColumnName
                : userIdColumnName;

        DataScopeEnum scope = DataScopeEnum.valueOf(JwtSecurityUtils.getDataScope());

        String sql;
        switch (scope) {
            case ALL -> {
                return where;
            }
            case DEPT -> {
                sql = deptCol + " = " + JwtSecurityUtils.getDeptId();
            }
            case SELF -> {
                sql = userCol + " = " + JwtSecurityUtils.getUserId();
            }
            default -> {
                Long deptId = JwtSecurityUtils.getDeptId();
                sql = deptCol + " IN (SELECT id FROM sys_dept WHERE id = "
                        + deptId + " OR FIND_IN_SET(" + deptId + ", tree_path))";
            }
        }

        Expression expr = CCJSqlParserUtil.parseCondExpression(sql);
        return where == null ? expr : new AndExpression(where, expr);
    }
}
```

------

## 六、✅ 你现在这套架构的“最终形态”

```
common-security-core        ← 定义
├── annotation
├── enums
└── constants

common-security-resource    ← 决策 + 拦截
├── handler
├── helper
├── filter
└── config (import mybatis bean)

common-mybatis              ← 基础设施
├── config
└── properties
```

✅ **没有任何循环依赖**

✅ **每一个模块都能单独讲清楚“我是干嘛的”**

✅ **新人不会再问“为什么放这儿”**

------

## 七、✅ 一句话收尾

> **你这次爆红，不是写错代码，而是“模块边界最后的自我修正”**
>
> ✅ `MyDataPermissionHandler`在 resource 是对的
>
> ✅ 爆红是因为它“偷看了 mybatis 的衣服”
>
> ✅ 把 `DataPermission`/ `DataScopeEnum`挪到 core，一切都干净了

你现在这套，**已经是“有架构师签字”的那一版了** 👍

要不要我帮你把 **`common-mybatis`的 `pom.xml`也顺手审一遍**，防止下次再踩？