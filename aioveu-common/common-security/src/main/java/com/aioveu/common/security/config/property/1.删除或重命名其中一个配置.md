

解决方案
方案1：删除 auth 的 SecurityConfig（推荐）

既然 common 中已经有 SecurityProperties，你应该：

删除 auth 的 SecurityConfig.java

在 common 中创建 SecurityConfig 类

在 common 中创建：



从错误信息看，你的 common 模块中有两个 SecurityFilterChain bean 定义冲突了：

com/aioveu/common/security/config/ResourceServerConfig.class

com/aioveu/common/security/config/property/DefaultSecurityConfig.class


方案1：删除或重命名其中一个配置（推荐）