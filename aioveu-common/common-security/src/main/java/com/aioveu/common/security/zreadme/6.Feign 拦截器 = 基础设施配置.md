这个 放哪儿 其实非常讲究，而且放错位置 = 拦截器直接不生效。  
我直接给你终局级答案，你照着放，一次成功 ✅

---

## ✅ 一句话终局结论（先给你）

> 放在 `common-security` 或 `common-core` 这种「被所有微服务依赖的公共模块」里  
> ❌ 不要放在某个业务微服务里

---

## ✅ 最推荐的位置（✅ 你这个项目的正确位置）

### ✅ `common-security` 模块（最合适）

```
common-security
 └── src/main/java
     └── com/aioveu/common/security/config
         └── FeignInterceptorConfig.java
```

✅ 订单、支付、租户都会生效
✅ 只配一次
✅ 全局生效

---

## ✅ 为什么必须放这里？

| 模块            | 是否适合       |
| --------------- | -------------- |
| common-security | ✅ 最适合       |
| common-core     | ✅ 也行         |
| order-service   | ❌ 只对自己生效 |
| pay-service     | ❌ 只对自己生效 |
| tenant-service  | ❌ 只对自己生效 |

👉 Feign 拦截器是“基础设施”

---

## ✅ 你这个类的终局形态（✅ 直接用）

```java
package com.aioveu.common.security.config;

import com.aioveu.common.security.interceptor.ClientContextFeignInterceptor;
import feign.RequestInterceptor;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class FeignInterceptorConfig {

    @Bean
    public RequestInterceptor clientContextFeignInterceptor() {
        return new ClientContextFeignInterceptor();
    }
}
```

✅ Spring Boot 启动自动扫描
✅ Feign 自动生效

---

## ✅ 怎么验证有没有生效？

### ✅ 1️⃣ 启动日志

你会看到：

```
Feign 透传 clientId = xxx
```

### ✅ 2️⃣ 调用不再失败

```java
tenantFeignClient.getTenantWxAppInfoByClientId(clientId);
```

✅ 不再 400
✅ 不再 null

---

## ✅ 常见错误（你一定要避开）

| 错误                  | 结果             |
| --------------------- | ---------------- |
| 放在 order-service    | ❌ 其他服务不生效 |
| 忘了 `@Configuration` | ❌ 不加载         |
| 类名不是 `*Config`    | ❌ 扫描不到       |
| 没被打包进 common     | ❌ 没用           |

---

## ✅ 用一句永远不会错的话总结

> Feign 拦截器 = 基础设施配置  
> 基础设施 = common 模块  
> 不是业务模块

---

## ✅ 最终结论（给你一颗真正的定心丸）

✅ 放 `common-security`
✅ 加 `@Configuration`
✅ 返回 `RequestInterceptor`
✅ 一次配置，全局生效

👉 这一版，真的、真的、真的可以封板了 😄🫡  
如果你愿意，我可以顺手帮你把 「common-security 的模块职责图」 画出来，以后谁都不敢乱放。