好，路线 B 是最干净的架构方案。我一步一步带你落地，确保每个环节都说清楚。

---

## 🏗️ 路线 B 完整实现步骤

### 第一步：枚举上删掉 `@EnumValue`

```java
package com.aioveu.common.enums.pay;

import lombok.Getter;

public enum PaymentMethodEnum {

    APP(1, "APP", "APP支付"),
    H5(2, "H5", "H5支付"),
    JSAPI(3, "JSAPI", "小程序/公众号支付"),
    NATIVE(4, "NATIVE", "扫码支付"),
    PAGE(5, "PAGE", "网页支付"),
    WAP(6, "WAP", "WAP支付"),
    BALANCE(7, "BALANCE", "余额支付"),
    UNKNOWN(8, "UNKNOWN", "未知");

    @Getter
    private final int code;       // OMS 用（INT 列）

    @Getter                   // ← @EnumValue 删掉了！
    private final String value;   // PAY 用（VARCHAR 列）

    @Getter
    private final String label;

    PaymentMethodEnum(int code, String value, String label) {
        this.code = code;
        this.value = value;
        this.label = label;
    }

    // ---------- 反向查找 ----------
    public static com.aioveu.common.core.enums.pay.PaymentMethodEnum fromCode(Integer code) {
        if (code == null) return null;
        for (com.aioveu.common.core.enums.pay.PaymentMethodEnum e : values()) {
            if (e.code == code) return e;
        }
        return UNKNOWN;
    }

    public static com.aioveu.common.core.enums.pay.PaymentMethodEnum fromValue(String value) {
        if (value == null || value.isEmpty()) return null;
        for (com.aioveu.common.core.enums.pay.PaymentMethodEnum e : values()) {
            if (e.value.equals(value)) return e;
        }
        return UNKNOWN;
    }
}
```

---

### 第二步：写两个 TypeHandler

#### OMS 专用（int ↔ 枚举，用 `code` 字段）

```java
package com.aioveu.oms.handler;  // ← 放在 OMS 服务里

import com.aioveu.common.core.enums.pay.PaymentMethodEnum;
import org.apache.ibatis.type.BaseTypeHandler;
import org.apache.ibatis.type.JdbcType;
import org.apache.ibatis.type.MappedTypes;

import java.sql.*;

@MappedTypes(PaymentMethodEnum.class)
public class PaymentMethodEnumOrdinalTypeHandler
        extends BaseTypeHandler<PaymentMethodEnum> {

    @Override
    public void setNonNullParameter(PreparedStatement ps, int i,
                                    PaymentMethodEnum parameter, JdbcType jdbcType) {
        ps.setInt(i, parameter.getCode());
    }

    @Override
    public PaymentMethodEnum getNullableResult(ResultSet rs, String columnName) throws SQLException {
        int val = rs.getInt(columnName);
        return rs.wasNull() ? null : PaymentMethodEnum.fromCode(val);
    }

    @Override
    public PaymentMethodEnum getNullableResult(ResultSet rs, int columnIndex) throws SQLException {
        int val = rs.getInt(columnIndex);
        return rs.wasNull() ? null : PaymentMethodEnum.fromCode(val);
    }

    @Override
    public PaymentMethodEnum getNullableResult(CallableStatement cs, int columnIndex) throws SQLException {
        int val = cs.getInt(columnIndex);
        return cs.wasNull() ? null : PaymentMethodEnum.fromCode(val);
    }
}
```

#### PAY 专用（String ↔ 枚举，用 `value` 字段）

```java
package com.aioveu.pay.handler;  // ← 放在 PAY 服务里

import com.aioveu.common.core.enums.pay.PaymentMethodEnum;
import org.apache.ibatis.type.BaseTypeHandler;
import org.apache.ibatis.type.JdbcType;
import org.apache.ibatis.type.MappedTypes;

import java.sql.*;

@MappedTypes(PaymentMethodEnum.class)
public class PaymentMethodEnumCodeTypeHandler
        extends BaseTypeHandler<PaymentMethodEnum> {

    @Override
    public void setNonNullParameter(PreparedStatement ps, int i,
                                    PaymentMethodEnum parameter, JdbcType jdbcType) {
        ps.setString(i, parameter.getValue());
    }

    @Override
    public PaymentMethodEnum getNullableResult(ResultSet rs, String columnName) throws SQLException {
        return PaymentMethodEnum.fromValue(rs.getString(columnName));
    }

    @Override
    public PaymentMethodEnum getNullableResult(ResultSet rs, int columnIndex) throws SQLException {
        return PaymentMethodEnum.fromValue(rs.getString(columnIndex));
    }

    @Override
    public PaymentMethodEnum getNullableResult(CallableStatement cs, int columnIndex) throws SQLException {
        return PaymentMethodEnum.fromValue(cs.getString(columnIndex));
    }
}
```

---

### 第三步：各自服务注册 TypeHandler

#### OMS 服务

```java
package com.aioveu.oms.config;

import com.aioveu.common.core.enums.pay.PaymentMethodEnum;
import com.aioveu.oms.handler.PaymentMethodEnumOrdinalTypeHandler;
import org.apache.ibatis.session.Configuration;
import org.mybatis.spring.boot.autoconfigure.ConfigurationCustomizer;
import org.springframework.context.annotation.Bean;

@Configuration
public class OmsMybatisConfig {

    @Bean
    public ConfigurationCustomizer omsTypeHandlerConfig() {
        return new ConfigurationCustomizer() {
            @Override
            public void customize(Configuration configuration) {
                configuration.getTypeHandlerRegistry()
                        .register(PaymentMethodEnum.class, PaymentMethodEnumOrdinalTypeHandler.class);
            }
        };
    }
}
```

#### PAY 服务

```java
package com.aioveu.pay.config;

import com.aioveu.common.core.enums.pay.PaymentMethodEnum;
import com.aioveu.pay.handler.PaymentMethodEnumCodeTypeHandler;
import org.apache.ibatis.session.Configuration;
import org.mybatis.spring.boot.autoconfigure.ConfigurationCustomizer;
import org.springframework.context.annotation.Bean;

@Configuration
public class PayMybatisConfig {

    @Bean
    public ConfigurationCustomizer payTypeHandlerConfig() {
        return new ConfigurationCustomizer() {
            @Override
            public void customize(Configuration configuration) {
                configuration.getTypeHandlerRegistry()
                        .register(PaymentMethodEnum.class, PaymentMethodEnumCodeTypeHandler.class);
            }
        };
    }
}
```

---

### 第四步：实体类恢复干净的枚举字段

OMS 的 `OmsOrder`：

```java
@TableField("payment_method")
private PaymentMethodEnum paymentMethod;  // ← 不加任何 typeHandler 注解，靠全局注册
```

PAY 的 `PyOrder`（或类似）：

```java
@TableField("payment_method")
private PaymentMethodEnum paymentMethod;  // ← 同样不加注解，靠全局注册
```

---

### 第五步：同样处理 `PaymentChannelEnum`

你的日志里 `payment_channel = 2` 也是一样的坑。照抄上面 5 步就行：

| 步骤                                          | PaymentChannelEnum |
| --------------------------------------------- | ------------------ |
| 删 `@EnumValue`                               | ✅                  |
| 加 `fromCode(Integer)` / `fromValue(String)`  | ✅                  |
| OMS 写 `PaymentChannelEnumOrdinalTypeHandler` | ✅                  |
| PAY 写 `PaymentChannelEnumCodeTypeHandler`    | ✅                  |
| 各自服务注册                                  | ✅                  |

---

## 🔍 验证是否生效

启动 OMS 服务，调订单详情接口，看日志：

```
【获取订单详情】支付方式, paymentMethod=JSAPI  ← ✅ 不再是 null
```

SQL 日志里应该看到：

```
payment_method = 3  →  MyBatis 用 OrdinalTypeHandler  →  PaymentMethodEnum.JSAPI
```

---

## ⚠️ 几个容易踩的坑

| 坑                                                           | 怎么避                                                     |
| ------------------------------------------------------------ | ---------------------------------------------------------- |
| 忘记删 `@EnumValue`                                          | 删掉后 TypeHandler 才生效，否则 `@EnumValue` 优先级更高    |
| TypeHandler 类放在 common 模块，两边服务都扫到了             | 各自服务放各自的 handler 包，不要放 common                 |
| MyBatis-Plus 版本 < 3.4.0                                    | `ConfigurationCustomizer` 需要换 `@PostConstruct` 方式注册 |
| XML Mapper 里手写了 `<result column="payment_method" property="paymentMethod"/>` 没指定 typeHandler | 加上 `typeHandler` 属性，或者删掉这行让它走全局            |

---

## 📋 改完后的整体架构

```
common 模块（枚举定义，干净无注解）
├── PaymentMethodEnum
├── PaymentChannelEnum
│
├── oms 服务
│   ├── handler/PaymentMethodEnumOrdinalTypeHandler.java
│   ├── handler/PaymentChannelEnumOrdinalTypeHandler.java
│   └── config/OmsMybatisConfig.java  （注册）
│
└── pay 服务
    ├── handler/PaymentMethodEnumCodeTypeHandler.java
    ├── handler/PaymentChannelEnumCodeTypeHandler.java
    └── config/PayMybatisConfig.java  （注册）
```

一个枚举，各服务各用各的 TypeHandler，互不干扰。

---

需要我帮你把 `PaymentChannelEnum` 的 TypeHandler 也写出来吗？你把它贴出来我照着改就行。