这个问题问得非常关键 👍  
我直接按 “在哪用 / 什么时候用 / 完整示例 / 易踩坑” 四个层次，帮你把这个机制彻底讲清楚，你照着抄就能用。

---

# 一、这句话「到底是干嘛的」

```java
Long tokenVersion = jwt.getClaimAsLong("token_version");
Long currentVersion = redisTemplate.opsForValue().get(versionKey);

if (currentVersion == null || !currentVersion.equals(tokenVersion)) {
    throw new OAuth2AuthenticationException(
        new OAuth2Error("invalid_token", "用户已被强制下线", null)
    );
}
```

👉 一句话含义：

> **JWT 里存一个“令牌版本号”，Redis 里存“当前有效版本号”，  
> 不一致 → 直接踢下线。**

这是 不改 JWT、不遍历 Redis、不扫 JTI 的终极踢人方案。

---

# 二、整体工作原理（先看懂这个）

### ✅ 登录时

```text
JWT 里写入：
token_version = 1
```

```text
Redis 里存：
auth:user:token:version:{userId} = 1
```

---

### ✅ 管理员踢人 / 改密

```text
Redis：
auth:user:token:version:{userId} = 2
```

👉 JWT 还是 1
👉 Redis 已经是 2
👉 版本不一致 → 拒绝

---

# 三、JWT 里必须有什么（非常关键）

### ✅ 登录时生成 JWT，必须包含这两个字段

```json
{
  "user_id": 10001,
  "token_version": 1,
  "exp": 1700000000
}
```

### ✅ 用 Hutool 写 JWT（示例）

```java
Map<String, Object> payload = new HashMap<>();
payload.put("user_id", userId);
payload.put("token_version", tokenVersion);
payload.put(JWTPayload.EXPIRES_AT, exp);

String token = JWTUtil.createToken(payload, secret);
```

---

# 四、这个校验「用在哪」（重点）

### ✅ ✅ ✅ 用在 **资源服务器**

不是网关，不是登录接口

✅ 正确位置：
• `JwtBlacklistFilter`

• 或 `OncePerRequestFilter`

• 或 Spring Security 的 `JwtAuthenticationConverter`


---

# 五、✅ 推荐你用的完整 Filter（直接可用）

```java
@Slf4j
@Component
@RequiredArgsConstructor
public class JwtVersionFilter extends OncePerRequestFilter {

    private final RedisTemplate<String, Object> redisTemplate;

    @Override
    protected void doFilterInternal(
            HttpServletRequest request,
            HttpServletResponse response,
            FilterChain filterChain) throws ServletException, IOException {

        Authentication authentication =
                SecurityContextHolder.getContext().getAuthentication();

        if (!(authentication instanceof JwtAuthenticationToken jwtAuth)) {
            filterChain.doFilter(request, response);
            return;
        }

        Jwt jwt = jwtAuth.getToken();

        Long userId = jwt.getClaimAsLong("user_id");
        Long tokenVersion = jwt.getClaimAsLong("token_version");

        if (userId == null || tokenVersion == null) {
            throw new OAuth2AuthenticationException(
                    new OAuth2Error("invalid_token", "Token 非法", null)
            );
        }

        String versionKey = String.format("auth:user:token:version:%d", userId);
        Long currentVersion = (Long) redisTemplate.opsForValue().get(versionKey);

        if (currentVersion == null || !currentVersion.equals(tokenVersion)) {
            log.warn("用户已被强制下线: userId={}", userId);
            throw new OAuth2AuthenticationException(
                    new OAuth2Error("invalid_token", "用户已被强制下线", null)
            );
        }

        filterChain.doFilter(request, response);
    }
}
```

---

# 六、什么时候触发「版本升级」

### ✅ 改密码

```java
tokenManagerService.invalidateUserSessions(userId);
```

### ✅ 管理员踢人

```java
tokenManagerService.invalidateUserSessions(userId);
```

### ✅ 用户自己踢自己（退出所有设备）

```java
tokenManagerService.invalidateUserSessions(currentUserId);
```

---

# 七、Redis Key 设计（你这套体系的标准）

| Key                                | 作用         |
| ---------------------------------- | ------------ |
| `auth:user:token:version:{userId}` | 当前有效版本 |
| `auth:revoked:jti:{jti}`           | 单点登出     |
| `auth:user:token:version`          | ✅ 主踢人机制 |

---

# 八、✅ 推荐你的最终校验顺序（非常重要）

### ✅ 资源服务器 Filter 顺序

```
1️⃣ JWT 合法性（签名 / 过期）
2️⃣ token_version 校验 ✅（主）
3️⃣ JTI 黑名单 ✅（辅）
4️⃣ 权限校验
```

### ✅ 优先级原则

> token_version 是“核武器”，JTI 是“战术武器”

---

# 九、常见坑（你一定会遇到）

### ❌ 1️⃣ JWT 里没 `token_version`

👉 老 Token 直接拒绝

### ❌ 2️⃣ Redis 里没有 versionKey

👉 视为“未登录 / 被踢”

### ❌ 3️⃣ 网关做版本校验

👉 ❌ 不要，留给资源服务器

---

# 十、一句话帮你彻底想通

> **JWT 是“身份证”，  
> Redis token_version 是“户籍状态”，  
> 身份证没过期 ≠ 户籍没注销。**

你现在这套：
• ✅ JTI 黑名单（精准）

• ✅ token_version（全局）

• ✅ Redis + JWT 双保险


已经是 企业级 SaaS / 多租户 / 中台级 的 Token 安全设计了 👍

如果你愿意，下一步我可以帮你把：
• ✅ JWT + Redis 校验时序图

• ✅ “改密 / 踢人 / 禁用” 的完整安全策略

• ✅ 网关 + 资源服务器职责边界图


整理成一份 《平台级 Token 安全规范》，你团队以后照着写就不会乱。