**结论：不能直接获取，必须加一行代码。**

你的 `SecurityUtils`类**目前还没有**直接获取 `clientId`的方法。但是，因为你已经在 **JWT Token Customizer** 里把 `client_id`写进了 Token，所以**加一个方法非常简单，而且非常安全**。

------

## 一、为什么现在拿不到？

你的 `SecurityUtils`里现有的方法（如 `getTenantId()`, `getUserId()`）都是从 `getTokenAttributes()`里取字段的。

```
Map<String, Object> tokenAttributes = getTokenAttributes();
return Convert.toLong(tokenAttributes.get("tenant_id"));
```

但是，你目前的 `getTokenAttributes()`只处理了 `JwtAuthenticationToken`，并且你**还没有**写一个专门取 `client_id`的方法。

------

## 二、✅ 解决方案：加一个 `getClientId()`方法

你只需要在 `SecurityUtils`里新增下面这个方法即可：

```
/**
 * 获取当前请求的客户端ID (ClientId)
 * <p>
 * 该值通常在 Authorization Server 的 JwtTokenCustomizer 中被注入到 JWT 中。
 *
 * @return ClientId，如果不存在则返回 null
 */
public static String getClientId() {
    Map<String, Object> tokenAttributes = getTokenAttributes();
    if (tokenAttributes != null) {
        // JWT 里的字段通常是字符串
        String clientId = (String) tokenAttributes.get("client_id");
        log.info("SecurityUtils 获取当前 ClientId: {}", clientId);
        return clientId;
    }
    return null;
}
```

------

## 三、✅ 优化 `getUser()`方法（重要）

你之前的 `getUser()`方法里有一段**注释掉的代码**，说 `Principal`是 `Jwt`类型而不是 `SysUserDetails`。

**现在有了 `client_id`，你可以让 `SysUserDetails`更完整。**

建议在 `getUser()`方法中，当 `principal instanceof Jwt`时，把 `clientId`也塞进去（如果你的 `SysUserDetails`支持的话）：

```
} else if (principal instanceof Jwt jwt) {
    log.info("✅ Principal 是 Jwt 类型，从 JWT 构建用户信息");

    SysUserDetails userDetails = new SysUserDetails();
    userDetails.setUsername(jwt.getSubject());

    // 从 JWT 中获取之前写入的 clientId
    String clientId = jwt.getClaimAsString("client_id");
    userDetails.setClientId(clientId); // 假设你给 SysUserDetails 加了 clientId 字段

    // ... 其他字段赋值
    log.info("✅ 从 JWT 成功构建用户: {}, clientId: {}", jwt.getSubject(), clientId);
    return Optional.of(userDetails);
}
```

------

## 四、✅ 使用场景

现在，你可以在**任何有 JWT 的地方**直接调用了：

| 场景                     | 代码                                                      |
| ------------------------ | --------------------------------------------------------- |
| **Controller / Service** | `String clientId = SecurityUtils.getClientId();`          |
| **Gateway 解析后**       | `claims.get("client_id", String.class)`                   |
| **Feign 透传**           | `ClientContext.setClientId(SecurityUtils.getClientId());` |

------

## 五、✅ 架构师级总结

| 层级              | 动作                             | 状态         |
| ----------------- | -------------------------------- | ------------ |
| **Auth Server**   | `claims.claim("client_id", ...)` | ✅ 已做       |
| **SecurityUtils** | `getClientId()`方法              | ✅ **需新增** |
| **下游服务**      | `SecurityUtils.getClientId()`    | ✅ 可用       |

> **最后一句话：**
>
> 你现在拥有了**全链路可信的 `clientId`**。从 Auth Server 签发，到 Gateway 校验，再到下游 Service 使用，一气呵成。

要不要我帮你检查一下 `SysUserDetails`是否需要增加 `clientId`字段，以及如何在 `getUser()`里统一处理？