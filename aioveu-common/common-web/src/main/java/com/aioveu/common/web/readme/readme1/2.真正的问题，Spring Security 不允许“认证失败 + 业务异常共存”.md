对，你这句话**完全正确**，而且这次日志已经**把最后一丝幻觉也打碎了** 👍

我直接给你一个**“不再挣扎、不再试、不再猜”的终局答案。

------

## ✅ 一句话终局结论（先给你）

> ✅ **tenant 的 Controller 确实调用了**
>
> ✅ **BusinessException 确实抛了**
>
> ✅ **GlobalExceptionHandler 也确实执行了**
>
> ❌ **但：Spring Security 在它之后，又把响应“覆盖”成了 401**

👉 **所以你才会看到：**

> “明明调用了，却还是被当成认证异常”

------

## ✅ 你这个日志，已经把“凶手”钉死在这一行

### tenant 日志里反复出现的：

```
com.aioveu.common.security.filter.JwtBlacklistFilter
BearerTokenAuthenticationFilter
AuthorizationFilter
```

👉 **这些是 Spring Security 的过滤器**

👉 **它们在 `GlobalExceptionHandler`之后仍然会执行**

------

## ✅ 你现在看到的，是“双重异常”

### tenant 内部真实发生的是：

```
1️⃣ Controller 执行
   ↓
2️⃣ BusinessException 抛出
   ↓
3️⃣ GlobalExceptionHandler 处理
   ↓
4️⃣ 返回 HTTP 200 + code=A0405
   ↓
5️⃣ Spring Security 过滤器链仍在运行
   ↓
6️⃣ AuthorizationFilter 发现：
   “这个请求虽然有响应，但当初没通过认证”
   ↓
7️⃣ 强制覆盖成 HTTP 401
   ↓
8️⃣ Feign 收到 401
```

👉 **不是没调用**

👉 **是被覆盖了**

------

## ✅ 为什么你会觉得“好麻烦啊”

因为你一直在解决：

> ❌ “为什么我的异常没生效”

但真正的问题是：

> ✅ **Spring Security 不允许“认证失败 + 业务异常共存”**

------

## ✅ 用一句人话解释

> **Spring Security 的逻辑是：**
>
> “只要你没带合法 token，
>
> 不管你后面抛什么异常，
>
> 我都只认 401。”

------

## ✅ 现在只有两条路（没有第三条）

------

## ✅ ✅ 方案一（✅ 强烈推荐，平台级）

### **这个接口必须完全绕过 Spring Security**

### tenant 的 `SecurityFilterChain`（✅ 必须）：

```
.requestMatchers(
    "/api/v1/oauth-client-wx-app/**"
).permitAll()
```

✅ 不校验 token

✅ 不覆盖响应

✅ 你的异常正常

✅ Feign 不炸

👉 **这是“不麻烦”的解法**

------

## ✅ 方案二（❌ 极麻烦，不推荐）

你要同时做：

- 

  自定义 `AuthenticationEntryPoint`

- 

  自定义 `AccessDeniedHandler`

- 

  自定义 `BearerTokenAuthenticationFilter`

- 

  自定义 Feign `ErrorDecoder`

- 

  每个微服务对齐异常码

👉 **这才是真的麻烦**

------

## ✅ 最终结论（你可以彻底收工了）

| 问题             | 答案                |
| ---------------- | ------------------- |
| 调用了吗         | ✅ 调用了            |
| 异常抛了吗       | ✅ 抛了              |
| handler 生效了吗 | ✅ 生效了            |
| 为什么还是 401   | ✅ Security 覆盖响应 |
| 怎么彻底解决     | ✅ 放行这个接口      |

------

## ✅ 工程级大实话（送你一句）

> **在 Spring Security 里：**
>
> ❌ **没有“业务异常优先”**
>
> ✅ **只有“认证优先”**
>
> **只要它还管这个接口，你就永远 401。**

------

如果你愿意，下一步我可以帮你：

- ✅ 把 **tenant 的 SecurityConfig 改成“最少麻烦版”**
- ✅ 把 **接口分成：用户接口 / 内部接口 / 公共接口**
- ✅ 让你 **再也不用处理这种“明明调用了还是 401”的鬼问题**

你现在不是写错代码

👉 **你是在跟 Spring Security 的设计博弈**

👉 **而且你已经赢到最后一厘米了** 😄