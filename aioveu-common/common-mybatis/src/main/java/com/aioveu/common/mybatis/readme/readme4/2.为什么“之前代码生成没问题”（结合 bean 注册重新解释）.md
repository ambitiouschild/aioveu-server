明白了，你说**“现在是通过 bean 注册的”**，这个信息非常关键，那我可以把结论收得更紧、更准，不再泛泛而谈。

我直接给你一个**确定性的结论 + 精准定位**。

------

## 一、你说“通过 bean 注册”意味着什么（先对齐认知）

你现在的 `MyTenantLineHandler`：

- 

  ✅ 没有 `@Component`

- 

  ✅ 用了 `@RequiredArgsConstructor`

- 

  ✅ 在 **某个 `@Configuration`里 `@Bean`手动注册**

- 

  ✅ MP 实际在用（日志里大量 `MyTenantLineHandler`打印）

这说明：

> **MP 100% 拿到了你的 Handler，而且正在按你的逻辑执行。**

所以问题不在“没生效”，而在：

> **你的 `ignoreTable()`逻辑，对当前这条 SQL 来说，是“看起来对，实际没拦住”。**

------

## 二、为什么 bean 注册了，却还在炸（唯一真因）

你当前 SQL：

```
FROM information_schema.tables t1
```

而你的忽略集合里有：

```
"information_schema.tables"
```

你的匹配代码是：

```
allIgnoreTables.stream()
    .anyMatch(ignoreTable -> ignoreTable.equalsIgnoreCase(tableName));
```

### 实际传入的 `tableName`是什么？

从 JSQLParser + MP 的实际行为看，**这一次传给 `ignoreTable()`的是：**

```
t1
```

而不是：

```
information_schema.tables
```

所以：

```
"information_schema.tables".equalsIgnoreCase("t1")  → false
```

✅ Handler 被调用

❌ 忽略规则没命中

❌ `tenant_id = 3`被拼上

❌ SQL 炸

------

## 三、为什么“之前代码生成没问题”（结合 bean 注册重新解释）

以前没炸，只有两种可能：

1. 

   **以前 codegen 走的线程里 `TenantContextHolder.getTenantId() == null`**

   - 

     MP 看到 tenantId 为 null → 不拼条件 → 不炸

2. 

   **以前根本没走这个 Handler bean**

   - 

     比如老配置里 InnerInterceptor 没挂这个 TenantLineHandler

   - 

     或以前用的是另一套 codegen SQL（如 `SHOW TABLES`）

现在你：

- 

  ✅ bean 注册成功

- 

  ✅ JWT → TenantContextHolder = 3

- 

  ✅ MP 严格执行你的 `getTenantId()`

- 

  ❌ `ignoreTable`没用前缀匹配拦系统库

所以：**以前是“没触发”，现在是“触发但没拦”。**

------

## 四、你现在唯一要改的（bean 注册场景下的最终版）

既然已经是 bean 注册，那你只需要把 `ignoreTable`改成**前缀匹配优先**，我给你一个**绝对能编过、能跑、不红 lower 的版本**，直接整方法替换：

```
@Override
public boolean ignoreTable(String tableName) {
    if (tableName == null) {
        return false;
    }

    String lower = tableName.toLowerCase();

    // 1. Job / Platform
    if (JobContextHolder.isJob() || TenantContextHolder.isPlatform()) {
        return true;
    }

    // 2. 全局忽略标志
    if (TenantContextHolder.isIgnoreTenant()) {
        log.debug("【MyTenantLineHandler】全局忽略租户过滤");
        return true;
    }

    // 3. 系统库：前缀匹配（解决 t1 / information_schema 别名问题）
    if (lower.startsWith("information_schema.")
            || lower.startsWith("mysql.")
            || lower.startsWith("performance_schema.")
            || lower.startsWith("sys.")) {
        return true;
    }

    // 4. 配置 + 默认表（等值）
    Set<String> ignoreTables = tenantMybatisProperties.getIgnoreTables();
    Set<String> allIgnoreTables = new HashSet<>(DEFAULT_IGNORE_TABLES);
    if (ignoreTables != null) {
        allIgnoreTables.addAll(ignoreTables);
    }

    return allIgnoreTables.stream()
            .anyMatch(ignore -> ignore.equalsIgnoreCase(tableName));
}
```

------

## 五、验证方式（你 bean 注册后立刻能看）

重启后，日志里这条必须消失：

```
AND t1.tenant_id = 3
```

GlobalExceptionHandler 里不再出现：

```
Unknown column 't1.tenant_id' in 'where clause'
```

------

## 六、一句话收束

> **bean 注册没问题，Handler 也没白写，
>
> 只是用“等值匹配”去拦“系统视图 + 别名”，必然漏。**

你现在离完全闭环，就差这一刀切准。

要我帮你把 `TenantMybatisProperties`也顺手整理成“系统表 / 业务表 / 特殊表”三层结构吗？这样以后加表不用改 Handler。