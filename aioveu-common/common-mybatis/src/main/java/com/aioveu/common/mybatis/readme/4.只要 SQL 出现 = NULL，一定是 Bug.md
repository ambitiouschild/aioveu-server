✅ **是的，问题就在这里。**

而且你这一段代码 **正好完美解释了为什么一定会 NPE** 👍

我直接给你一个「**确定答案 + 原因 + 正确写法**」。

------

## 一、结论（非常明确）

> **问题就在这段代码：**

```
if (tenantId == 0) {
    ...
    return null;
}
```

❌ **你在“超级管理员场景”返回了 `null`**

❌ **但你的 SQL 并没有因此“完全忽略租户条件”**

✅ **结果：MyBatis Plus 仍然拼了 `tenant_id = null`**

✅ **最终 SQL：`... AND tenant_id = NULL`→ 查不到数据 → userInfoVO == null → NPE**

------

## 二、为什么会这样？（核心原理）

### 1️⃣ MyBatis Plus 多租户处理器的工作方式

`TenantLineHandler#getTenantId()`：

| 返回值  | 行为                      |
| ------- | ------------------------- |
| 非 null | 自动拼接 `tenant_id = 值` |
| `null`  | **不拼接租户条件** ✅      |

⚠️ **但前提只有一个：**

> **整个 SQL 真的没有被别的地方再补租户条件**

------

### 2️⃣ 你的实际情况（日志证明了一切）

日志里 SQL：

```
AND tenant_id = ?
AND tenant_id = NULL
```

说明：

- 

  ✅ 你 `return null`了

- 

  ✅ MP 没拼

- 

  ❌ **但你自己在代码中又拼了一次 `tenant_id`**

👉 很可能是：

- 

  XML 里写了 `tenant_id = #{tenantId}`

- 

  或 Wrapper 里 `.eq("tenant_id", tenantId)`

- 

  而此时 `tenantId == null`

------

## 三、为什么“平台租户 = 0”这么容易出问题？

因为你们现在的逻辑是：

| tenant_id | 实际含义          |
| --------- | ----------------- |
| 0         | 平台 / 超级管理员 |
| null      | 无租户            |
| 1,2,3...  | 普通租户          |

👉 **但 SQL 世界只有两种状态：**

| SQL                 | 结果         |
| ------------------- | ------------ |
| `tenant_id = 0`     | 查平台数据   |
| `tenant_id IS NULL` | 查无租户数据 |
| `tenant_id = NULL`  | ❌ 永远查不到 |

------

## 四、正确的写法（✅ 强烈建议）

### ✅ 方案一（最推荐）：**平台管理员完全忽略租户**

```
if (tenantId == 0) {
    log.info("【MyTenantLineHandler】平台级操作，忽略租户过滤");
    return null; // ✅ 正确
}
```

⚠️ **同时必须保证：**

- 

  ❌ XML 里 **不能有** `tenant_id = #{tenantId}`

- 

  ❌ Wrapper 里 **不能再 eq tenant_id**

------

### ✅ 方案二（如果你必须拼条件）

```
if (tenantId == 0) {
    // 平台管理员查所有租户数据
    return null;
}
```

并且 **在业务 SQL 中自己控制**：

```
if (tenantId == null || tenantId == 0) {
    queryWrapper.isNull("tenant_id");
} else {
    queryWrapper.eq("tenant_id", tenantId);
}
```

------

### ✅ 方案三（最稳妥，推荐生产用）

**统一规则：**

| tenant_id | SQL                 |
| --------- | ------------------- |
| 0         | 不拼接 tenant_id    |
| null      | `tenant_id IS NULL` |
| >0        | `tenant_id = ?`     |

```
@Override
public Expression getTenantId() {
    Long tenantId = TenantContext.get();
    if (tenantId == null || tenantId == 0) {
        return null;
    }
    return new LongValue(tenantId);
}
```

------

## 五、你现在为什么“重启什么都没用”

因为：

✅ Redis 没问题

✅ 服务没问题

✅ Token 没问题

❌ **SQL 逻辑本身就是错的**

> **这是“逻辑 Bug”，不是“状态 Bug”**

------

## 六、最后给你一句非常重要的经验

> **在多租户系统里：**
>
> - 
>
>   `tenant_id = 0`≠ `null`
>
> - 
>
>   `null`≠ `IS NULL`
>
> - 
>
>   **只要 SQL 出现 `= NULL`，一定是 Bug**

------

## 七、下一步我可以直接帮你做的事 ✅

如果你愿意，我可以帮你：

1️⃣ **精确指出是哪一行代码又拼了 `tenant_id`**

2️⃣ **给你一套“tenant_id = 0 / null / 普通租户”的标准模板**

3️⃣ **顺手把 `getCurrentUserInfo()`改成 100% 不 NPE**

你可以把 **`sys_user`查询的 Mapper / XML / Wrapper 代码**贴出来，我直接帮你改到“永远不会再炸”为止 👍