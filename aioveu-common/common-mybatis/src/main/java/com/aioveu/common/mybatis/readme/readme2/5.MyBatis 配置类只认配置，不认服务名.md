这个 `MybatisPlusConfig` 已经非常接近“平台级终态”了，但你现在还在用“猜服务名”的方式决定插件行为，这就是最后一个必须拔掉的“架构钉子”。

我直接给你 结论 → 问题点 → 最终正确写法，不绕。

---

## ✅ 一句话结论（先拍板）

> ❌ `isAuthService()` 必须整个删掉
>
> ✅ 是否启用租户 / 数据权限，只能由 `application.yml` 决定
>
> ✅ MyBatis 配置类不应该知道“自己在哪个服务”
>
> ✅ `MyDataPermissionHandler` 也不应该在这里 `new`

---

## 二、你现在这个配置类的 4 个核心问题

---

### ❌ 问题 1：`isAuthService()` 是“架构级反模式”

```java
private boolean isAuthService() {
    applicationName.contains("auth")
}
```

#### 为什么这是错的？

| 问题           | 说明                    |
| -------------- | ----------------------- |
| 服务改名就炸   | auth-service → auth-svc |
| 多实例无法区分 | 灰度 / 多部署           |
| 单元测试不可控 |                         |
| MyBatis 越权   | DAO 层判断应用身份      |

👉 MyBatis 插件只应该问两个问题：
1. 配置开了吗？
2. Bean 存在吗？

---

### ❌ 问题 2：`MyDataPermissionHandler` 被硬编码 new

```java
new DataPermissionInterceptor(new MyDataPermissionHandler())
```

这会导致：

• `common-mybatis` 被迫依赖 `MyDataPermissionHandler`

• 违反你之前已经接受的 SPI 模型

• resource 无法覆盖实现


---

### ❌ 问题 3：`myTenantLineHandler` 字段不存在

```java
log.info("MyTenantLineHandler: {}", myTenantLineHandler);
```

这个类里 没有这个字段，但你却在 `init()` 里用。

👉 这是编译期残留

---

### ❌ 问题 4：`dbType` 仍然“裸”着

```java
private String dbType;
```

虽然注释掉了 `@Value`，但它仍然是：

• 无默认值

• 无 `@ConstructorBinding`

• 可空


---

## 三、✅ 正确的“终态写法”（直接替换）

---

### ✅ 1️⃣ 新增：`MybatisProperties`

📍 `common-mybatis/config/property/MybatisExtProperties.java`

```java
@Data
@ConfigurationProperties(prefix = "mybatis")
public class MybatisExtProperties {

    /**
     * 是否启用多租户插件
     */
    private boolean tenantEnabled = false;

    /**
     * 是否启用数据权限插件
     */
    private boolean dataPermissionEnabled = false;

    /**
     * 数据库类型
     */
    private String dbType = "mysql";
}
```

---

### ✅ 2️⃣ 改造后的 `MybatisPlusConfig`（终态）

```java
@Slf4j
@Configuration
@EnableTransactionManagement
@RequiredArgsConstructor
@EnableConfigurationProperties({
        TenantMybatisProperties.class,
        MybatisExtProperties.class
})
public class MybatisPlusConfig {

    private final TenantMybatisProperties tenantMybatisProperties;
    private final MybatisExtProperties mybatisProperties;

    @PostConstruct
    public void init() {
        log.info("=== MybatisPlusConfig initialized ===");
        log.info("tenantEnabled: {}", mybatisProperties.isTenantEnabled());
        log.info("dataPermissionEnabled: {}", mybatisProperties.isDataPermissionEnabled());
        log.info("dbType: {}", mybatisProperties.getDbType());
    }

    @Bean
    @ConditionalOnMissingBean
    public TenantLineHandler tenantLineHandler() {
        return new MyTenantLineHandler(tenantMybatisProperties);
    }

    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor(
            Optional<TenantLineHandler> tenantLineHandler,
            Optional<DataPermissionHandler> dataPermissionHandler
    ) {

        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();

        // 1. 乐观锁（永远启用）
        interceptor.addInnerInterceptor(new OptimisticLockerInnerInterceptor());

        // 2. 多租户（配置驱动）
        if (mybatisProperties.isTenantEnabled() && tenantLineHandler.isPresent()) {
            interceptor.addInnerInterceptor(
                new TenantLineInnerInterceptor(tenantLineHandler.get())
            );
            log.info("【MybatisPlus】TenantLineInnerInterceptor enabled");
        }

        // 3. 数据权限（配置驱动 + SPI）
        if (mybatisProperties.isDataPermissionEnabled() && dataPermissionHandler.isPresent()) {
            interceptor.addInnerInterceptor(
                new DataPermissionInterceptor(dataPermissionHandler.get())
            );
            log.info("【MybatisPlus】DataPermissionInterceptor enabled");
        }

        // 4. 分页（最后）
        DbType dbType = resolveDbType(mybatisProperties.getDbType());
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(dbType));

        return interceptor;
    }

    private DbType resolveDbType(String dbType) {
        if ("postgresql".equalsIgnoreCase(dbType)
                || "postgres".equalsIgnoreCase(dbType)) {
            return DbType.POSTGRE_SQL;
        }
        return DbType.MYSQL;
    }

    @Bean
    public ConfigurationCustomizer configurationCustomizer() {
        return configuration -> {
            TypeHandlerRegistry registry = configuration.getTypeHandlerRegistry();
            registry.register(String[].class, JdbcType.OTHER, StringArrayJsonTypeHandler.class);
            registry.register(Long[].class, JdbcType.OTHER, LongArrayJsonTypeHandler.class);
            registry.register(Integer[].class, JdbcType.OTHER, IntegerArrayJsonTypeHandler.class);
        };
    }

    @Bean
    public GlobalConfig globalConfig() {
        GlobalConfig globalConfig = new GlobalConfig();
        globalConfig.setMetaObjectHandler(new MyMetaObjectHandler());
        return globalConfig;
    }

    @Bean
    public DatabaseIdProvider databaseIdProvider() {
        VendorDatabaseIdProvider provider = new VendorDatabaseIdProvider();
        Properties props = new Properties();
        props.setProperty("MySQL", "mysql");
        provider.setProperties(props);
        return provider;
    }
}
```

---

### ✅ 3️⃣ YAML 示例（auth / pms 差异）

#### ✅ auth 服务

```yaml
mybatis:
  tenant-enabled: false
  data-permission-enabled: false
  db-type: mysql
```

#### ✅ PMS / SMS

```yaml
mybatis:
  tenant-enabled: true
  data-permission-enabled: true
  db-type: mysql
```

---

## 四、✅ 现在这套的“架构收益”

| 维度         | 老版本 | 新版本 |
| ------------ | ------ | ------ |
| 是否猜服务名 | ❌      | ✅      |
| 是否可测试   | ❌      | ✅      |
| 是否可配置   | ❌      | ✅      |
| 是否 SPI     | ❌      | ✅      |
| 是否可演进   | ❌      | ✅      |

---

## 五、一句话帮你“封死”

> **MyBatis 配置类只认配置，不认服务名。  
> 是否启用租户、是否启用数据权限，是“部署决策”，不是“代码逻辑”。**

---

## 六、你现在这个文件的“最终定性”

你刚才这个配置类，已经走到了 “从‘能跑’到‘平台级’”的临界点。  
删掉 `isAuthService()`，它就是终态。

如果你愿意，下一步我可以帮你：

• ✅ 把 `MyDataPermissionHandler` 的自动装配条件补齐

• ✅ 写 ArchUnit 测试：mybatis 禁止依赖 resource

• ✅ 把这套配置写成 《MyBatis 插件启用规范》ADR


你现在已经不是在“配 MP”，  
你是在 给平台写“启动契约” 👍