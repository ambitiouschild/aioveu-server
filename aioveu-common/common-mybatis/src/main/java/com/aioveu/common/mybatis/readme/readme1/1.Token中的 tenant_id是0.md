从您的日志可以看到问题所在了。让我分析一下：

## 问题分析

从日志可以看出：

1. **租户ID为0**：`SecurityUtils获取当前租户ID:0`
2. **JWT中租户ID为0**：`"tenant_id":0`
3. **MyBatis-Plus获取租户ID为0**：`从SecurityUtils的token获取租户ID: 0`
4. **SQL没有加tenant_id条件**：`SELECT * FROM ums_member`没有 `WHERE tenant_id = 0`

## 问题原因

**租户ID为0**！在数据库中，租户ID通常从1开始，0可能表示无效或特殊的租户。

### 具体分析：

1. **Token中的租户ID是0**：

   ```
   "tenant_id":0
   ```

2. **MyBatis-Plus配置可能跳过了tenant_id=0**：

   ```
   public class MyTenantLineHandler implements TenantLineHandler {
       @Override
       public boolean ignoreTable(String tableName) {
           // 如果租户ID为0，可能被忽略了
           Long tenantId = getTenantId();
           if (tenantId == null || tenantId == 0) {
               return true; // 跳过租户过滤
           }
           return false;
       }
   }
   ```

## 解决方案

### 方案1：确保生成Token时租户ID不为0

在认证服务中，生成Token时要确保租户ID有效：

```
// 生成JWT Token时
public OAuth2AccessToken generateToken(Authentication authentication) {
    SysUserDetails userDetails = (SysUserDetails) authentication.getPrincipal();
    
    // 确保租户ID不为0
    Long tenantId = userDetails.getTenantId();
    if (tenantId == null || tenantId == 0) {
        // 使用默认租户或抛出异常
        tenantId = 1L; // 或从数据库获取用户的默认租户
    }
    
    // 生成包含正确租户ID的Token
    Map<String, Object> claims = new HashMap<>();
    claims.put("tenant_id", tenantId);
    // ... 其他声明
}
```

### 方案2：修改MyBatis-Plus配置

检查您的 `MyTenantLineHandler`实现：

```
@Component
@Slf4j
public class MyTenantLineHandler implements TenantLineHandler {
    
    @Override
    public Expression getTenantId() {
        Long tenantId = TenantContextHolder.getTenantId();
        
        if (tenantId == null) {
            // 从SecurityUtils获取
            tenantId = SecurityUtils.getTenantId();
        }
        
        // ✅ 关键：如果租户ID为0，应该处理
        if (tenantId == null || tenantId == 0) {
            log.warn("租户ID为0或null，使用默认租户ID: 1");
            tenantId = 1L; // 使用默认租户
        }
        
        log.info("MyBatis-Plus使用租户ID: {}", tenantId);
        return new LongValue(tenantId);
    }
    
    @Override
    public String getTenantIdColumn() {
        return "tenant_id";
    }
    
    @Override
    public boolean ignoreTable(String tableName) {
        // 只有特定的表才忽略租户过滤
        List<String> ignoreTables = Arrays.asList(
            "sys_config", "sys_dict", "sys_tenant"
        );
        
        boolean ignore = ignoreTables.contains(tableName);
        if (ignore) {
            log.debug("忽略表 {} 的租户过滤", tableName);
        }
        return ignore;
    }
}
```

### 方案3：修改数据库表结构

检查 `ums_member`表是否有 `tenant_id`列：

```
-- 检查表结构
DESC ums_member;

-- 如果没有tenant_id列，添加
ALTER TABLE ums_member ADD COLUMN tenant_id BIGINT DEFAULT 1 NOT NULL COMMENT '租户ID';

-- 为现有数据设置租户ID
UPDATE ums_member SET tenant_id = 1 WHERE tenant_id IS NULL OR tenant_id = 0;
```

### 方案4：调试MyBatis-Plus插件

添加调试日志：

```
@Configuration
public class MybatisPlusConfig {
    
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        
        interceptor.addInnerInterceptor(new TenantLineInnerInterceptor(new TenantLineHandler() {
            @Override
            public Expression getTenantId() {
                Long tenantId = TenantContextHolder.getTenantId();
                log.info("=== MyBatis-Plus 租户插件调试 ===");
                log.info("1. TenantContextHolder租户ID: {}", tenantId);
                
                if (tenantId == null) {
                    tenantId = SecurityUtils.getTenantId();
                    log.info("2. SecurityUtils租户ID: {}", tenantId);
                }
                
                if (tenantId == null || tenantId == 0) {
                    log.warn("3. 租户ID无效，使用默认值1");
                    tenantId = 1L;
                }
                
                log.info("4. 最终使用的租户ID: {}", tenantId);
                return new LongValue(tenantId);
            }
            
            @Override
            public String getTenantIdColumn() {
                return "tenant_id";
            }
        }));
        
        return interceptor;
    }
}
```

## 立即验证

### 步骤1：检查数据库表结构

```
-- 查看ums_member表是否有tenant_id列
SHOW CREATE TABLE ums_member;
```

### 步骤2：检查MyBatis-Plus配置

查看您的 `MyTenantLineHandler`实现，确保不会因为租户ID为0而忽略过滤。

### 步骤3：修改Token生成

确保登录时生成的Token包含正确的租户ID（不为0）。

## 临时解决方案

如果急需解决，可以修改 `MyTenantLineHandler`：

```
@Override
public Expression getTenantId() {
    Long tenantId = SecurityUtils.getTenantId();
    
    // 如果租户ID为0，改为1
    if (tenantId != null && tenantId == 0) {
        log.warn("租户ID为0，自动修正为1");
        tenantId = 1L;
    }
    
    // 如果还是没有租户ID，使用默认值
    if (tenantId == null) {
        tenantId = 1L;
    }
    
    return new LongValue(tenantId);
}
```

## 总结

**根本原因**：Token中的 `tenant_id`是0，而MyBatis-Plus可能：

1. 跳过了tenant_id=0的过滤
2. 或者数据库表中没有tenant_id列
3. 或者tenant_id=0被认为是不需要过滤

**解决方案**：

1. ✅ 确保生成Token时租户ID不为0
2. ✅ 修改MyBatis-Plus配置，正确处理tenant_id=0的情况
3. ✅ 检查数据库表是否有tenant_id列
4. ✅ 确保现有数据有正确的租户ID值