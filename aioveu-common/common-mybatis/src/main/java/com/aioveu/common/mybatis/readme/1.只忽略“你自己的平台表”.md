这个问题问得非常细、也非常对 👍  
我直接给你 明确答案 + 原因 + 你现在这套代码里「正确的做法」。

---

## ✅ 一句话结论（先给你）

> ✅ 是的，这里必须加你要“忽略的表名”  
> ❌ 不是让你加“系统元数据表”  
> ✅ 而是加「你自己的业务平台表」

你现在这段：

```java
Set<String> systemTables = Set.of(
    "tables",
    "columns",
    "all_tables"
);
```

👉 这是错的用途（这是数据库字典表）  
👉 不是你这个 `ignoreTable()` 该干的事

---

## ✅ 二、你这个 `ignoreTable()` 的正确用途是什么？

👉 告诉 MP：哪些表“不需要租户隔离”

也就是：

| 表类型     | 举例              | 是否 ignore |
| ---------- | ----------------- | ----------- |
| 平台配置表 | pay_config_wechat | ✅           |
| 平台配置表 | pay_config_alipay | ✅           |
| 租户表     | sys_tenant        | ✅           |
| 用户表     | sys_user          | ✅           |
| 字典表     | sys_dict          | ✅           |
| 支付订单   | pay_order         | ❌           |
| 订单       | oms_order         | ❌           |

---

## ✅ 三、✅ 你现在应该这样写（正确版）

```java
@Override
public boolean ignoreTable(String tableName) {

    if (tableName == null) {
        return false;
    }

    String table = tableName.toLowerCase();

    // ✅ 1. 平台级表（永远不隔离）
    Set<String> platformTables = Set.of(
        "sys_tenant",
        "sys_user",
        "sys_role",
        "sys_menu",
        "pay_config_wechat",
        "pay_config_alipay"
    );
    if (platformTables.contains(table)) {
        return true;
    }

    // ✅ 2. 显式忽略租户（平台查询用）
    if (TenantContextHolder.isIgnoreTenant()) {
        return true;
    }

    // ✅ 3. 配置里的忽略表（可选）
    List<String> ignoreTables = tenantProperties.getIgnoreTables();
    if (ignoreTables != null) {
        return ignoreTables.stream()
                .anyMatch(t -> t.equalsIgnoreCase(table));
    }

    return false;
}
```

✅ 这才是“对的东西，放在对的地方”

---

## ✅ 四、你原来那段 `systemTables` 应该放哪？

### ❌ 不应该在 `ignoreTable()` 里

```java
"tables"
"columns"
"all_tables"
```

这些是：
• Oracle / MySQL 的 系统元数据表

• 业务系统根本不该直接操作它们


👉 删掉这一段

---

## ✅ 五、为什么你之前会“明明 return null，还是有 tenant_id”？

因为：

• `getTenantId() == null`

• 但 表没有被 ignore

• MP 在某些 SQL（COUNT / UPDATE / DELETE）下 仍然会补条件


✅ 只有 `ignoreTable = true` 才是最终解法

---

## ✅ 六、最终结论（你现在这一步非常关键）

> ❌ 不要忽略数据库系统表  
> ✅ 只忽略“你自己的平台表”  
> ✅ 平台表 = 没有 tenant_id，或被 ignoreTable 忽略

你现在这套 TenantLineHandler  
👉 只要按这个改，平台级全查的问题就彻底解决了 👍

---

## 🚀 下一步我可以帮你

✅ 帮你 把“平台表 / 租户表”边界画清楚  
✅ 帮你 设计一套 SaaS 表结构规范  
✅ 帮你 做一次多租户上线前 Checklist

你可以直接说一句：  
👉 “帮我划分平台表和租户表的边界”