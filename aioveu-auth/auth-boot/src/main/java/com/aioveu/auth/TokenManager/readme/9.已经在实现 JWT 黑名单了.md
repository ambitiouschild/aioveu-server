# **是的！你已经在实现 JWT 黑名单了！**

你的代码**确实已经实现了 Redis 黑名单**的基础功能。让我分析一下：

## **1. 你的实现分析**

### **✅ 已实现的功能**

```
// 1. 令牌吊销功能 ✅
public void invalidateToken(String token) {
    // 解析JWT
    JWT jwt = JWTUtil.parseToken(token);
    JSONObject payloads = jwt.getPayloads();
    String jti = payloads.getStr(JWTPayload.JWT_ID);
    Integer expirationAt = payloads.getInt(JWTPayload.EXPIRES_AT);
    
    // 将jti加入黑名单
    revokeTokenByJti(jti, expirationAt);
}

// 2. 检查令牌是否吊销 ✅
private boolean isTokenRevoked(String jti) {
    return Boolean.TRUE.equals(
        redisTemplate.hasKey(StrUtil.format(RedisConstants.Auth.REVOKED_JTI, jti))
    );
}

// 3. 用户会话管理 ✅
public void invalidateUserSessions(Long userId) {
    // 通过递增版本号使所有旧令牌失效
    String versionKey = StrUtil.format(RedisConstants.Auth.USER_TOKEN_VERSION, userId);
    redisTemplate.opsForValue().increment(versionKey);
}
```

### **2. 你的 Redis 数据结构**

```
# 你的黑名单设计：

# 1. 吊销令牌存储
KEY:  oauth2:revoked:jti:{jti}
VALUE: true
TTL:  令牌剩余有效期

# 2. 用户令牌版本
KEY:  oauth2:token:version:{userId}
VALUE: 版本号（递增）
TTL:  无（永久有效）

# 检查令牌是否吊销：
GET oauth2:revoked:jti:abc123
# 返回 true → 已吊销
# 返回 null → 有效
```

## **2. 缺少的关键组件**

### **❌ 缺失：验证时的黑名单检查**

```
// 问题：你只实现了吊销功能，但没有在验证JWT时检查黑名单！

// 需要在资源服务器中添加这个检查：
@Component
public class JwtValidationService {
    
    @Autowired
    private TokenManagerService tokenManagerService;
    
    /**
     * 验证JWT时，需要调用这个方法检查黑名单
     * 但你现在没有这个调用！
     */
    public boolean validateToken(String token) {
        // 1. 验证JWT签名（Spring Security会做）
        // 2. 检查过期时间（Spring Security会做）
        // 3. ❌ 缺失：检查黑名单！
        
        // 需要添加：
        String jti = extractJtiFromToken(token);
        if (tokenManagerService.isTokenRevoked(jti)) {  // ❌ 你这个方法不是public！
            return false;
        }
        
        return true;
    }
}
```

### **❌ 缺失：公开的检查方法**

```
// 你的 isTokenRevoked 是 private 方法，外部无法调用！
@Service
public class JwtTokenManagerImpl implements TokenManagerService {
    
    // ❌ 这个方法是 private，资源服务器无法调用
    private boolean isTokenRevoked(String jti) { ... }
    
    // ✅ 需要改成 public
    public boolean isTokenRevoked(String token) {
        if (StringUtils.isBlank(token)) {
            return true;
        }
        
        try {
            JWT jwt = JWTUtil.parseToken(token);
            String jti = jwt.getPayloads().getStr(JWTPayload.JWT_ID);
            return isTokenRevokedByJti(jti);  // 内部方法
        } catch (Exception e) {
            return true;  // 解析失败视为无效
        }
    }
    
    // 或者提供一个通过jti检查的方法
    public boolean isTokenRevokedByJti(String jti) {
        if (StringUtils.isBlank(jti)) {
            return false;
        }
        return Boolean.TRUE.equals(
            redisTemplate.hasKey(StrUtil.format(RedisConstants.Auth.REVOKED_JTI, jti))
        );
    }
}
```

## **3. 完整改进方案**

### **3.1 改进你的 TokenManager**

```
@Service
@Slf4j
@RequiredArgsConstructor
public class JwtTokenManagerImpl implements TokenManagerService {

    private final RedisTemplate<String, Object> redisTemplate;
    
    /**
     * 检查令牌是否被吊销
     */
    @Override
    public boolean isTokenRevoked(String token) {
        if (StringUtils.isBlank(token)) {
            return true;
        }
        
        // 移除 Bearer 前缀
        if (token.startsWith(SecurityConstants.BEARER_TOKEN_PREFIX)) {
            token = token.substring(SecurityConstants.BEARER_TOKEN_PREFIX.length());
        }
        
        try {
            JWT jwt = JWTUtil.parseToken(token);
            JSONObject payloads = jwt.getPayloads();
            String jti = payloads.getStr(JWTPayload.JWT_ID);
            
            return isTokenRevokedByJti(jti);
        } catch (Exception e) {
            log.error("检查令牌吊销状态失败", e);
            return true;  // 解析失败视为无效
        }
    }
    
    /**
     * 通过jti检查令牌是否被吊销
     */
    public boolean isTokenRevokedByJti(String jti) {
        if (StringUtils.isBlank(jti)) {
            return false;
        }
        
        String revokedKey = StrUtil.format(RedisConstants.Auth.REVOKED_JTI, jti);
        return Boolean.TRUE.equals(redisTemplate.hasKey(revokedKey));
    }
    
    /**
     * 吊销令牌
     */
    @Override
    public void invalidateToken(String token) {
        if (StringUtils.isBlank(token)) {
            return;
        }
        
        if (token.startsWith(SecurityConstants.BEARER_TOKEN_PREFIX)) {
            token = token.substring(SecurityConstants.BEARER_TOKEN_PREFIX.length());
        }
        
        try {
            JWT jwt = JWTUtil.parseToken(token);
            JSONObject payloads = jwt.getPayloads();
            String jti = payloads.getStr(JWTPayload.JWT_ID);
            Integer expirationAt = payloads.getInt(JWTPayload.EXPIRES_AT);
            
            revokeTokenByJti(jti, expirationAt);
            log.info("【JWT Token 管理器】将令牌加入黑名单, jti={}", jti);
        } catch (Exception e) {
            log.error("吊销令牌失败", e);
        }
    }
    
    /**
     * 内部方法：通过jti吊销令牌
     */
    private void revokeTokenByJti(String jti, Integer expirationAt) {
        if (StringUtils.isBlank(jti)) {
            return;
        }
        
        String revokedJtiKey = StrUtil.format(RedisConstants.Auth.REVOKED_JTI, jti);
        
        if (expirationAt != null) {
            int currentTimeSeconds = Convert.toInt(System.currentTimeMillis() / 1000);
            if (expirationAt < currentTimeSeconds) {
                return;  // 已过期，不需要加入黑名单
            }
            int expirationIn = expirationAt - currentTimeSeconds;
            redisTemplate.opsForValue().set(revokedJtiKey, Boolean.TRUE, expirationIn, TimeUnit.SECONDS);
        } else {
            redisTemplate.opsForValue().set(revokedJtiKey, Boolean.TRUE);
        }
    }
    
    /**
     * 获取用户当前令牌版本
     */
    public Long getUserTokenVersion(Long userId) {
        if (userId == null) {
            return 0L;
        }
        
        String versionKey = StrUtil.format(RedisConstants.Auth.USER_TOKEN_VERSION, userId);
        Object version = redisTemplate.opsForValue().get(versionKey);
        return version != null ? Convert.toLong(version) : 0L;
    }
}
```

### **3.2 添加资源服务器验证**

```
@Component
public class JwtBlacklistValidator {
    
    @Autowired
    private TokenManagerService tokenManagerService;
    
    /**
     * 验证JWT并检查黑名单
     */
    public Jwt validateJwtWithBlacklist(String token) {
        // 1. 先验证JWT签名和基本有效性
        Jwt jwt = validateJwt(token);  // 使用Spring Security的验证
        
        // 2. 检查黑名单
        if (tokenManagerService.isTokenRevoked(token)) {
            throw new JwtValidationException("Token has been revoked");
        }
        
        // 3. 检查用户令牌版本（可选增强）
        String userId = jwt.getSubject();
        Long currentVersion = getCurrentTokenVersionFromJwt(jwt);
        Long storedVersion = tokenManagerService.getUserTokenVersion(Long.valueOf(userId));
        
        if (currentVersion < storedVersion) {
            throw new JwtValidationException("Token version is outdated");
        }
        
        return jwt;
    }
}
```

## **4. 集成到 Spring Security**

### **4.1 创建自定义 JWT 验证器**

```
@Configuration
public class SecurityConfig {
    
    @Autowired
    private TokenManagerService tokenManagerService;
    
    @Bean
    public SecurityFilterChain resourceServerFilterChain(HttpSecurity http) throws Exception {
        
        // 添加黑名单检查过滤器
        http.addFilterBefore(jwtBlacklistFilter(), BearerTokenAuthenticationFilter.class);
        
        http.authorizeHttpRequests(authz -> authz
                .anyRequest().authenticated()
            )
            .oauth2ResourceServer(oauth2 -> oauth2
                .jwt(jwt -> jwt
                    .jwtAuthenticationConverter(jwtAuthenticationConverter())
                )
            );
        
        return http.build();
    }
    
    /**
     * 创建黑名单检查过滤器
     */
    @Bean
    public JwtBlacklistFilter jwtBlacklistFilter() {
        return new JwtBlacklistFilter(tokenManagerService);
    }
    
    /**
     * 自定义JWT认证转换器
     */
    private JwtAuthenticationConverter jwtAuthenticationConverter() {
        JwtAuthenticationConverter converter = new JwtAuthenticationConverter();
        
        converter.setJwtGrantedAuthoritiesConverter(jwt -> {
            // 在这里也可以添加额外的验证
            return extractAuthorities(jwt);
        });
        
        return converter;
    }
}
```

### **4.2 黑名单检查过滤器**

```
@Component
public class JwtBlacklistFilter extends OncePerRequestFilter {
    
    private final TokenManagerService tokenManagerService;
    
    public JwtBlacklistFilter(TokenManagerService tokenManagerService) {
        this.tokenManagerService = tokenManagerService;
    }
    
    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                   HttpServletResponse response, 
                                   FilterChain filterChain) throws ServletException, IOException {
        
        String authHeader = request.getHeader("Authorization");
        
        if (authHeader != null && authHeader.startsWith("Bearer ")) {
            String token = authHeader.substring(7);
            
            // 检查黑名单
            if (tokenManagerService.isTokenRevoked(token)) {
                response.setStatus(HttpStatus.UNAUTHORIZED.value());
                response.setContentType(MediaType.APPLICATION_JSON_VALUE);
                
                Map<String, Object> error = Map.of(
                    "error", "invalid_token",
                    "error_description", "The access token was revoked",
                    "error_uri", "https://tools.ietf.org/html/rfc6750#section-3.1"
                );
                
                new ObjectMapper().writeValue(response.getWriter(), error);
                return;
            }
        }
        
        filterChain.doFilter(request, response);
    }
}
```

## **5. 你需要做的补充工作**

### **5.1 立即要做的**

1. **将 `isTokenRevoked`方法改为 public**
2. **在资源服务器中添加黑名单检查**
3. **实现注销接口**

```
@RestController
public class LogoutController {
    
    @Autowired
    private TokenManagerService tokenManagerService;
    
    @PostMapping("/oauth2/logout")
    public ResponseEntity<?> logout(@RequestHeader("Authorization") String authHeader) {
        if (authHeader != null && authHeader.startsWith("Bearer ")) {
            String token = authHeader.substring(7);
            tokenManagerService.invalidateToken(token);
        }
        
        return ResponseEntity.ok(Map.of("success", true, "message", "注销成功"));
    }
}
```

### **5.2 增强功能（可选）**

1. **会话管理**

   ```
   // 记录用户会话
   public void recordUserSession(Long userId, String jti, String deviceInfo) {
       String sessionKey = StrUtil.format("oauth2:sessions:user:{}", userId);
       redisTemplate.opsForSet().add(sessionKey, jti);
   }
   ```

2. **吊销所有会话的接口**

   ```
   @PostMapping("/admin/users/{userId}/revoke-all")
   public ResponseEntity<?> revokeAllUserSessions(@PathVariable Long userId) {
       tokenManagerService.invalidateUserSessions(userId);
       return ResponseEntity.ok("已吊销用户所有会话");
   }
   ```

## **6. 总结**

**✅ 你已经实现的部分：**

- Redis 黑名单存储结构
- 令牌吊销功能
- 用户令牌版本管理

**❌ 缺失的部分：**

- 公开的黑名单检查方法
- 资源服务器集成验证
- 注销接口

**🔧 建议的改进：**

1. 将 `isTokenRevoked`方法公开
2. 在资源服务器过滤器中添加检查
3. 实现标准注销接口
4. 考虑添加会话记录功能

**你的基础很好，离完整的黑名单方案只差最后一步集成！**