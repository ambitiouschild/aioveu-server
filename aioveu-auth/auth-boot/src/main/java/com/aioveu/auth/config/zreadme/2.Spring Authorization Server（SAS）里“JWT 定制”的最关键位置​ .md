你这个问题问到了 Spring Authorization Server（SAS）里“JWT 定制”的最关键位置 👍  
结论先给：

> ✅ 就在你这个 `JwtTokenCustomizer` 里加
>
> ✅ 加到 `claims.claim("clientId", ...)`
>
> ✅ 不管是系统用户还是会员

下面我给你可直接复制落地的代码。

---

## 一、✅ 在哪里加 clientId？

### ✅ 核心位置（你这个文件里）

```java
if (OAuth2TokenType.ACCESS_TOKEN.equals(context.getTokenType())
        && context.getPrincipal() instanceof UsernamePasswordAuthenticationToken) {

    Optional.ofNullable(context.getPrincipal().getPrincipal())
            .ifPresent(principal -> {

                JwtClaimsSet.Builder claims = context.getClaims();

                // ✅ 统一加 clientId（重点）
                String clientId = context.getRegisteredClient().getClientId();
                claims.claim("client_id", clientId);
                log.info("【JwtTokenCustomizer】✅ 添加 client_id = {}", clientId);

                if (principal instanceof SysUserDetails userDetails) {
                    ...
                } else if (principal instanceof MemberDetails userDetails) {
                    ...
                }
            });
}
```

✅ 这是最干净、最标准的做法

---

## 二、✅ 完整示例（推荐你直接替换）

```java
@Bean
public OAuth2TokenCustomizer<JwtEncodingContext> jwtTokenCustomizer() {
    return context -> {

        if (OAuth2TokenType.ACCESS_TOKEN.equals(context.getTokenType())
                && context.getPrincipal() instanceof UsernamePasswordAuthenticationToken) {

            Optional.ofNullable(context.getPrincipal().getPrincipal())
                    .ifPresent(principal -> {

                        JwtClaimsSet.Builder claims = context.getClaims();

                        // ✅ 1️⃣ 统一注入 clientId（核心）
                        String clientId = context.getRegisteredClient().getClientId();
                        claims.claim("client_id", clientId);
                        log.info("【JwtTokenCustomizer】✅ 添加 client_id = {}", clientId);

                        // ✅ 2️⃣ 系统用户
                        if (principal instanceof SysUserDetails userDetails) {

                            if (userDetails.getUserId() != null) {
                                claims.claim(JwtClaimConstants.USER_ID, userDetails.getUserId());
                            }
                            if (StringUtils.hasText(userDetails.getUsername())) {
                                claims.claim(JwtClaimConstants.USERNAME, userDetails.getUsername());
                            }
                            if (userDetails.getTenantId() != null) {
                                claims.claim(JwtClaimConstants.TENANT_ID, userDetails.getTenantId());
                            }

                        // ✅ 3️⃣ 会员用户
                        } else if (principal instanceof MemberDetails userDetails) {

                            if (userDetails.getTenantId() != null) {
                                claims.claim(JwtClaimConstants.TENANT_ID, userDetails.getTenantId());
                            }
                            claims.claim(JwtClaimConstants.MEMBER_ID, userDetails.getId());
                        }

                        // ✅ 4️⃣ 角色
                        var authorities = AuthorityUtils
                                .authorityListToSet(context.getPrincipal().getAuthorities());

                        claims.claim(JwtClaimConstants.AUTHORITIES, authorities);
                    });
        }
    };
}
```

---

## 三、✅ 为什么要从 `context.getRegisteredClient()` 取？

| 来源                                          | 是否可信 | 原因           |
| --------------------------------------------- | -------- | -------------- |
| `context.getRegisteredClient().getClientId()` | ✅ 100%   | SAS 内部校验过 |
| Header 前端传                                 | ❌        | 不安全         |
| Principal                                     | ❌        | 可能为空       |

👉 这是 Spring Authorization Server 给你的“官方答案”

---

## 四、✅ Gateway 里怎么用？

```java
private String resolveClientIdFromJwt(ServerWebExchange exchange) {

    String token = exchange.getRequest()
            .getHeaders()
            .getFirst(HttpHeaders.AUTHORIZATION);

    if (StringUtils.isBlank(token)) return null;

    Claims claims = Jwts.parserBuilder()
            .setSigningKey(secretKey)
            .build()
            .parseClaimsJws(token.replace("Bearer ", ""))
            .getBody();

    return claims.get("client_id", String.class);
}
```

✅ Gateway 解析 JWT
✅ 拿到可信 clientId
✅ 注入 Header

---

## 五、✅ 架构师级总结（你现在的位置）

| 层级        | 动作                      |
| ----------- | ------------------------- |
| Auth Server | ✅ JWT 里写 `client_id`    |
| Gateway     | ✅ 解析 JWT 拿 `client_id` |
| 下游服务    | ✅ 直接用 Header           |

---

## ✅ 最后一句话（给你定心丸）

> `clientId` 就应该在这里加 —— 在 JWT 签发源头一次性写死。

如果你愿意，下一步我可以帮你：
1️⃣ 把 `client_id` 改成常量统一管理
2️⃣ 设计 JWT 字段命名规范（snake_case / camelCase）
3️⃣ 帮你画 「JWT → Gateway → 微服务」流转图

你直接选 👍