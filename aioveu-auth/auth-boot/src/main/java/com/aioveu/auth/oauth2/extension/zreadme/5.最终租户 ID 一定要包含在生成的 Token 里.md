是的，**最终租户 ID 一定要包含在生成的 Token 里**，否则你这套「多小程序 + 多租户 + 单后端」在**资源服务器侧根本无法做数据隔离**。

而且这一步，在你当前的 `WechatAuthenticationProvider`里**只需要改很少的代码**就能完成。

下面我**只围绕你这一版 Provider**，一步一步告诉你：

> ✅ 租户 ID 从哪里来
>
> ✅ 放在 Token 的哪里
>
> ✅ 怎么放（不破坏你现有结构）
>
> ✅ 资源服务器怎么用

------

## 一、先给最终结论（非常重要）

> ✅ **租户 ID 必须进 JWT 声明（claims）**
>
> ✅ **不是放在 additionalParameters**
>
> ✅ **由 WechatAuthenticationProvider 在生成 token 前写入 TokenContext**
>
> ✅ **资源服务器通过 `Jwt.claims["tenant_id"]`做隔离**

------

## 二、你现在缺的关键三步（只缺这三步）

### ✅ Step 1：从 `wx_appid`拿 tenant

你已经有了：

```
String wxAppId = wxMaService.getWxMaConfig().getAppid();
```

✅ 新增：

```
Tenant tenant = tenantService.findByWxAppid(wxAppId);
if (tenant == null) {
    throw new OAuth2AuthenticationException("invalid_client", "租户不存在");
}

Long tenantId = tenant.getId();
TenantContextHolder.setTenantId(tenantId);
```

------

### ✅ Step 2：把 tenantId 放进 `UserDetails`

你的 `memberDetailsService.loadUserByOpenid(openid)`

**必须改成：**

```
UserDetails userDetails =
        memberDetailsService.loadUserByOpenid(openid, tenantId);
```

并且在返回的 `UserDetails`（比如 `MemberDetails`）里：

```
public class MemberDetails implements UserDetails {

    private Long tenantId;

    // getter / setter
}
```

------

### ✅ Step 3（最关键）：**把 tenantId 写进 JWT**

你现在是：

```
OAuth2TokenContext tokenContext =
        tokenContextBuilder
            .tokenType(OAuth2TokenType.ACCESS_TOKEN)
            .build();
```

✅ **改成这样（核心）**：

```
OAuth2TokenContext tokenContext = tokenContextBuilder
        .tokenType(OAuth2TokenType.ACCESS_TOKEN)
        .claims(claims -> {
            claims.put("tenant_id", tenantId);
            claims.put("openid", openid);
        })
        .build();
```

✅ 这一步 **直接决定资源服务器能不能识别租户**

------

## 三、你这个 Provider 的「最小侵入修改版」

### ✅ 核心片段（可直接复制）

```
// 1. 获取 wx_appid
String wxAppId = wxMaService.getWxMaConfig().getAppid();

// 2. 查租户
Tenant tenant = tenantService.findByWxAppid(wxAppId);
if (tenant == null) {
    throw new OAuth2AuthenticationException("invalid_client", "租户不存在");
}
Long tenantId = tenant.getId();
TenantContextHolder.setTenantId(tenantId);

// 3. 查用户（带 tenantId）
UserDetails userDetails =
        memberDetailsService.loadUserByOpenid(openid, tenantId);

// 4. 构建 token context（写 tenant_id）
DefaultOAuth2TokenContext.Builder tokenContextBuilder =
        DefaultOAuth2TokenContext.builder()
                .registeredClient(registeredClient)
                .principal(usernamePasswordAuthentication)
                .authorizationServerContext(AuthorizationServerContextHolder.getContext())
                .authorizationGrantType(WechatAuthenticationToken.WECHAT_MINI_APP)
                .authorizationGrant(wechatAuthenticationToken)
                .claims(claims -> {
                    claims.put("tenant_id", tenantId);
                    claims.put("openid", openid);
                });
```

✅ **就改这几处**

✅ 不影响你现有 token 生成逻辑

------

## 四、生成的 JWT 最终长什么样？

```
{
  "sub": "member_123",
  "tenant_id": 1001,
  "openid": "oUxxxxxx",
  "scope": "read write",
  "iat": 1700000000,
  "exp": 1700003600
}
```

✅ 资源服务器 **只认 `tenant_id`**

✅ 不需要 TenantContextHolder

✅ 不需要 ThreadLocal

------

## 五、资源服务器怎么用（非常关键）

### ✅ 方式一（推荐）：JWT 解析

```
Long tenantId = jwt.getClaim("tenant_id");
```

### ✅ 方式二：Spring Security 自动注入

```
@PreAuthorize("hasAuthority('order:read') and #tenantId == authentication.tenantId")
```

------

## 六、为什么不能放在 additionalParameters？

你现在的返回是：

```
return new OAuth2AccessTokenAuthenticationToken(
        registeredClient,
        clientPrincipal,
        accessToken,
        refreshToken,
        additionalParameters);
```

❌ **additionalParameters 不会进 JWT**

❌ 只影响响应 body

❌ 资源服务器拿不到

👉 **所以必须走 `.claims()`**

------

## 七、给你一句可以直接写进方案的话

> **租户 ID 必须在 WechatAuthenticationProvider 中通过 wx_appid 识别，并通过 OAuth2TokenContext.claims 写入 JWT，
>
> 资源服务器通过解析 JWT 中的 tenant_id 完成多租户数据隔离，而不是依赖 ThreadLocal 或请求参数。**

------

## 八、下一步你可以继续让我帮你

你可以直接说一句：

- ✅「帮我补 `memberDetailsService.loadUserByOpenid(openid, tenantId)`示例」
- ✅「告诉我资源服务器怎么从 JWT 里取 tenant_id 并自动注入」
- ✅「帮我检查你这个 Provider 会不会重复生成 token」
- ✅「我想让 tenant_id 同时进 refresh_token」

我可以直接按你**当前这一版 Provider 文件**给你**可复制的最终实现**。