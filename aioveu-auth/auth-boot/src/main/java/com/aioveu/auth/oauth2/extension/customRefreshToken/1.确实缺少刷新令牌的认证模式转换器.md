你说得对！我看了一下你的配置，**确实缺少刷新令牌的认证模式转换器**。

## 🔍 问题分析

你的配置中：

1. **客户端已配置了** `authorizationGrantType(AuthorizationGrantType.REFRESH_TOKEN)`
2. **但缺少刷新令牌的认证转换器**，没有 `RefreshTokenAuthenticationConverter`

## 🔧 解决方案

### 方案1：最简单的修复 - 使用Spring Security自带的刷新令牌转换器

```
@Bean
@Order(Ordered.HIGHEST_PRECEDENCE)
public SecurityFilterChain authorizationServerSecurityFilterChain(
        HttpSecurity http,
        AuthenticationManager authenticationManager,
        OAuth2AuthorizationService authorizationService,
        OAuth2TokenGenerator<?> tokenGenerator,
        SysUserDetailsService sysUserDetailsService
) throws Exception {

    // ... 前面配置不变 ...

    http.getConfigurer(OAuth2AuthorizationServerConfigurer.class)
        .tokenEndpoint(tokenEndpoint -> tokenEndpoint
            .accessTokenRequestConverters(
                authenticationConverters -> {
                    // 添加Spring Security自带的刷新令牌转换器
                    // 这行是关键的修复
                    authenticationConverters.add(new OAuth2RefreshTokenAuthenticationConverter());
                    
                    // 添加你的自定义转换器
                    authenticationConverters.addAll(
                        List.of(
                            new PasswordAuthenticationConverter(),
                            new CaptchaAuthenticationConverter(),
                            new WechatAuthenticationConverter(),
                            new SmsCodeAuthenticationConverter()
                        )
                    );
                }
            )
            .authenticationProviders(
                authenticationProviders -> {
                    // 添加Spring Security自带的刷新令牌提供者
                    // 这行是关键的修复
                    authenticationProviders.add(new OAuth2RefreshTokenAuthenticationProvider(
                        authorizationService, tokenGenerator));
                    
                    // 添加你的自定义提供者
                    authenticationProviders.addAll(
                        List.of(
                            new PasswordAuthenticationProvider(authenticationManager, authorizationService, tokenGenerator, sysUserDetailsService),
                            new CaptchaAuthenticationProvider(authenticationManager, authorizationService, tokenGenerator, redisTemplate, codeGenerator),
                            new WechatAuthenticationProvider(authorizationService, tokenGenerator, memberDetailsService, wxMaService),
                            new SmsCodeAuthenticationProvider(authorizationService, tokenGenerator, memberDetailsService, redisTemplate)
                        )
                    );
                }
            )
            .accessTokenResponseHandler(new MyAuthenticationSuccessHandler())
            .errorResponseHandler(new MyAuthenticationFailureHandler())
        );

    // ... 后面配置不变 ...
    return http.build();
}
```

### 方案2：完全的自定义刷新令牌实现

如果你需要更精细的控制，可以创建自定义的刷新令牌转换器和提供者：

```
// 1. 创建刷新令牌转换器
@Component
public class CustomRefreshTokenAuthenticationConverter implements AuthenticationConverter {
    
    @Override
    public Authentication convert(HttpServletRequest request) {
        String grantType = request.getParameter(OAuth2ParameterNames.GRANT_TYPE);
        if (!AuthorizationGrantType.REFRESH_TOKEN.getValue().equals(grantType)) {
            return null;
        }
        
        String refreshToken = request.getParameter(OAuth2ParameterNames.REFRESH_TOKEN);
        if (refreshToken == null || refreshToken.isEmpty()) {
            throw new OAuth2AuthenticationException(OAuth2ErrorCodes.INVALID_REQUEST);
        }
        
        Map<String, Object> additionalParameters = new HashMap<>();
        request.getParameterMap().forEach((key, value) -> {
            if (!key.equals(OAuth2ParameterNames.GRANT_TYPE) && 
                !key.equals(OAuth2ParameterNames.REFRESH_TOKEN)) {
                additionalParameters.put(key, value[0]);
            }
        });
        
        return new OAuth2RefreshTokenAuthenticationToken(refreshToken, null, additionalParameters);
    }
}
```

### 方案3：查看你的完整配置，确保没有遗漏

在你的配置文件中添加这些导入：

```
// 添加必要的导入
import org.springframework.security.oauth2.server.authorization.authentication.OAuth2RefreshTokenAuthenticationToken;
import org.springframework.security.oauth2.server.authorization.authentication.OAuth2RefreshTokenAuthenticationConverter;
import org.springframework.security.oauth2.server.authorization.authentication.OAuth2RefreshTokenAuthenticationProvider;
```

## 🎯 关键修复位置

在 `tokenEndpoint`配置中添加这两行：

```
.tokenEndpoint(tokenEndpoint -> tokenEndpoint
    .accessTokenRequestConverters(authenticationConverters -> {
        // ✅ 添加刷新令牌转换器
        authenticationConverters.add(new OAuth2RefreshTokenAuthenticationConverter());
        
        // 你的其他转换器...
    })
    .authenticationProviders(authenticationProviders -> {
        // ✅ 添加刷新令牌提供者
        authenticationProviders.add(new OAuth2RefreshTokenAuthenticationProvider(
            authorizationService, tokenGenerator));
        
        // 你的其他提供者...
    })
)
```

## 📋 完整的修复配置

```
@Bean
@Order(Ordered.HIGHEST_PRECEDENCE)
public SecurityFilterChain authorizationServerSecurityFilterChain(
        HttpSecurity http,
        AuthenticationManager authenticationManager,
        OAuth2AuthorizationService authorizationService,
        OAuth2TokenGenerator<?> tokenGenerator,
        SysUserDetailsService sysUserDetailsService
) throws Exception {

    log.info("在应用默认安全配置前添加验证码过滤器");
    http.addFilterBefore(
        captchaValidationFilter(),
        BasicAuthenticationFilter.class
    );

    OAuth2AuthorizationServerConfiguration.applyDefaultSecurity(http);

    http.getConfigurer(OAuth2AuthorizationServerConfigurer.class)
        .tokenEndpoint(tokenEndpoint -> tokenEndpoint
            .accessTokenRequestConverters(
                authenticationConverters -> {
                    // ✅ 关键修复1：添加刷新令牌转换器
                    authenticationConverters.add(new OAuth2RefreshTokenAuthenticationConverter());
                    
                    // 你的自定义转换器
                    authenticationConverters.addAll(
                        List.of(
                            new PasswordAuthenticationConverter(),
                            new CaptchaAuthenticationConverter(),
                            new WechatAuthenticationConverter(),
                            new SmsCodeAuthenticationConverter()
                        )
                    );
                }
            )
            .authenticationProviders(
                authenticationProviders -> {
                    // ✅ 关键修复2：添加刷新令牌提供者
                    authenticationProviders.add(new OAuth2RefreshTokenAuthenticationProvider(
                        authorizationService, tokenGenerator));
                    
                    // 你的自定义提供者
                    authenticationProviders.addAll(
                        List.of(
                            new PasswordAuthenticationProvider(authenticationManager, authorizationService, tokenGenerator, sysUserDetailsService),
                            new CaptchaAuthenticationProvider(authenticationManager, authorizationService, tokenGenerator, redisTemplate, codeGenerator),
                            new WechatAuthenticationProvider(authorizationService, tokenGenerator, memberDetailsService, wxMaService),
                            new SmsCodeAuthenticationProvider(authorizationService, tokenGenerator, memberDetailsService, redisTemplate)
                        )
                    );
                }
            )
            .accessTokenResponseHandler(new MyAuthenticationSuccessHandler())
            .errorResponseHandler(new MyAuthenticationFailureHandler())
        )
        .oidc(oidcCustomizer ->
            oidcCustomizer.userInfoEndpoint(userInfoEndpointCustomizer -> {
                userInfoEndpointCustomizer.userInfoRequestConverter(new CustomOidcAuthenticationConverter(customOidcUserInfoService));
                userInfoEndpointCustomizer.authenticationProvider(new CustomOidcAuthenticationProvider(authorizationService));
            })
        );

    http.oauth2ResourceServer(oauth2ResourceServer ->
        oauth2ResourceServer.jwt(Customizer.withDefaults()));

    return http.build();
}
```

## 🔄 测试刷新令牌

修复后，测试刷新令牌：

```
// 前端测试
refreshToken(refreshToken: string): Promise<LoginResult> {
  const clientId = getClientId() || CLIENT_CONFIG.CLIENT_ID;
  const basicAuth = CLIENT_CONFIG.getBASIC_AUTH();
  
  // 构建表单数据
  const formData = buildFormData({
    grant_type: "refresh_token",      // ✅
    refresh_token: refreshToken,      // ✅
    client_id: clientId,
  });
  
  return request<LoginResult>({
    url: `${AUTH_LOGIN_URL}/oauth2/token`,
    method: "POST",
    data: formData,
    header: {
      "Content-Type": "application/x-www-form-urlencoded",
      "Authorization": basicAuth,
      skipAuth: true,
    },
  });
}
```

## 🎯 总结

**问题根源**：你配置了客户端支持 `REFRESH_TOKEN`授权类型，但没有配置对应的认证转换器和提供者。

**解决方案**：

1. 添加 `OAuth2RefreshTokenAuthenticationConverter`到转换器列表
2. 添加 `OAuth2RefreshTokenAuthenticationProvider`到提供者列表
3. 确保客户端已配置 `REFRESH_TOKEN`授权类型（你已配置）

这样修复后，刷新令牌功能就能正常工作了。