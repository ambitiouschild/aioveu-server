问题**一眼就能定位**，而且这是一个 **Spring Authorization Server 里非常经典、也非常隐蔽的坑**。

我直接给你结论，再逐行指出你这段代码里**真正致命的地方**。

------

## ✅ 一句话结论（重点）

> **第 175 行左右这个强转是错的：**
>
> ```
> Authentication userAuthentication =
>     authorization.getAttribute(Principal.class.getName());
> ```
>
> **`authorization.getAttribute()`返回的是 `String`（principalName），不是 `Authentication`。**

这就是为什么你看到：

```
java.lang.ClassCastException:
String cannot be cast to org.springframework.security.core.Authentication
```

------

## 二、你这个 Provider 的「核心错误点」

### ❌ 错误代码（你现在的）

```
// 6. 获取用户认证对象（Authentication），不是 Principal
Authentication userAuthentication =
        authorization.getAttribute(Principal.class.getName()); // ❌ 实际是 String
```

### ✅ 真相（Spring Authorization Server 的设计）

`OAuth2Authorization`里存的是：

| Key                         | 实际类型                    |
| --------------------------- | --------------------------- |
| `Principal.class.getName()` | **String（principalName）** |
| `Authorization`             | `OAuth2Authorization`       |
| `Authentication`            | ❌ **不会序列化保存**        |

👉 **Token 是持久化的，Authentication 是运行时对象**

------

## 三、你这个 Provider 的「逻辑模型错误」

你现在在做的是：

```
RefreshToken
   ↓
authorization.getAttribute(Principal.class)
   ↓
强转为 Authentication ❌
```

✅ **正确模型应该是：**

```
RefreshToken
   ↓
authorization.getPrincipalName()  ✅ String
   ↓
loadUserByUsername / loadUserByOpenId ✅
   ↓
new UsernamePasswordAuthenticationToken ✅
```

------

## 四、你这段代码里还有 **3 个隐藏炸弹**

### 💣 炸弹 1：你用了错误的 principal 构建 TokenContext

```
DefaultOAuth2TokenContext.Builder tokenContextBuilder = DefaultOAuth2TokenContext.builder()
        .principal(userAuthentication) // ❌ userAuthentication 是 String 强转来的
```

👉 实际上这里 **根本不应该用 userAuthentication**

👉 而应该用 **重新构建的 Authentication**

------

### 💣 炸弹 2：你保存 Authorization 时又塞回了错误的 Authentication

```
.authorizationGrantType(AuthorizationGrantType.REFRESH_TOKEN)
.attribute(Principal.class.getName(), userAuthentication); // ❌
```

👉 这会导致下一次刷新直接炸

------

### 💣 炸弹 3：你完全没用到 `MemberDetailsService`

你前面微信登录、Feign 401、openId = null……

**这套 RefreshToken Provider 完全没有和用户体系打通**

------

## 五、✅ 正确的修复方式（核心代码）

### ✅ 1️⃣ 从 Authorization 取 principalName（String）

```
String principalName = authorization.getPrincipalName();
```

------

### ✅ 2️⃣ 重新加载用户（关键）

```
@Autowired
private MemberDetailsService memberDetailsService;

UserDetails userDetails =
        memberDetailsService.loadUserByUsername(principalName);
```

> ⚠️ 如果你的 principalName 是 openId，那你要保证：
>
> `loadUserByUsername()`内部支持按 openId 查用户

------

### ✅ 3️⃣ 构建新的 Authentication（必须）

```
UsernamePasswordAuthenticationToken newAuthentication =
        new UsernamePasswordAuthenticationToken(
                userDetails,
                null,
                userDetails.getAuthorities()
        );
```

------

### ✅ 4️⃣ TokenContext 使用新的 Authentication

```
DefaultOAuth2TokenContext.Builder tokenContextBuilder =
        DefaultOAuth2TokenContext.builder()
                .registeredClient(registeredClient)
                .principal(newAuthentication) // ✅ 正确
                .authorizationServerContext(AuthorizationServerContextHolder.getContext())
                .authorizationGrantType(AuthorizationGrantType.REFRESH_TOKEN)
                .authorizationGrant(refreshTokenAuthentication);
```

------

### ✅ 5️⃣ 保存 Authorization 时也用新的 Authentication

```
OAuth2Authorization.Builder authorizationBuilder =
        OAuth2Authorization.withRegisteredClient(registeredClient)
                .principalName(principalName)
                .authorizationGrantType(AuthorizationGrantType.REFRESH_TOKEN)
                .attribute(Principal.class.getName(), newAuthentication); // ✅
```

------

## 六、修正后的「关键流程对比」

### ❌ 你现在

```
RefreshToken
→ getAttribute(Principal)
→ 强转 Authentication ❌
→ 直接拿去生成 Token ❌
```

### ✅ 正确做法

```
RefreshToken
→ getPrincipalName() ✅
→ loadUserByOpenId / username ✅
→ new Authentication ✅
→ 生成 Token ✅
```

------

## 七、为什么你“微信登录成功，刷新就炸”？

因为：

1. 

   微信登录时：

   ```
   principalName = openId
   ```

2. 

   RefreshToken 里只存了 `openId`

3. 

   刷新时你试图把 `openId`当 `Authentication`

4. 

   **ClassCastException**

✅ **完全符合 Spring Authorization Server 行为**

------

## 八、你现在这整个 Provider 的“定位问题”

你这个 Provider **看起来像 Password / Wechat Provider**，但实际上：

- 

  ❌ 没有微信 code

- 

  ❌ 没有 wxMaService

- 

  ❌ 没有 openId 解析

- 

  ✅ 只是一个 **RefreshToken Provider**

👉 **RefreshToken Provider 的职责只有一个：**

> **用 principalName 重新构建 Authentication**

------

## 九、给你一个「最小修改版」（直接可用）

### 替换你第 6 步之后的代码（核心）

```
// 6. 获取 principalName（不是 Authentication）
String principalName = authorization.getPrincipalName();

// 7. 重新加载用户
UserDetails userDetails =
        memberDetailsService.loadUserByUsername(principalName);

// 8. 构建新的 Authentication
Authentication newAuthentication =
        new UsernamePasswordAuthenticationToken(
                userDetails,
                null,
                userDetails.getAuthorities()
        );

log.info("刷新令牌验证通过, 用户: {}, 客户端: {}",
        principalName, registeredClient.getClientId());
```

后面所有用到 `userAuthentication`的地方，**全部换成 `newAuthentication`**。

------

## 十、一句话总结（记住这句）

> **RefreshToken 里永远没有 Authentication，只有 principalName。
>
> 刷新令牌的本质是：用 principalName 重新查用户，再 new 一个 Authentication。**

------

如果你愿意，下一步我可以帮你：

- 

  ✅ 把你这个 `CustomRefreshTokenAuthenticationProvider`**精简成 80 行标准实现**

- 

  ✅ 帮你对齐 **微信登录 / RefreshToken / JWT subject** 的统一规范

- 

  ✅ 帮你验证 `principalName`到底是 `openId`还是 `userId`

你把 `MemberDetailsService.loadUserByUsername()`的实现贴出来，我顺便帮你把 **openId 查询**这块一次修到位。