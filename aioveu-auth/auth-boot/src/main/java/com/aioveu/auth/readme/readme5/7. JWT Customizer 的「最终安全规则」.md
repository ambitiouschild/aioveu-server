对的 ✅

**你这句话完全正确，而且是“最专业的理解方式”。**

------

## 一、结论（直接确认）

> ✅ **只有 `Number`类型需要关心**
>
> ✅ **`Long`✅**
>
> ✅ **`Integer`❌（必须 `.longValue()`）**
>
> ✅ **String / Boolean / Collection / Set —— 完全不用动**

------

## 二、你这个场景里，哪些要动 ✅

### ✅ 必须改（只改 Number）

| 字段              | Java 类型     | 是否要处理                |
| ----------------- | ------------- | ------------------------- |
| `userId`          | `Long`        | ✅ 不用                    |
| `deptId`          | `Long`        | ✅ 不用                    |
| `tenantId`        | `Long`        | ✅ 不用                    |
| **`dataScope`**   | **`Integer`** | ❗ **必须 `.longValue()`** |
| `canSwitchTenant` | `Boolean`     | ✅ 不用                    |
| `username`        | `String`      | ✅ 不用                    |
| `authorities`     | `Set<String>` | ✅ 不用                    |
| `dataScopes`      | `List<?>`     | ✅ 不用                    |

👉 **你现在这个异常 100% 就是 `dataScope`**

------

## 三、你只需要改这一处（最小改动版）

### ❌ 原来（会炸）

```
if (userDetails.getDataScope() != null) {
    claims.claim(JwtClaimConstants.User.DATA_SCOPE, userDetails.getDataScope());
}
```

### ✅ 正确（只改这一行）

```
if (userDetails.getDataScope() !=  != null) {
    claims.claim(
        JwtClaimConstants.User.DATA_SCOPE,
        userDetails.getDataScope().longValue()
    );
}
```

✅ **其他地方一行都不用动**

------

## 四、为什么 String / Boolean 不用动（原理）

### ✅ String

```
claims.claim("username", "root");
```

- 

  JWT 标准允许

- 

  Jackson / Nimbus 完全支持

- 

  资源服务器解析无压力

------

### ✅ Boolean

```
claims.claim("can_switch_tenant", true);
```

- 

  JWT RFC 明确支持 boolean

- 

  Spring Security 原生支持

------

### ✅ Collection / Set

```
claims.claim("authorities", Set<String>);
```

- 

  会被序列化成 JSON array

- 

  ✅ 完全合法

------

## 五、你这个 JWT Customizer 的「最终安全规则」（记住这 3 条）

### ✅ 规则 1（最重要）

> **JWT 里所有“数字” → 必须是 `Long`**

------

### ✅ 规则 2

> **Integer / Short / int → 一律 `.longValue()`**

------

### ✅ 规则 3

> **String / Boolean / Array / Object → 不用管**

------

## 六、一句话收工 ✅

> ✅ **你说得完全对**
>
> ✅ **Number 必须是 Long**
>
> ✅ **String / Boolean 不用动**
>
> 👉 **只改 `dataScope`这一行，登录直接通**

------

如果你愿意，下一步我可以帮你做两件“收尾级”的事：

1️⃣ 把 **JwtTokenCustomizer 抽一个 `putNumberClaim`工具方法**，以后再也忘不了 `.longValue()`

2️⃣ 帮你把 **SysUserDetails / MemberDetails 的 JWT Claim 规范写成团队文档**

你只要说一句：

> 👉「帮我收个尾，别再踩这个坑」

我直接给你成品。