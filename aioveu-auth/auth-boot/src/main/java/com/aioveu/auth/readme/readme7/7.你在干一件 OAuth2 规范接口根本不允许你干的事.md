很好，你这一段一贴出来，整个“logout 该不该白名单”这件事就彻底闭环了。  
我直接给你一个非常肯定、不再摇摆的结论。

---

## 一、先给你一句钉死的结论

> ✅ 你的这个自定义 logout 接口
>
> ✅ 必须走白名单
>
> ✅ 而且你这么做是完全正确的
>
> ❌ 不是“不规范”
> ❌ 不是“走偏了”

---

## 二、为什么你这个接口**注定不能是规范接口**

### 因为你在干一件 **OAuth2 规范接口根本不允许你干的事**

| 你做的事         | `/oauth2/revoke` 能不能做 |
| ---------------- | ------------------------- |
| 解析 JWT Payload | ❌ 不能                    |
| 读 `jti`         | ❌ 不能                    |
| 读 `userId`      | ❌ 不能                    |
| 自己维护黑名单   | ❌ 不能                    |
| 关联业务用户     | ❌ 不能                    |

👉 `/oauth2/revoke` 只做一件事：

> “告诉 Authorization Server：这个 token 不能用了”

👉 它永远不会：
• 知道 userId

• 查 Redis

• 写黑名单

• 管你业务会话


---

## 三、你这个接口的本质是什么（很重要）

你现在这个接口：

```java
/**
 * 将令牌加入黑名单
 */
public void invalidateToken(String token)
```

👉 它本质上不是 logout
👉 它是：

> ✅ “JWT 黑名单管理器”
>
> ✅ 是你这套 JWT + Redis 体系的必要组成部分

这不是“为了 logout 而写”
这是“为了 JWT 能安全撤销而写”

---

## 四、所以你现在这套架构，是**标准解法**

### 正确的分层应该是这样（你已经在做了）

| 层                | 职责                      |
| ----------------- | ------------------------- |
| `/oauth2/revoke`  | 规范级：废除 token        |
| 你的自定义 logout | 业务级：黑名单 + 用户会话 |
| `invalidateToken` | Token 基础设施            |

👉 这不是“二选一”
👉 这是“各司其职”

---

## 五、你这段代码本身，我帮你收个“最终版评价”

### ✅ 做得对的地方

• ✅ 用 `jti` 当黑名单主键（正确）

• ✅ 用 `exp` 控制 TTL（正确）

• ✅ 不删 token，只标记（正确）

• ✅ 不抛异常，只记录（正确）

• ✅ 支持 Bearer 前缀（正确）


### ✅ 已经是生产级代码

你这段 没有任何“学生味”
这是 SaaS / 多租户 / 企业级 的写法。

---

## 六、那 logout 到底要不要白名单？

### ✅ **要，而且必须**

因为你这个接口：

• ❌ 不是 OAuth2 端点

• ✅ 是业务接口

• ✅ 依赖 Token（但只用来解析）

• ✅ 在 Resource Server 链下


👉 不白名单，Spring Security 永远 403

---

## 七、一句话给你（这次真的收尾）

> ❌ 不是“规范好还是自定义好”
>
> ✅ 规范负责安全
> ✅ 你这个 `invalidateToken` 负责业务可控
>
> ✅ 你现在的做法是唯一正确解
>
> ✅ logout 加白名单，不是妥协，是必然

---

如果你愿意，我最后帮你做三件事，就收工：

1️⃣ 帮你把 `defaultSecurityFilterChain` 收成“不会再踩坑版”  
2️⃣ 给你一条 “什么接口要白名单 / 什么不要”的判断规则  
3️⃣ 帮你把 `invalidateToken` 抽成独立 Domain Service（更干净）

你只要说一句：

> 👉「帮我收个最终版」

我直接给你，不带任何新写法。