太好了，你把真实的数据库内容贴出来，这个问题就彻底清楚了 👍  
你现在看到的，是 Spring Authorization Server 官方 `clientSettings` 的标准 JSON。

---

## ✅ 一句话结论（先给你）

> ❌ 你这个 JSON 里没有 `enabled`
>
> ✅ Spring 官方 `clientSettings` 默认也不包含 `enabled`
>
> ✅ 启用 / 禁用 OAuth2 Client 的正确方式是：`client_id + 是否存在`

---

## 一、你现在看到的 JSON 是什么？

```json
{
  "@class": "java.util.Collections$UnmodifiableMap",
  "settings.client.require-proof-key": false,
  "settings.client.require-authorization-consent": true
}
```

✅ 这是 Spring OAuth2 内部序列化格式
✅ 不是你设计的
✅ 不可直接加字段

---

## 二、❌ 为什么不能从这里面判断 enabled？

| 原因       | 说明         |
| ---------- | ------------ |
| JSON 固定  | Spring 控制  |
| 无 enabled | 官方设计如此 |
| 改它会炸   | 不兼容升级   |

👉 所以你之前“从 clientSettings 读 enabled”这条路是走不通的

---

## 三、✅ Spring 官方“启用 / 禁用 Client”的正确方式

### ✅ 方式一（✅ 最推荐）：**删除 / 不存 = 禁用**

| 状态   | 行为   |
| ------ | ------ |
| 有记录 | ✅ 启用 |
| 没记录 | ❌ 禁用 |

✅ 你现在已经在用了

---

### ✅ 方式二（进阶）：**加独立字段（你最该用）**

```sql
ALTER TABLE oauth2_registered_client
ADD COLUMN enabled TINYINT DEFAULT 1;
```

```java
private Integer enabled;
```

```java
if (client.getEnabled() == null || client.getEnabled() != 1) {
    return false;
}
```

✅ 最清晰
✅ 最安全
✅ 最可运维

---

## 四、✅ 修正后的 `checkFromDb`（✅ 正确版）

```java
private boolean checkFromDb(String clientId) {

    if (StringUtils.isBlank(clientId)) {
        return false;
    }

    if (!clientId.matches("^[A-Za-z0-9_-]{3,64}$")) {
        return false;
    }

    Oauth2RegisteredClient client =
            oauth2RegisteredClientMapper.selectOne(
                    new LambdaQueryWrapper<Oauth2RegisteredClient>()
                            .eq(Oauth2RegisteredClient::getClientId, clientId)
            );

    if (client == null) {
        return false;
    }

    // ✅ 正确方式
    return client.getEnabled() != null
            && client.getEnabled() == 1;
}
```

---

## 五、✅ 架构师级总结（你现在的位置）

| 层级          | 结论                        |
| ------------- | --------------------------- |
| Spring OAuth2 | ✅ clientSettings 无 enabled |
| 白名单        | ✅ 必须自己加 enabled        |
| DB            | ✅ 唯一信任源                |
| Redis         | ✅ 防高频                    |

---

## ✅ 最后一句话

> 你现在这个 JSON，是 Spring 的“内部实现细节”，不是你的业务字段。

如果你愿意，下一步我可以帮你：
1️⃣ 把 OAuth2 Client 启用 / 禁用做成管理平台
2️⃣ 设计 clientId 白名单 + OAuth2 生命周期
3️⃣ 帮你画 「公共接口 → OAuth2 → 微服务」安全图

你直接选 👍