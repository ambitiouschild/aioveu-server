# 🔧 添加 MemberDetails Mixin 完整流程

## 📋 步骤概览

1. 创建 `MemberDetailsMixin`类
2. 在配置中添加 Mixin
3. 验证代码是否正确存储 Authentication
4. 清理旧数据
5. 重启测试

------

## 🔧 步骤1：创建 MemberDetailsMixin 类

### 位置：`src/main/java/com/aioveu/auth/oauth2/jackson/MemberDetailsMixin.java`

```
package com.aioveu.auth.oauth2.jackson;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import org.springframework.security.core.GrantedAuthority;

import java.util.Collection;
import java.util.List;

/**
 * MemberDetails 的 Jackson Mixin
 * 解决 Spring Security 6+ 反序列化安全限制
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.CLASS)
@JsonIgnoreProperties(ignoreUnknown = true)
public abstract class MemberDetailsMixin {
    
    @JsonCreator
    public MemberDetailsMixin(
            @JsonProperty("id") Long id,
            @JsonProperty("tenantId") Long tenantId,
            @JsonProperty("username") String username,
            @JsonProperty("password") String password,
            @JsonProperty("nickName") String nickName,
            @JsonProperty("mobile") String mobile,
            @JsonProperty("avatarUrl") String avatarUrl,
            @JsonProperty("openid") String openid,
            @JsonProperty("unionid") String unionid,
            @JsonProperty("authorities") List<? extends GrantedAuthority> authorities,
            @JsonProperty("enabled") boolean enabled,
            @JsonProperty("accountNonExpired") boolean accountNonExpired,
            @JsonProperty("credentialsNonExpired") boolean credentialsNonExpired,
            @JsonProperty("accountNonLocked") boolean accountNonLocked) {
    }
}
```

**如果 `MemberDetails`没有 unionid 字段，可以删除相关行。**

------

## 🔧 步骤2：在配置中添加 Mixin

### 修改 `AuthorizationServerConfig.java`

找到 `authorizationService`方法，添加 Mixin：

```
@Bean
public OAuth2AuthorizationService authorizationService(JdbcTemplate jdbcTemplate,
                                                       RegisteredClientRepository registeredClientRepository) {

    JdbcOAuth2AuthorizationService service =
            new JdbcOAuth2AuthorizationService(jdbcTemplate, registeredClientRepository);

    JdbcOAuth2AuthorizationService.OAuth2AuthorizationRowMapper rowMapper = 
            new JdbcOAuth2AuthorizationService.OAuth2AuthorizationRowMapper(registeredClientRepository);
    rowMapper.setLobHandler(new DefaultLobHandler());

    ObjectMapper objectMapper = new ObjectMapper();
    ClassLoader classLoader = JdbcOAuth2AuthorizationService.class.getClassLoader();
    List<Module> securityModules = SecurityJackson2Modules.getModules(classLoader);
    objectMapper.registerModules(securityModules);
    objectMapper.registerModule(new OAuth2AuthorizationServerJackson2Module());

    // ✅ 关键：添加 MemberDetails 的 Mixin
    objectMapper.addMixIn(
        com.aioveu.auth.model.MemberDetails.class,  // 你的 MemberDetails 类
        com.aioveu.auth.oauth2.jackson.MemberDetailsMixin.class  // Mixin 类
    );
    
    // 已有的 Mixin
    objectMapper.addMixIn(SysUserDetails.class, SysUserMixin.class);
    objectMapper.addMixIn(Long.class, Object.class);

    rowMapper.setObjectMapper(objectMapper);
    service.setAuthorizationRowMapper(rowMapper);

    return service;
}
```

**确保导入正确的类：**

```
import com.aioveu.auth.oauth2.jackson.MemberDetailsMixin;
```

------

## 🔧 步骤3：验证 Authentication 存储正确

### 检查 `WechatAuthenticationProvider.java`

确保存储的是完整的 `Authentication`对象：

```
// 第 157-160 行，应该是这样的：
UsernamePasswordAuthenticationToken usernamePasswordAuthentication = new UsernamePasswordAuthenticationToken(
    userDetails,  // ✅ 完整的 MemberDetails 对象
    userDetails.getPassword(),  // ✅ 密码
    userDetails.getAuthorities()  // ✅ 权限
);

// 第 212-214 行，应该是这样的：
OAuth2Authorization.Builder authorizationBuilder = OAuth2Authorization.withRegisteredClient(registeredClient)
    .principalName(userDetails.getUsername())  // 主体名称
    .authorizationGrantType(WechatAuthenticationToken.WECHAT_MINI_APP)  // 授权类型
    .attribute(Principal.class.getName(), usernamePasswordAuthentication);  // ✅ 存储完整的 Authentication
```

**重要检查点：**

1. 第 158 行：`userDetails`不是 `userDetails.getUsername()`
2. 第 214 行：`usernamePasswordAuthentication`不是 `userDetails.getUsername()`

------

## 🔧 步骤4：清理旧数据（可选但推荐）

旧的授权记录中存储的是错误的格式，需要清理：

### 方法1：SQL 清理

```
-- 清理所有旧的授权记录
DELETE FROM oauth2_authorization;
```

### 方法2：让所有用户重新登录

重启应用后，让测试用户重新微信登录，生成新的令牌。

------

## 🔧 步骤5：重启并测试

### 重启应用

```
# 如果你用 Maven
mvn clean spring-boot:run

# 如果你用 Gradle
./gradlew bootRun
```

### 测试流程

1. **微信登录**获取新的 access_token 和 refresh_token

2. **测试刷新令牌**：

   ```
   // 前端测试代码
   const testRefresh = async () => {
     const refreshToken = uni.getStorageSync('refresh_token');
     const formData = `grant_type=refresh_token&refresh_token=${refreshToken}&client_id=mall-app`;
   
     const res = await uni.request({
       url: 'http://localhost:6699/aioveu-tenant-auth/oauth2/token',
       method: 'POST',
       header: {
         'Content-Type': 'application/x-www-form-urlencoded',
         'Authorization': 'Basic bWFsbC1hcHA6MTIzNDU2'
       },
       data: formData
     });
   
     console.log('刷新结果:', res.data);
   };
   ```

------

## 🎯 验证日志

成功时应该看到：

```
# 1. 登录成功
生成新的访问令牌, 过期时间: ...

# 2. 刷新令牌成功
刷新令牌验证通过, 用户: xxx, 客户端: mall-app
生成新的访问令牌, 过期时间: ...
```

------

## 🔍 常见问题排查

### 问题1：还是报 ClassCastException

**原因**：可能旧的代码还在存储 String

**解决**：确保第 214 行是 `usernamePasswordAuthentication`

### 问题2：反序列化错误依然存在

**原因**：Mixin 没生效

**解决**：

1. 检查 Mixin 路径是否正确
2. 重启应用
3. 清理旧数据

### 问题3：其他自定义类也需要 Mixin

如果还有其他自定义类存储在 Authentication 中，也需要添加 Mixin。

------

## 📊 验证数据库存储

登录后，检查数据库中的存储：

```
-- 查看存储的 attributes 格式
SELECT 
    id,
    principal_name,
    JSON_EXTRACT(attributes, '$.principal.@class') as principal_class
FROM oauth2_authorization
WHERE principal_name IS NOT NULL;
```

应该看到：

```
principal_class: "com.aioveu.auth.model.MemberDetails"
```

------

## 🎯 完成标志

1. ✅ 微信登录成功
2. ✅ 获取到 access_token 和 refresh_token
3. ✅ 使用 refresh_token 成功获取新的 access_token
4. ✅ 新令牌包含完整的用户信息

------

## 📋 如果还有问题

提供以下信息：

1. 修改后的 `WechatAuthenticationProvider`相关代码截图
2. 重启后的错误日志
3. 测试刷新令牌的请求和响应

先按这个完整流程操作，应该能解决问题！