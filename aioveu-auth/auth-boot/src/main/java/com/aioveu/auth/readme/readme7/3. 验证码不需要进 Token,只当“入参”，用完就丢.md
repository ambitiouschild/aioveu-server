好，这个问题非常关键，你现在这个类是SAS 标准写法，我直接告诉你“怎么加才不烂”，而且完全适配你刚刚那套 Provider 校验验证码的新设计。

---

## 一、先说结论（你照着抄就行）

> ✅ 验证码不需要进 Token
> ✅ 但必须进 `PasswordAuthenticationToken`
> ✅ 只当“入参”，用完就丢

所以：

• ❌ 不要加进 `scopes`

• ❌ 不要加进 `additionalParameters`（那是给 OAuth2 协议用的）

• ✅ 单独加字段


---

## 二、✅ 正确改法（推荐方案）

### 1️⃣ 给 `PasswordAuthenticationToken` 加字段

```java
public class PasswordAuthenticationToken extends OAuth2AuthorizationGrantAuthenticationToken {

    public static final AuthorizationGrantType PASSWORD = new AuthorizationGrantType("password");

    private final Set<String> scopes;

    /** ✅ 新增：验证码Key */
    private final String captchaKey;

    /** ✅ 新增：验证码 */
    private final String captchaCode;

    public PasswordAuthenticationToken(
            Authentication clientPrincipal,
            Set<String> scopes,
            String captchaKey,
            String captchaCode,
            Map<String, Object> additionalParameters
    ) {
        super(PASSWORD, clientPrincipal, additionalParameters);

        this.scopes = Collections.unmodifiableSet(
                scopes != null ? new HashSet<>(scopes) : Collections.emptySet()
        );
        this.captchaKey = captchaKey;
        this.captchaCode = captchaCode;
    }

    public String getCaptchaKey() {
        return captchaKey;
    }

    public String getCaptchaCode() {
        return captchaCode;
    }

    @Override
    public Object getCredentials() {
        return this.getAdditionalParameters().get(OAuth2ParameterNames.PASSWORD);
    }

    public Set<String> getScopes() {
        return scopes;
    }
}
```

✅ 不污染 OAuth2 协议参数
✅ Provider 里直接 get
✅ JWT 里完全看不到

---

## 三、Converter 里怎么塞（✅ 关键）

### `PasswordAuthenticationConverter`

```java
@Override
public Authentication convert(HttpServletRequest request) {

    String username = request.getParameter(OAuth2ParameterNames.USERNAME);
    String password = request.getParameter(OAuth2ParameterNames.PASSWORD);

    String captchaKey = request.getParameter("captchaKey");
    String captchaCode = request.getParameter("captchaCode");

    Authentication clientPrincipal =
            SecurityContextHolder.getContext().getAuthentication();

    Map<String, Object> additionalParameters = new HashMap<>();
    additionalParameters.put(OAuth2ParameterNames.USERNAME, username);
    additionalParameters.put(OAuth2ParameterNames.PASSWORD, password);

    return new PasswordAuthenticationToken(
            clientPrincipal,
            Collections.emptySet(),
            captchaKey,
            captchaCode,
            additionalParameters
    );
}
```

---

## 四、Provider 里怎么用（✅ 你刚才那版）

```java
@Override
public Authentication authenticate(Authentication authentication) {

    PasswordAuthenticationToken token =
            (PasswordAuthenticationToken) authentication;

    // ✅ 验证码校验
    captchaValidator.validate(
            token.getCaptchaKey(),
            token.getCaptchaCode()
    );

    // ✅ 用户名密码校验
}
```

---

## 五、为什么**不要**放 `additionalParameters`

| 原因        | 说明                                    |
| ----------- | --------------------------------------- |
| OAuth2 规范 | `additionalParameters` 是给协议扩展用的 |
| SAS 内部    | 可能会被打印 / 记录                     |
| 安全        | 验证码不应该出现在协议层                |
| 可维护性    | 单独字段语义更清晰                      |

👉 验证码不是 OAuth2 参数
👉 它是你业务的“一次性入参”

---

## 六、最终一句话钉死

> ✅ 验证码加在 `PasswordAuthenticationToken` 里
> ✅ 用单独字段，不用 additionalParameters
> ✅ Provider 校验完就结束
> ✅ 绝不进 Token

---

如果你愿意，下一步我可以直接帮你把：

• ✅ `PasswordAuthenticationConverter`

• ✅ `PasswordAuthenticationProvider`

• ✅ `CaptchaValidator`


这三件套收成最终版（可上线、不踩坑），你一句话就行：

> 👉「给我最终版，这次别再玩我了」