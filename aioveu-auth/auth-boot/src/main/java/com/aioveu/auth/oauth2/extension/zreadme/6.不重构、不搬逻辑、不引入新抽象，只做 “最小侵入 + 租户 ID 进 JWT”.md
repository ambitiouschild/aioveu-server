好，这一步我们就**只在你这一份 `WechatAuthenticationProvider`上改**，

**不重构、不搬逻辑、不引入新抽象**，只做 **“最小侵入 + 租户 ID 进 JWT”**。

我会给你：

1️⃣ **最终结论（要不要 tenant_id）**

2️⃣ **你这个类需要新增的依赖**

3️⃣ **精确插入点（标清行号级）**

4️⃣ **最终 JWT 里长什么样**

5️⃣ **资源服务器怎么用**

------

# ✅ 一、结论（先给你定心丸）

> ✅ **租户 ID 必须进 JWT**
>
> ✅ **来源 = `wx_appid → sys_tenant`**
>
> ✅ **放入位置 = `OAuth2TokenContext.claims()`**
>
> ✅ **不是 additionalParameters**
>
> ✅ **你这个 Provider 是唯一正确的地方**

------

# ✅ 二、你这个类需要新增的依赖（只加 2 个）

```
import com.aioveu.common.tenant.TenantContextHolder;
import com.aioveu.tenant.api.TenantFeignClient; // 或本地 service
import com.aioveu.tenant.dto.TenantDTO;
```

并在构造器中注入：

```
private final TenantFeignClient tenantFeignClient;

public WechatAuthenticationProvider(
        OAuth2AuthorizationService authorizationService,
        OAuth2TokenGenerator<? extends OAuth2Token> tokenGenerator,
        MemberDetailsService memberDetailsService,
        WxMaService wxMaService,
        TenantFeignClient tenantFeignClient
) {
    Assert.notNull(authorizationService, "authorizationService cannot be null");
    Assert.notNull(tokenGenerator, "tokenGenerator cannot be null");
    Assert.notNull(memberDetailsService, "memberDetailsService cannot be null");
    Assert.notNull(wxMaService, "wxMaService cannot be null");
    Assert.notNull(tenantFeignClient, "tenantFeignClient cannot be null");

    this.authorizationService = authorizationService;
    this.tokenGenerator = tokenGenerator;
    this.memberDetailsService = memberDetailsService;
    this.wxMaService = wxMaService;
    this.tenantFeignClient = tenantFeignClient;
}
```

------

# ✅ 三、在你这个 `authenticate()`里「精确加 4 步」

## ✅ Step 1：在 **code2Session 之后** 加（关键）

### 📍 位置（你原有代码）：

```
String openid = sessionInfo.getOpenid();
log.info("OpenID获取：从微信响应中提取用户唯一标识openid:{}",openid);
```

### ✅ 插入下面代码（**直接粘**）：

```
// ===== 租户识别（核心）=====
String wxAppId = wxMaService.getWxMaConfig().getAppid();
log.info("根据 wx_appid 查询租户: {}", wxAppId);

TenantDTO tenant = tenantFeignClient.getByWxAppId(wxAppId);
if (tenant == null) {
    throw new OAuth2AuthenticationException("invalid_client", "租户不存在");
}

Long tenantId = tenant.getId();
TenantContextHolder.setTenantId(tenantId);
```

------

## ✅ Step 2：修改 `loadUserByOpenid`（必须）

### 📍 原来：

```
UserDetails userDetails = memberDetailsService.loadUserByOpenid(openid);
```

### ✅ 改成：

```
UserDetails userDetails =
        memberDetailsService.loadUserByOpenid(openid, tenantId);
```

⚠️ 如果你现在 **还没改这个方法签名**，那是**下一个必做项**，但我这里先按你已支持来写。

------

## ✅ Step 3：在「构建 tokenContext 之前」加 claims

### 📍 原来：

```
DefaultOAuth2TokenContext.Builder tokenContextBuilder =
        DefaultOAuth2TokenContext.builder()
            .registeredClient(registeredClient)
            .principal(usernamePasswordAuthentication)
            .authorizationServerContext(AuthorizationServerContextHolder.getContext())
            .authorizationGrantType(WechatAuthenticationToken.WECHAT_MINI_APP)
            .authorizationGrant(wechatAuthenticationToken);
```

### ✅ 改成（**只加 claims**）：

```
DefaultOAuth2TokenContext.Builder tokenContextBuilder =
        DefaultOAuth2TokenContext.builder()
            .registeredClient(registeredClient)
            .principal(usernamePasswordAuthentication)
            .authorizationServerContext(AuthorizationServerContextHolder.getContext())
            .authorizationGrantType(WechatAuthenticationToken.WECHAT_MINI_APP)
            .authorizationGrant(wechatAuthenticationToken)
            .claims(claims -> {
                claims.put("tenant_id", tenantId);
                claims.put("openid", openid);
            });
```

✅ **这是唯一正确的写入 JWT 的方式**

------

## ✅ Step 4：刷新令牌也带上（可选但强烈推荐）

在 **生成 refresh token 的 tokenContext** 也加：

```
tokenContext = tokenContextBuilder
        .tokenType(OAuth2TokenType.REFRESH_TOKEN)
        .claims(claims -> claims.put("tenant_id", tenantId))
        .build();
```

------

# ✅ 四、最终 JWT 里长什么样？

```
{
  "sub": "member_123",
  "tenant_id": 1001,
  "openid": "oUxxxxxx",
  "scope": "read write",
  "iat": 1700000000,
  "exp": 1700003600
}
```

✅ **资源服务器不需要 TenantContextHolder**

✅ **不需要 ThreadLocal**

✅ **不依赖请求参数**

------

# ✅ 五、资源服务器怎么用（你后面一定会用到）

### ✅ 方式一（推荐）

```
Long tenantId = jwt.getClaim("tenant_id");
```

### ✅ 方式二（Spring Security）

```
@PreAuthorize("#tenantId == authentication.claims['tenant_id']")
```

------

# ✅ 六、给你一句“可以直接写进 MR / PR 说明”的话

> **在 WechatAuthenticationProvider 中通过 wx_appid 识别租户，并将 tenant_id 写入 JWT claims，
>
> 资源服务器通过解析 JWT 中的 tenant_id 完成多租户数据隔离，
>
> 不依赖 ThreadLocal、不通过 additionalParameters 传递租户信息。**

------

## ✅ 下一步你可以继续让我帮你

你可以直接说一句：

- ✅「帮我补 `loadUserByOpenid(openid, tenantId)`示例」
- ✅「告诉我资源服务器怎么从 JWT 自动注入 tenant_id」
- ✅「我想让 tenant_id 同时进 ID Token（OIDC）」
- ✅「帮我检查这个 Provider 会不会重复创建用户」

我**只基于你这一版 Provider 文件**继续改，不推翻你现有结构。