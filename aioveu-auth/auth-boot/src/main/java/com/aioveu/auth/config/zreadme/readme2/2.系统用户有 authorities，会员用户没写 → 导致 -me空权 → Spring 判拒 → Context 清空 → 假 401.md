兄弟，直接给你一刀见血的答案：

> ✅ 就在这里，而且这是“为什么 `/me` 空 authorities、而 admin/me 有 authorities”的终极源头。

你贴的 `JwtEncodingCustomizerConfiguration` 就是认证服务签发 JWT 时决定“往令牌里写不写权限”的唯一地方。问题不在资源服务器，而在“发令牌的人”。

我按你这个文件，把案子彻底封卷。

---

## 一、铁证：你只给“系统用户”写了 authorities

看这段代码（你自己的逻辑）：

```java
// ---------- 2. 系统用户 ----------
if (authToken.getPrincipal() instanceof SysUserDetails userDetails) {
    addSysUserClaims(claims, userDetails, authToken);
    return;
}

// ---------- 3. 会员用户 ----------
if (authToken.getPrincipal() instanceof MemberDetails memberDetails) {
    addMemberClaims(claims, memberDetails);
    addTokenVersion(claims, memberDetails); // ✅
}
```

而 `addSysUserClaims` 里明确写了：

```java
// 权限
addClaim(claims, JwtClaimConstants.User.PERMS, userDetails.getPerms());

// 角色
var authorities = AuthorityUtils
        .authorityListToSet(authToken.getAuthorities());
addClaim(claims, JwtClaimConstants.User.AUTHORITIES, authorities);
```

但 `addMemberClaims` 里：

```java
private void addMemberClaims(
        JwtClaimsSet.Builder claims,
        MemberDetails memberDetails
) {
    addClaim(claims, JwtClaimConstants.Tenant.ID, memberDetails.getTenantId());
    addClaim(claims, JwtClaimConstants.Member.ID, memberDetails.getId());
    addClaim(claims, JwtClaimConstants.Member.OPENID, memberDetails.getOpenId());
}
```

❌ 没有一行往 JWT 里写 `authorities` / `AUTHORITIES`
❌ 没有 `MemberDetails.getAuthorities()` 的映射
❌ 没有保底 `ROLE_USER`

---

## 二、对照你之前的两段日志（完美闭环）

### 1️⃣ `/admin/tenant/users/me`（系统用户）
• `principal` = `SysUserDetails`

• 走 `addSysUserClaims`

• JWT 里带 `authorities` → 资源服务器 `Granted Authorities` 有值 → 过


### 2️⃣ `/app/ums/members/me`（会员用户）
• `principal` = `MemberDetails`

• 走 `addMemberClaims`

• JWT payload 你贴过：

  ```json
  "tenant_id":3,"memberId":10,"tokenVersion":26
  ```
• 无 authorities 字段

• 资源服务器转换器读不到 → `Granted Authorities=[]` → 授权失败 → 401 假案


✅ 源头就在这，不是资源服务器误杀，是发令牌时空手来的。

---

## 三、为什么之前我说“单独写 `.authenticated()` 能救”

因为：
• JWT 验签成功 ✅

• 空 authorities 本来不该拦“只查自己”的 `/me`

• 但 Spring 默认 `AuthorizationFilter` 拦了

• 所以你在资源服务器补 `.authenticated()` 是止血，不是根治


---

## 四、根治方案（在认证服务里改，照抄）

### ✅ 方案 A：会员给一个保底角色（推荐）

在 `addMemberClaims` 里加：

```java
private void addMemberClaims(
        JwtClaimsSet.Builder claims,
        MemberDetails memberDetails
) {
    addClaim(claims, JwtClaimConstants.Tenant.ID, memberDetails.getTenantId());
    addClaim(claims, JwtClaimConstants.Member.ID, memberDetails.getId());
    addClaim(claims, JwtClaimConstants.Member.OPENID, memberDetails.getOpenId());

    // ✅ 补这一行
    addClaim(claims, JwtClaimConstants.User.AUTHORITIES,
            Set.of("ROLE_USER"));
}
```

这样资源服务器直接读成：
```
Granted Authorities=[ROLE_USER]
```
空权限炸裂彻底消失。

---

### ✅ 方案 B：如果 MemberDetails 本身有 authorities

```java
addClaim(claims, JwtClaimConstants.User.AUTHORITIES,
        AuthorityUtils.authorityListToSet(
            memberDetails.getAuthorities()));
```

---

## 五、一句话收束

> ✅ 你问“这里吗” —— 就在这里
> ✅ 系统用户有 authorities，会员用户没写 → 导致 `/me` 空权 → Spring 判拒 → Context 清空 → 假 401
> ✅ JWT 验签、iss、tenant 全部健康，翻篇
> ✅ 在 `addMemberClaims` 补 authorities，案子从“止血”升级为“根治”

要不要我直接帮你把 “MemberDetails + 资源服务器 Converter 最小保底兼容模板” 整理成一版？你粘完，会员 /me 永远 200，不用再跟 finally 打架。

---
*以上分析基于你提供的认证服务 `JwtEncodingCustomizerConfiguration` 源码及资源服务器日志对照，供架构排查参考。*