现在的问题是 **`MemberDetails`类缺少 Jackson 需要的构造函数**。错误信息显示：

```
Cannot construct instance of `com.aioveu.auth.model.MemberDetails` (no Creators, like default constructor, exist)
```

## 🔍 问题分析

Jackson 需要以下之一来反序列化：

1. **无参构造函数** + setter 方法
2. **带 `@JsonCreator`注解的构造函数**

你的 `MemberDetailsMixin`定义了带 `@JsonCreator`的构造函数，但实际的 `MemberDetails`类可能没有对应的构造函数。

## 🔧 解决方案

### 方案1：修改 MemberDetails 类（推荐）

在 `MemberDetails`类中添加 Jackson 注解：

```
package com.aioveu.auth.model;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.springframework.security.core.GrantedAuthority;
import org.springframework.security.core.userdetails.UserDetails;

import java.util.Collection;
import java.util.List;

// 添加 @JsonIgnoreProperties 注解
@JsonIgnoreProperties(ignoreUnknown = true)
public class MemberDetails implements UserDetails {
    
    // ... 原有字段 ...
    
    // 添加 @JsonCreator 注解的构造函数
    @JsonCreator
    public MemberDetails(
            @JsonProperty("id") Long id,
            @JsonProperty("tenantId") Long tenantId,
            @JsonProperty("username") String username,
            @JsonProperty("password") String password,
            @JsonProperty("nickName") String nickName,
            @JsonProperty("mobile") String mobile,
            @JsonProperty("avatarUrl") String avatarUrl,
            @JsonProperty("openid") String openid,
            @JsonProperty("unionid") String unionid,
            @JsonProperty("authorities") List<? extends GrantedAuthority> authorities,
            @JsonProperty("enabled") boolean enabled,
            @JsonProperty("accountNonExpired") boolean accountNonExpired,
            @JsonProperty("credentialsNonExpired") boolean credentialsNonExpired,
            @JsonProperty("accountNonLocked") boolean accountNonLocked) {
        this.id = id;
        this.tenantId = tenantId;
        this.username = username;
        this.password = password;
        this.nickName = nickName;
        this.mobile = mobile;
        this.avatarUrl = avatarUrl;
        this.openid = openid;
        this.unionid = unionid;
        this.authorities = authorities;
        this.enabled = enabled;
        this.accountNonExpired = accountNonExpired;
        this.credentialsNonExpired = credentialsNonExpired;
        this.accountNonLocked = accountNonLocked;
    }
    
    // 确保有无参构造函数（如果原来没有）
    public MemberDetails() {
    }
    
    // ... 其他方法 ...
}
```

### 方案2：简化 Mixin

如果不想修改 `MemberDetails`类，可以简化 Mixin：

```
// MemberDetailsMixin.java
package com.aioveu.auth.oauth2.jackson;

import com.fasterxml.jackson.annotation.JsonAutoDetect;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * 简化的 MemberDetails Mixin
 * 使用属性自动检测
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.CLASS)
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonAutoDetect(
    fieldVisibility = JsonAutoDetect.Visibility.ANY,           // 所有字段可见
    getterVisibility = JsonAutoDetect.Visibility.NONE,         // 不序列化 getter
    isGetterVisibility = JsonAutoDetect.Visibility.NONE,       // 不序列化 isGetter
    setterVisibility = JsonAutoDetect.Visibility.NONE          // 不序列化 setter
)
public abstract class MemberDetailsMixin {
    // 空的 Mixin，依赖字段自动检测
}
```

### 方案3：在配置中启用默认构造函数检测

修改 `AuthorizationServerConfig`：

```
@Bean
public OAuth2AuthorizationService authorizationService(JdbcTemplate jdbcTemplate,
                                                       RegisteredClientRepository registeredClientRepository) {

    JdbcOAuth2AuthorizationService service =
            new JdbcOAuth2AuthorizationService(jdbcTemplate, registeredClientRepository);

    JdbcOAuth2AuthorizationService.OAuth2AuthorizationRowMapper rowMapper = 
            new JdbcOAuth2AuthorizationService.OAuth2AuthorizationRowMapper(registeredClientRepository);
    rowMapper.setLobHandler(new DefaultLobHandler());

    ObjectMapper objectMapper = new ObjectMapper();
    ClassLoader classLoader = JdbcOAuth2AuthorizationService.class.getClassLoader();
    List<Module> securityModules = SecurityJackson2Modules.getModules(classLoader);
    objectMapper.registerModules(securityModules);
    objectMapper.registerModule(new OAuth2AuthorizationServerJackson2Module());

    // ✅ 启用默认类型
    objectMapper.activateDefaultTyping(
        objectMapper.getPolymorphicTypeValidator(),
        ObjectMapper.DefaultTyping.NON_FINAL
    );
    
    // 或者使用更简单的 Mixin
    objectMapper.addMixIn(MemberDetails.class, SimpleMemberDetailsMixin.class);
    objectMapper.addMixIn(SysUserDetails.class, SysUserMixin.class);
    objectMapper.addMixIn(Long.class, Object.class);

    rowMapper.setObjectMapper(objectMapper);
    service.setAuthorizationRowMapper(rowMapper);

    return service;
}
```

## 🎯 推荐：方案1 + 方案2 结合

### 步骤1：检查 MemberDetails 类

查看 `MemberDetails`是否有：

1. 无参构造函数
2. 所有字段的 getter/setter

如果没有，添加必要的构造函数。

### 步骤2：使用简化的 Mixin

```
// MemberDetailsMixin.java
package com.aioveu.auth.oauth2.jackson;

import com.fasterxml.jackson.annotation.JsonAutoDetect;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

@JsonTypeInfo(use = JsonTypeInfo.Id.CLASS)
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonAutoDetect(fieldVisibility = JsonAutoDetect.Visibility.ANY)
public abstract class MemberDetailsMixin {
}
```

## 🔧 快速修复测试

### 测试1：先添加无参构造函数

在 `MemberDetails`类中添加：

```
public MemberDetails() {
    // 无参构造函数
}
```

### 测试2：清理数据库

```
DELETE FROM oauth2_authorization;
```

### 测试3：重启测试

## 📋 验证 MemberDetails 类结构

你的 `MemberDetails`类应该类似：

```
@Data
@NoArgsConstructor  // Lombok 生成无参构造函数
@AllArgsConstructor // Lombok 生成全参构造函数
@JsonIgnoreProperties(ignoreUnknown = true)
public class MemberDetails implements UserDetails {
    
    private Long id;
    private Long tenantId;
    private String username;
    private String password;
    private String nickName;
    private String mobile;
    private String avatarUrl;
    private String openid;
    private String unionid;
    private List<SimpleGrantedAuthority> authorities;
    private boolean enabled = true;
    private boolean accountNonExpired = true;
    private boolean credentialsNonExpired = true;
    private boolean accountNonLocked = true;
    
    // 如果使用 Lombok，不需要显式写构造函数
    // 如果没有 Lombok，需要添加：
    // public MemberDetails() {}
    // public MemberDetails(Long id, ...) { this.id = id; ... }
}
```

## 🎯 如果还是不行

如果上述方法都不行，可以尝试**临时方案**：不在认证信息中存储完整对象。

修改 `WechatAuthenticationProvider`：

```
// 存储用户名，不存储完整对象
UsernamePasswordAuthenticationToken usernamePasswordAuthentication = new UsernamePasswordAuthenticationToken(
    userDetails.getUsername(),  // String
    null,
    userDetails.getAuthorities()
);

// 存储时
.attribute(Principal.class.getName(), userDetails.getUsername());  // String
```

然后在刷新令牌时重新加载用户。

## 📊 建议顺序

1. **先检查** `MemberDetails`是否有无参构造函数
2. **尝试简化** Mixin
3. **如果还不行**，临时修改为存储用户名
4. **长期解决**：完善 `MemberDetails`的序列化支持

先告诉我你的 `MemberDetails`类的结构，我可以给出更精确的修复方案。