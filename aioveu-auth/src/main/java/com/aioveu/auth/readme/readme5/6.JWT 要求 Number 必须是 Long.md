问题已经100% 定位，而且就在你贴出来的代码里，不是猜测。

---

## 一、结论（直接给答案）

> ❌ 不是 tenantId
> ❌ 不是 token_version
> ❌ 不是 Redis
>
> ✅ 是你在这里把 `Integer` 塞进了 JWT Claims：

```java
claims.claim(JwtClaimConstants.User.DATA_SCOPE, userDetails.getDataScope());
```

而：

```java
private Integer dataScope;
```

👉 JWT 要求 Number 必须是 `Long`，你给了 `Integer`

---

## 二、为什么是这一行（铁证）

### 1️⃣ 异常栈精确到这一行

```text
JwtTokenCustomizerConfig.lambda$jwtTokenCustomizer$0(JwtTokenCustomizerConfig.java:101)
```

你第 101 行附近 只有这一段是 claim + Integer：

```java
// 数据权限 - 可以为空，但建议设置默认值
if (userDetails.getDataScope() != null) {
    claims.claim(JwtClaimConstants.User.DATA_SCOPE, userDetails.getDataScope());
}
```

---

### 2️⃣ 异常类型是“Integer → Long”

```text
java.lang.ClassCastException:
  java.lang.Integer cannot be cast to java.lang.Long
```

Spring 在生成 JWT 时，会把所有 Number claim 统一当成 Long 处理。

---

## 三、为什么 tenantId 没炸，但 dataScope 炸了

因为你 刚好没走到 tenantId 的 claim：

```java
if (userDetails.getTenantId() != null) {
    claims.claim(JwtClaimConstants.Tenant.ID, userDetails.getTenantId());
}
```

而你的日志里：

```text
tenantId=0
```

但你 在前面日志里打印的是 `tenantId=0`
👉 如果 tenantId 是 null，就不会 claim
👉 但 dataScope 一定有值

---

## 四、你这个类的「必炸点清单」（不只一个）

### ❌ 必炸（现在）

```java
claims.claim(JwtClaimConstants.User.DATA_SCOPE, userDetails.getDataScope()); // Integer
```

### ⚠️ 潜在会炸（下次）

```java
claims.claim(JwtClaimConstants.Tenant.ID, userDetails.getTenantId()); // Long ✅（暂时安全）
claims.claim(JwtClaimConstants.User.ID, userDetails.getUserId());     // Long ✅
claims.claim(JwtClaimConstants.User.DEPT_ID, userDetails.getDeptId()); // Long ✅
```

### ❌ 已经错误（MemberDetails）

```java
claims.claim(JwtClaimConstants.Tenant.ID, String.valueOf(tenantId)); // ❌ 不该是 String
```

---

## 五、✅ 正确改法（你现在就该这么改）

### ✅ 1️⃣ 立刻止血（只改一行）

```java
if (userDetails.getDataScope() != null) {
    claims.claim(
        JwtClaimConstants.User.DATA_SCOPE,
        userDetails.getDataScope().longValue()
    );
}
```

---

### ✅ 2️⃣ 顺手把所有 Number claim 统一成 Long（强烈推荐）

```java
// 用户ID
if (userDetails.getUserId() != null) {
    claims.claim(JwtClaimConstants.User.ID, userDetails.getUserId());
}

// 部门ID
if (userDetails.getDeptId() != null) {
    claims.claim(JwtClaimConstants.User.DEPT_ID, userDetails.getDeptId());
}

// 数据权限
if (userDetails.getDataScope() != null) {
    claims.claim(JwtClaimConstants.User.DATA_SCOPE,
            userDetails.getDataScope().longValue());
}

// 租户ID
if (userDetails.getTenantId() != null) {
    claims.claim(JwtClaimConstants.Tenant.ID, userDetails.getTenantId());
}
```

---

### ✅ 3️⃣ 修复 MemberDetails 的错误（重要）

❌ 现在：

```java
claims.claim(JwtClaimConstants.Tenant.ID, String.valueOf(tenantId));
```

✅ 改成：

```java
if (tenantId != null) {
    claims.claim(JwtClaimConstants.Tenant.ID, tenantId);
}
```

⚠️ JWT 的 tenant_id 必须是数值，不是字符串

---

## 六、你这个 JwtTokenCustomizer 的「铁律」（以后不再踩）

> ❗ JWT 里所有 Number 类型，必须是 `Long`
>
> ❗ Integer / Short / int 一律 `.longValue()`
>
> ❗ 不要为了“好看”把数值转成 String

---

## 七、一句话总结（你可以彻底收工了）

> ❗ 这次 401 / ClassCastException 的唯一原因：
>
> ✅ 你在 JWT 里塞了一个 `Integer`：`dataScope`
>
> 👉 改成 `dataScope.longValue()`，问题立刻消失

---

如果你愿意，下一步我可以帮你：

1️⃣ 把 整个 JwtTokenCustomizer 重构成「零风险模板」  
2️⃣ 帮你把 SysUserDetails / MemberDetails 的 JWT Claim 规范收敛成统一约定  
3️⃣ 帮你把 token_version / tenant_id / data_scope 的写入位置标准化

你只要说一句：

> 👉「帮我整理一套不会踩坑的 JWT Customizer 模板」

我直接给你成品。