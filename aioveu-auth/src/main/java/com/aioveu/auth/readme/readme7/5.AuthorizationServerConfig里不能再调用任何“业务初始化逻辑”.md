好，这个 Service 我已经完整看完了，而且我可以非常明确地告诉你：

> ✅ 这个类本身没有“直接”依赖 `AuthorizationServerConfig`
> ✅ 但它“间接”触发了循环依赖
> ✅ 罪魁祸首不在你写的业务代码，而在 `JdbcRegisteredClientRepository` 的初始化时机

我直接带你精确定位，你照着改就行。

---

## 一、先给你结论（最重要）

### ❌ 循环不是因为你写了什么业务逻辑
### ✅ 循环是因为：

```
Oauth2RegisteredClientServiceImpl
   ↓
JdbcRegisteredClientRepository
   ↓
AuthorizationServerConfig（@Bean 创建它）
   ↓
Oauth2RegisteredClientServiceImpl（又被 Config 引用）
```

👉 `JdbcRegisteredClientRepository` 的创建，依赖了 `AuthorizationServerConfig`
👉 而 `AuthorizationServerConfig` 又依赖了你的 Service

---

## 二、真正“作死”的那一行在哪里？

### ✅ 就在这里（你贴的代码里）

```java
@Bean
@Primary
public JdbcRegisteredClientRepository jdbcRegisteredClientRepository(JdbcTemplate jdbcTemplate) {
    JdbcRegisteredClientRepository repository =
            new JdbcRegisteredClientRepository(jdbcTemplate);

    initMallAppClient(repository);
    initMallAdminClient(repository);

    return repository;
}
```

👉 这个 Bean 是在 `AuthorizationServerConfig` 里创建的
👉 但它“初始化时调用了 Service 方法”
👉 而 Service 又依赖 `JdbcRegisteredClientRepository`

💥 闭环形成

---

## 三、为什么偏偏是现在才炸？

因为你之前：

• ✅ 没用 `CaptchaValidator`

• ✅ 没把 `AuthorizationServerConfig` 注入 Service

• ✅ 没触发 Config → Service → Config 的依赖链


现在你：

• ✅ 加了 `CaptchaValidator`

• ✅ 启动了完整 Bean 扫描

• ✅ Spring 在启动期严格检查循环依赖


👉 于是炸了

---

## 四、✅ 正确解法（必须这么改）

### ✅ 原则（记住这一条）

> ❌ `AuthorizationServerConfig` 里不能再调用任何“业务初始化逻辑”
> ✅ 客户端初始化必须挪出去
> ✅ Config 只负责创建 Bean

---

## 五、你现在必须做的修改（一步一步）

---

### ✅ 第一步：删掉 `AuthorizationServerConfig` 里的这两行

```java
initMallAppClient(registeredClientRepository);
initMallAdminClient(registeredClientRepository);
```

✅ 删干净  
✅ 不要心疼  
✅ 这是唯一正确做法

---

### ✅ 第二步：新建一个「客户端初始化器」（✅ 正确位置）

```java
@Component
@RequiredArgsConstructor
@Slf4j
public class RegisteredClientInitializer {

    private final JdbcRegisteredClientRepository registeredClientRepository;
    private final PasswordEncoder passwordEncoder;

    @PostConstruct
    public void init() {
        initMallAppClient();
        initMallAdminClient();
    }

    private void initMallAppClient() {
        // 你的 initMallAppClient 逻辑
    }

    private void initMallAdminClient() {
        // 你的 initMallAdminClient 逻辑
    }
}
```

✅ 这是 Spring 官方推荐方式
✅ @PostConstruct 在 Bean 创建后执行
✅ 不会参与 Config 依赖链

---

### ✅ 第三步：确认 Service 里不再依赖 Config

你现在的 Service：

```java
@RequiredArgsConstructor
public class Oauth2RegisteredClientServiceImpl {

    private final JdbcRegisteredClientRepository registeredClientRepository;
    private final PasswordEncoder passwordEncoder;
}
```

✅ 这是 100% 正确的
✅ 不要再碰 `AuthorizationServerConfig`

---

## 六、你现在这套结构的“正确依赖图”

```
AuthorizationServerConfig
   ↓
JdbcRegisteredClientRepository (@Bean)

RegisteredClientInitializer (@Component)
   ↓
JdbcRegisteredClientRepository

Oauth2RegisteredClientServiceImpl
   ↓
JdbcRegisteredClientRepository
```

✅ 没有环
✅ Spring 启动成功

---

## 七、一句话给你钉死

> ❌ 不是你 Service 写错
>
> ✅ 是你在 Config 里“初始化客户端”
>
> ✅ 把 init 挪到 `@Component + @PostConstruct`，这关直接过

---

如果你愿意，我可以直接帮你：

1️⃣ 把 `RegisteredClientInitializer` 写成最终版（含幂等初始化）  
2️⃣ 把 `AuthorizationServerConfig` 彻底瘦身（只留 @Bean）  
3️⃣ 给你一份 《Auth 服务 Bean 依赖红线图》

你只要说一句：

> 👉「给我最终版，这次别再玩我了」

我直接给你。