结论先给你：

> ✅ **token_version 的“写入位置”是对的**
>
> ❌ **但“写入方式”还有两个隐患**
>
> ✅ **不是你之前那个 Redis → Long 强转的问题了**
>
> ✅ **现在这个是“并发 + 语义”层面的问题**

我直接给你 **“对 / 不对 / 怎么改成生产级”**。

------

## 一、先说结论（你这段代码里 token_version 对不对）

### ✅ 对的地方（非常重要）

1️⃣ **位置是对的（唯一正确位置）**

```
// ✅ 在 authenticationManager.authenticate() 之后
// ✅ 用户已确认存在
// ✅ 在 JWT 生成之前
```

👉 这是 **100% 正确的地方**

👉 比放在 JwtCustomizer 里 **正确 10 倍**

------

2️⃣ **没有再往 additionalParameters 里塞东西**

```
// ❌ 你已经注释掉了
// additionalParameters.put(...)
```

✅ 这个坑你已经填平了

------

3️⃣ **Redis increment 用法是对的**

```
Long tokenVersion = redisTemplate.opsForValue().increment(versionKey);
```

✅ 这是 **正确写法**

✅ 不会再出现 ClassCastException

------

## 二、❌ 不对 / 不安全的地方（现在真正的问题）

------

### ❌ 问题 1：**token_version 没有放进 Authentication**

你现在只是：

```
log.info("【TokenVersion】用户 {} 登录，token_version = {}", userId, tokenVersion);
```

但 **JWT Customizer 拿不到它**。

👉 你现在 **JwtTokenCustomizer 里是从 Redis 再 get 一次**

👉 那是 **另一个值（并发问题）**

------

### ❌ 问题 2（并发坑，很严重）

假设：

1. 

   用户同时发 2 个登录请求

2. 

   Redis increment 变成 2

3. 

   Provider A 还没写完 JWT

4. 

   Provider B 又 increment → 3

5. 

   JWT Customizer 读 Redis → 3

6. 

   结果：

   - 

     JWT A 的 version = 3

   - 

     JWT B 的 version = 3

   - 

     ❌ 其中一个 token 永远无法失效

👉 **这是分布式登录的经典并发 bug**

------

### ❌ 问题 3：JwtCustomizer 不应该再查 Redis

你现在 JwtCustomizer 里是：

```
Long tokenVersion = (Long) redisTemplate.opsForValue().get(versionKey);
```

❌ 这是错误的设计：

| 原因       | 说明                            |
| ---------- | ------------------------------- |
| 重复 IO    | JWT 生成阶段不应该再查 Redis    |
| 并发不一致 | 可能拿到别人的 version          |
| 职责混乱   | Customizer 只应该“写”，不该“算” |

------

## 三、✅ 正确做法（你现在就该这么改）

### ✅ 核心原则（记住这一句）

> **token_version 必须在 Provider 里“算好 + 绑定到 Authentication”**
>
> **JwtCustomizer 只负责“抄写到 JWT”**

------

## 四、✅ 正确改法（最小侵入版）

### ✅ 1️⃣ Provider 里：把 token_version 放进 Authentication.details

```
if (userId != null) {
    String versionKey = RedisConstants.Auth.USER_TOKEN_VERSION + userId;
    Long tokenVersion = redisTemplate.opsForValue().increment(versionKey);
    if (tokenVersion == null) {
        tokenVersion = 1L;
    }

    // ✅ 放进 details（不是 additionalParameters）
    Map<String, Object> details = new HashMap<>();
    details.put(JwtClaimConstants.Token.VERSION, tokenVersion);

    ((UsernamePasswordAuthenticationToken) usernamePasswordAuthentication)
            .setDetails(details);

    log.info("【TokenVersion】用户 {} 登录，token_version = {}", userId, tokenVersion);
}
```

------

### ✅ 2️⃣ JwtCustomizer 里：**只从 details 读**

```
Object details = authentication.getDetails();
if (details instanceof Map) {
    Object version = ((Map<?, ?>) details).get(JwtClaimConstants.Token.VERSION);
    if (version instanceof Number) {
        claims.claim(
            JwtClaimConstants.Token.VERSION,
            ((Number) version).longValue()
        );
    }
}
```

✅ 好处：

| 好处                | 说明                       |
| ------------------- | -------------------------- |
| ✅ 并发安全          | version 和 token 一一绑定  |
| ✅ 无 Redis 二次查询 | 性能更好                   |
| ✅ 职责清晰          | Provider 算，Customizer 写 |
| ✅ 永不出错          | 不依赖 Redis 时序          |

------

## 五、你这套 token_version 的「最终正确架构」

```
登录请求
  ↓
Provider
  ↓
Redis increment（生成 version）
  ↓
放入 Authentication.details
  ↓
JwtCustomizer 从 details 写进 JWT
  ↓
JWT 里 version = 2
```

✅ **version 永远和 token 绑定**

✅ **不会串**

✅ **不会并发错**

------

## 六、一句话总结（你可以彻底放心）

> ✅ **你现在的 token_version 位置是对的**
>
> ❌ **但 version 没有绑定到 Authentication，导致 JwtCustomizer 从 Redis 再读，会并发出错**
>
> 👉 **把 token_version 放进 Authentication.details，JwtCustomizer 只读，不改**

------

如果你愿意，下一步我可以帮你：

1️⃣ 把 **token_version + 黑名单 + 刷新令牌失效** 做成一套完整方案

2️⃣ 或帮你把 **Provider / JwtCustomizer 的“职责边界”画成一张不会再踩坑的图**

3️⃣ 或帮你写 **并发测试用例，直接证明这个 bug 的存在**

你只要说一句：

> 👉「帮我把 token_version 做成生产级方案」

我直接给你成品。