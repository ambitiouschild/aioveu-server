从您提供的代码中，**切换租户后调用后端接口刷新的逻辑在 `switchTenant`方法中**。

## 切换租户的完整流程

### 1. 主要方法：`switchTenant`

```
async function switchTenant(tenantId: number): Promise<void> {
  // ✅ 1. 切换租户前，如果需要刷新Token，先刷新
  await refreshTokenIfSupported(tenantId);

  // ✅ 2. 调用后端切换租户接口
  const tenantInfo = await TenantAPI.switchTenant(tenantId);
  
  if (tenantInfo) {
    setCurrentTenant(tenantInfo);
    return;
  }

  // ✅ 3. 如果后端返回失败，尝试本地匹配
  const matched = tenantList.value.find((t) => t.id === tenantId);
  if (matched) {
    setCurrentTenant(matched);
    return;
  }

  // ✅ 4. 最后尝试获取当前租户
  const fallback = await safeGetCurrentTenant();
  if (fallback) {
    setCurrentTenant(fallback);
    return;
  }

  throw new Error("切换租户后无法获取租户信息");
}
```

### 2. 关键的Token刷新方法：`refreshTokenIfSupported`

```
async function refreshTokenIfSupported(tenantId: number): Promise<void> {
  try {
    // ✅ 调用后端接口刷新Token
    const token = await AuthAPI.switchTenant(tenantId);
    
    if (token?.accessToken && token?.refreshToken) {
      // 更新本地存储的Token
      AuthStorage.setTokens(
        token.accessToken, 
        token.refreshToken, 
        AuthStorage.getRememberMe()
      );
    }
  } catch {
    // 忽略异常：非平台用户或后端未启用该接口时，回退到旧接口
  }
}
```

## 接口定义分析

### 1. 租户切换接口：`TenantAPI.switchTenant(tenantId)`

```
// 对应代码
const tenantInfo = await TenantAPI.switchTenant(tenantId);
```

### 2. Token刷新接口：`AuthAPI.switchTenant(tenantId)`

```
// 对应代码
const token = await AuthAPI.switchTenant(tenantId);
```

## 完整调用流程

当用户在前端切换租户时，调用链如下：

```
用户点击切换租户
    ↓
调用 tenantStore.switchTenant(tenantId)
    ↓
┌→ refreshTokenIfSupported(tenantId)  // 1. 尝试刷新Token
│  ↓
│  AuthAPI.switchTenant(tenantId)  // 调用后端接口获取新Token
│  ↓
│  AuthStorage.setTokens()  // 更新本地Token
│
└→ TenantAPI.switchTenant(tenantId)  // 2. 调用切换租户接口
    ↓
setCurrentTenant(tenantInfo)  // 3. 更新本地状态
    ↓
保存到 localStorage
```

## API 接口实现示例

根据您的代码结构，API 接口可能如下定义：

**auth.api.ts**：

```
// AuthAPI.switchTenant 的实现
export default class AuthAPI {
  /**
   * 切换租户并获取新Token
   */
  static async switchTenant(tenantId: number) {
    return request({
      url: '/api/v1/auth/switch-tenant',
      method: 'POST',
      params: { tenantId }
    });
  }
}
```

**tenant.api.ts**：

```
// TenantAPI.switchTenant 的实现
export default class TenantAPI {
  /**
   * 切换租户
   */
  static async switchTenant(tenantId: number) {
    return request({
      url: '/api/v1/tenant/switch',
      method: 'POST',
      params: { tenantId }
    });
  }
}
```

## 前端组件的使用方式

```
<!-- 父组件中使用 -->
<template>
  <tenant-switcher @change="handleTenantChange" />
</template>

<script setup>
import { useTenantStore } from '@/store/tenant';

const tenantStore = useTenantStore();

async function handleTenantChange(tenantId) {
  try {
    // ✅ 这里会调用后端接口
    await tenantStore.switchTenant(tenantId);
    
    // 切换成功后的处理
    ElMessage.success('租户切换成功');
    
    // 如果需要，刷新页面
    window.location.reload();
    
  } catch (error) {
    ElMessage.error('切换失败: ' + error.message);
  }
}
</script>
```

## 总结

在您的代码中，**切换租户后调用后端接口的逻辑位于**：

1. **`refreshTokenIfSupported`方法** → 调用 `AuthAPI.switchTenant()`获取新Token
2. **`switchTenant`方法** → 调用 `TenantAPI.switchTenant()`切换租户

**调用顺序**：

1. 先调用Token刷新接口（如果需要）
2. 再调用租户切换接口
3. 更新本地状态和存储

这样设计确保了在切换租户时：

- ✅ 获取新的访问Token（如果需要）
- ✅ 更新后端的租户上下文
- ✅ 同步更新前端的租户状态
- ✅ 持久化存储当前租户信息