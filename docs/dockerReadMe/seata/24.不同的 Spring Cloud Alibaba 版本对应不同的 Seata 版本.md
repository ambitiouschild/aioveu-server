从你的 `pom.xml`来看，你**没有直接定义 Seata 版本**，而是使用了 `spring-cloud-starter-alibaba-seata`这个 Spring Cloud Alibaba 的封装包。

## 如何确定版本

### 1. 检查父 POM

先看你的父 POM (`aioveu-common/pom.xml`)：

```
<!-- 查看父 POM 中是否有 Seata 版本定义 -->
<properties>
    <seata.version>2.0.0</seata.version> <!-- 可能在这里 -->
</properties>
```

### 2. 查看依赖传递

运行这个命令查看实际版本：

```
# 在项目根目录运行
mvn dependency:tree | grep -i seata
```

你应该会看到类似这样的输出：

```
[INFO] +- com.alibaba.cloud:spring-cloud-starter-alibaba-seata:jar:2023.0.0.0-RC1:compile
[INFO] |  +- io.seata:seata-spring-boot-starter:jar:2.0.0:compile
[INFO] |  |  +- io.seata:seata-spring-autoconfigure-client:jar:2.0.0:compile
```

**如果看到 `2.0.0`，那就是 2.x 版本。**

### 3. 查看 Spring Cloud Alibaba 版本

从你的依赖看：

```
<dependency>
    <groupId>com.alibaba.cloud</groupId>
    <artifactId>spring-cloud-starter-alibaba-seata</artifactId>
</dependency>
```

不同的 Spring Cloud Alibaba 版本对应不同的 Seata 版本：

| Spring Cloud Alibaba 版本 | 默认的 Seata 版本 |
| ------------------------- | ----------------- |
| 2022.0.0.0                | 1.8.0             |
| 2023.0.0.0-RC1            | 2.0.0             |
| 2023.0.0.0                | 2.0.0             |

## 立即检查

运行这个诊断脚本：

```
#!/bin/bash
echo "=== 检查 Seata 版本 ==="

echo "1. 查看依赖树中的 Seata:"
mvn dependency:tree | grep -A3 -B3 "seata"

echo -e "\n2. 查看项目使用的 Spring Cloud Alibaba 版本:"
grep -A2 -B2 "alibaba" pom.xml

echo -e "\n3. 查看父 POM 中的版本定义:"
if [ -f "../pom.xml" ]; then
    grep -i "seata\|alibaba" ../pom.xml
fi
```

## 最可能的情况

从你的配置和错误日志看，**几乎可以肯定你用的是 Seata 2.0.0**，因为：

1. 你用了最新的 `spring-cloud-starter-alibaba-seata`
2. 日志显示 Seata 在单独订阅配置项（2.0.0 的特性）
3. 你遇到了 2.0.0 特有的配置问题

## 解决方案

### 方案一：在父 POM 中统一指定版本

在父 POM (`aioveu-common/pom.xml`) 中添加：

```
<properties>
    <!-- 指定 Seata 1.8.1 版本 -->
    <seata.version>1.8.1</seata.version>
</properties>

<dependencyManagement>
    <dependencies>
        <!-- 覆盖 Spring Cloud Alibaba 的 Seata 版本 -->
        <dependency>
            <groupId>io.seata</groupId>
            <artifactId>seata-spring-boot-starter</artifactId>
            <version>${seata.version}</version>
        </dependency>
    </dependencies>
</dependencyManagement>
```

### 方案二：在当前模块中排除并指定

修改你的 `common-seata/pom.xml`：

```
<dependencies>
    <dependency>
        <groupId>com.alibaba.cloud</groupId>
        <artifactId>spring-cloud-starter-alibaba-seata</artifactId>
        <exclusions>
            <!-- 排除所有 Seata 依赖 -->
            <exclusion>
                <groupId>io.seata</groupId>
                <artifactId>*</artifactId>
            </exclusion>
        </exclusions>
    </dependency>
    
    <!-- 手动指定 1.8.1 -->
    <dependency>
        <groupId>io.seata</groupId>
        <artifactId>seata-spring-boot-starter</artifactId>
        <version>1.8.1</version>
    </dependency>
</dependencies>
```

### 方案三：直接指定版本

最简单的方法，在当前 `pom.xml`中：

```
<properties>
    <seata.version>1.8.1</seata.version>
</properties>

<dependencies>
    <dependency>
        <groupId>com.alibaba.cloud</groupId>
        <artifactId>spring-cloud-starter-alibaba-seata</artifactId>
        <exclusions>
            <exclusion>
                <groupId>io.seata</groupId>
                <artifactId>*</artifactId>
            </exclusion>
        </exclusions>
    </dependency>
    
    <dependency>
        <groupId>io.seata</groupId>
        <artifactId>seata-spring-boot-starter</artifactId>
        <version>${seata.version}</version>
    </dependency>
</dependencies>
```

## 立即验证

运行这个命令查看当前版本：

```
# 在 common-seata 目录运行
mvn dependency:tree -Dincludes=io.seata:*
```

**告诉我输出结果，我就能确定你的确切版本。**

## 总结

你的客户端**几乎肯定是 2.0.0 版本**，因为：

1. 用了最新的 Spring Cloud Alibaba
2. 出现了 2.0.0 特有的错误
3. 与 1.7.1 的 Seata Server 不兼容

**立即修改为 1.8.1 版本**，问题应该就能解决。