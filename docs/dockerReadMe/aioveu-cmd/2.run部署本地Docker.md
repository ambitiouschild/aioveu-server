
# MySQL

## 安装

```bash
docker-compose -f docker-compose.yml -p aioveu-server up -d  --no-recreate
```
## 卸载
```bash
docker-compose -f docker-compose.yml -p aioveu-server down
```
```bash
# 2.切换回本地上下文
docker context use default
```


```bash
# 3.切换远程上下文执行验证
docker context show  # 应显示 "remote"
```
