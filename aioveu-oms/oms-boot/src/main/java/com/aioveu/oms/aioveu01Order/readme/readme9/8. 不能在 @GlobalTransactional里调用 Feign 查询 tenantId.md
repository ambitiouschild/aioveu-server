你这个判断非常对，而且我已经可以直接告诉你：

> ✅ 这里不仅是“有问题”，而且是“Seata 回滚的根源”

我直接给你把致命点 + 正确写法说出来。

---

## 一、先给你一句“死结论”

> ❌ 不能在 `@GlobalTransactional` 里调用 Feign 查询 `tenantId`
>
> ✅ 更不能把 Feign 结果当成“强依赖”来用
>
> ❌ `tenantId == null` 抛异常 = 必回滚

👉 你这段代码的每一行，都在踩 Seata 的雷区

---

## 二、为什么这里一定会出问题（核心原因）

### 1️⃣ Feign 调用 **不属于数据库资源**

Seata 只保证：

• 数据库

• MQ

• Redis（部分）


👉 Feign 调用是“外部系统”
👉 Seata 无法回滚 tenant-service

---

### 2️⃣ 你在这里抛异常，Seata 只能回滚

```java
if (tenantId == null) {
    throw new RuntimeException(...)
}
```

👉 Seata 看到异常：
• ✅ 回滚本地事务

• ✅ 回滚所有分支

• ❌ 但 tenant-service 那边已经查完了


👉 这不是 Bug，是 Seata 的正确行为

---

### 3️⃣ `Result<T>` 在事务里是“毒药”

```java
Result<Long> result = tenantFeignClient.getTenantIdByClientId(clientId);
```

• Feign 超时 ❌

• 网络抖动 ❌

• tenant-service 重启 ❌


👉 任何一个，都会导致 `tenantId == null`
👉 然后你亲手把整个全局事务炸了

---

## 三、你现在这段代码，正确的“定性”

> ❌ 这不是“校验逻辑”
>
> ❌ 这是“分布式事务里的外部强依赖”
>
> ✅ 这是 Seata 回滚的 100% 原因

---

## 四、✅ 正确做法（三种，你选一个）

---

### ✅✅✅ 方案一（**强烈推荐，最干净**）

#### 🔥 **tenantId 必须在事务开始前就拿到**

```java
@PostMapping("/submit")
public Result<?> submit(@RequestHeader("X-Tenant-Id") Long tenantId) {
    orderService.submit(tenantId);
    return Result.success();
}
```

```java
@GlobalTransactional
public void submit(Long tenantId) {
    // ✅ 不再查 Feign
}
```

✅ Gateway 已经查过  
✅ OMS 只信任 Header  
✅ 不碰 Feign  
✅ Seata 100% 安全  

👉 这是你这套架构的“标准答案”

---

### ✅ 方案二（次优）：Feign 调用 **移出事务**

```java
public Long resolveTenantId(String clientId) {
    return tenantFeignClient.getTenantIdByClientId(clientId);
}
```

```java
@GlobalTransactional
public void submit(String clientId) {
    Long tenantId = resolveTenantId(clientId); // ❌ 仍然有风险
}
```

⚠️ 只能缓解，不能根治

---

### ✅ 方案三（兜底）：本地缓存 / 冗余

```java
Long tenantId = tenantIdCache.get(clientId);
```

✅ 不查 Feign  
✅ 不回滚  
❌ 要维护一致性  

---

## 五、你现在这个判断，正确的写法（如果你一定要保留）

```java
Long tenantId = result.getData();

if (tenantId == null) {
    log.error("【Oms-submitOrder】租户不存在，clientId={}", clientId);
    // ❌ 不要抛异常
    // 直接 fail fast，返回错误
    throw new BizException("租户不存在");
}
```

👉 但前提：
• 这个方法 不能是 `@GlobalTransactional`

• 或者 Feign 调用不在事务内


---

## 六、一句话把这件事钉死（你可以当铁律）

> ❌ @GlobalTransactional 里禁止 Feign 查询
>
> ❌ 禁止在事务里抛“外部系统导致”的异常
>
> ✅ 事务只做：数据库 + 本地逻辑
>
> ✅ tenantId 必须在事务外确定

---

## 七、你现在这个 Seata 回滚，是“设计层面的必然结果”

你不是“写错了一行代码”，  
你是在一个分布式事务里，引入了不可回滚的外部依赖。

Seata 只是在忠实地履行它的职责。

---

## 八、我建议你马上做的 3 件事（非常关键）

1️⃣ 把 `getTenantIdByClientId` 从 `@GlobalTransactional` 方法里挪出去  
2️⃣ tenantId 从 Gateway Header 拿  
3️⃣ OMS 里永远不要再 Feign 查租户

---

如果你愿意，我可以帮你把：

👉 「Seata 事务边界规范」
👉 「Gateway / OMS / Tenant 的事务拆分方案」

直接给你画成一张时序图 + 禁区表，以后你团队 再也不会在事务里踩 Feign 的坑 👍