好，这个 `OrderExportTask` 我直接给你一个“平台级、多租户、审计级、不会返工”的终局版本 👍  
你照着用就行。

---

## ✅ 一、OrderExportTask（✅ 终局实体）

```java
@Data
@TableName("order_export_task")
@Schema(description = "订单导出任务")
public class OrderExportTask implements Serializable {

    private static final long serialVersionUID = 1L;

    @TableId(value = "id", type = IdType.ASSIGN_ID)
    @Schema(description = "主键ID")
    private Long id;

    @Schema(description = "租户ID")
    private Long tenantId;

    @Schema(description = "操作人ID")
    private Long operatorId;

    @Schema(description = "客户端ID（小程序 / H5 / PC）")
    private String clientId;

    /* ================= 查询条件 ================= */

    @Schema(description = "订单状态")
    private Integer orderStatus;

    @Schema(description = "开始时间")
    private LocalDateTime startTime;

    @Schema(description = "结束时间")
    private LocalDateTime endTime;

    @Schema(description = "导出格式：excel / csv")
    private String exportFormat;

    @Schema(description = "总条数")
    private Integer totalCount;

    /* ================= 任务状态 ================= */

    @Schema(description = "任务状态：PENDING / RUNNING / SUCCESS / FAILED")
    private String status;

    @Schema(description = "文件地址")
    private String fileUrl;

    @Schema(description = "失败原因")
    private String failReason;

    /* ================= 时间线 ================= */

    @Schema(description = "创建时间")
    private LocalDateTime createTime;

    @Schema(description = "开始执行时间")
    private LocalDateTime startTimeActual;

    @Schema(description = "完成时间")
    private LocalDateTime finishTime;
}
```

---

## ✅ 二、对应表结构（✅ 推荐）

```sql
CREATE TABLE order_export_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    operator_id BIGINT NOT NULL,
    client_id VARCHAR(64),

    order_status INT,
    start_time DATETIME,
    end_time DATETIME,

    export_format VARCHAR(16),
    total_count INT,

    status VARCHAR(32),
    file_url VARCHAR(255),
    fail_reason VARCHAR(512),

    create_time DATETIME,
    start_time_actual DATETIME,
    finish_time DATETIME,

    INDEX idx_tenant_status (tenant_id, status),
    INDEX idx_create_time (create_time)
);
```

---

## ✅ 三、状态枚举（✅ 平台级）

```java
@Getter
@AllArgsConstructor
public enum ExportStatusEnum {

    PENDING("待执行"),
    RUNNING("执行中"),
    SUCCESS("成功"),
    FAILED("失败");

    private final String desc;
}
```

---

## ✅ 四、你现在这套的“终局语义”

| 字段       | 为什么必须有  |
| ---------- | ------------- |
| tenantId   | ✅ 多租户隔离  |
| operatorId | ✅ 审计 / 追责 |
| clientId   | ✅ 端识别      |
| status     | ✅ 幂等 / 重试 |
| fileUrl    | ✅ 下载        |
| failReason | ✅ 排查问题    |
| 时间线     | ✅ 监控 / 报警 |

---

## ✅ 五、一句话终局结论

> OrderExportTask = 平台级任务的“身份证 + 履历”  
> 不是一张临时表

你现在这套：
• ✅ 不返工

• ✅ 可审计

• ✅ 可扩展

• ✅ 可直接上线


👉 这一版，就是终局了 😄🫡