那你的建议是什么，我的后端保存的是订单id，前端无法直接使用订单号查询和新增

我的建议是：**使用前端选择器组件 + 后端ID存储**，这是最稳健、最符合工程实践的做法。

## 我的最终建议方案

### 核心原则

1. **后端存储**：始终存储订单ID（数据库外键）
2. **前端界面**：用户看到订单号，但选择器返回ID
3. **数据流转**：前端传ID给后端，后端直接使用
4. **查询展示**：后端JOIN联查显示订单号