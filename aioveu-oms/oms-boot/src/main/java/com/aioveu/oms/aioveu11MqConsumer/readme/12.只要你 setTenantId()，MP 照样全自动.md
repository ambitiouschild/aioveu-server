不是“MP 不能用了”，而是 MP 的租户插件在这个方法里“拿不到 tenantId”。  
我直接把这句话说死，不再绕。

---

## ✅ 一句话结论（先给你）

> ❌ 不是 MP 失效
> ✅ 是 MP 在这个方法里问：`tenantId 是谁？`
> ❌ 你没给它，它只能拿到 `null`

---

## ✅ 二、你这个方法的真实执行环境

```java
@RabbitListener(
    queues = PayCommonMqConstantWithBizName.Queue.SUCCESS,
    concurrency = "4-8",
    ackMode = "MANUAL"
)
@Transactional(rollbackFor = Exception.class)
public void onMessage(Message message, Channel channel) {
    ...
}
```

### ✅ 执行上下文是什么？

| 项目            | 是否有 |
| --------------- | ------ |
| HTTP 请求       | ❌      |
| JWT             | ❌      |
| SecurityContext | ❌      |
| 当前登录用户    | ❌      |
| 租户信息        | ❌      |

👉 这是一个“裸线程”
👉 MP 插件照样工作
👉 只是 `getTenantId()` 返回 `null`

---

## ✅ 三、MP 在这里到底还能不能“自动插 tenant_id”？

### ✅ 能，但前提是：

```java
TenantLineHandler.getTenantId() != null
```

### ❌ 现在的情况是：

```java
TenantLineHandler.getTenantId() == null
```

所以结果是：

```sql
INSERT INTO mq_consume_record (..., tenant_id)
VALUES (..., null); -- ❌ 炸
```

👉 不是 MP 不插
👉 是你没告诉它插什么

---

## ✅ 四、✅ 正确、最小、最稳的改法（只加一行）

```java
@RabbitListener(
    queues = PayCommonMqConstantWithBizName.Queue.SUCCESS,
    concurrency = "4-8",
    ackMode = "MANUAL"
)
@Transactional(rollbackFor = Exception.class)
public void onMessage(Message message, Channel channel) {

    PaymentSuccessMessage msg = parse(message);

    TenantContext.setTenantId(msg.getTenantId()); // ✅ 只加这一行

    try {
        service.handlePaymentSuccess(msg);
    } finally {
        TenantContext.clear(); // ✅ 必须清
    }
}
```

✅ MP 继续自动插
✅ SQL 正常
✅ 事务正常
✅ 并发安全

---

## ✅ 五、为什么“下单时能自动插，这里不能？”

因为 这是两个请求入口：

| 入口     | tenantId 来源         |
| -------- | --------------------- |
| 下单     | JWT → SecurityContext |
| MQ       | ❌ 没有                |
| 定时任务 | ❌ 没有                |
| 异步线程 | ❌ 没有                |

👉 MP 不会“记住”你是谁

---

## ✅ 六、最终结论（给你拍板）

> ❌ 不是 MP 不能用了
> ✅ 是 MP 在这里“不知道你是谁”
> ✅ 只要你 `setTenantId()`，MP 照样全自动

你现在这个理解，已经是“真正在生产跑过的人”才会有的困惑 👏  
按上面补一行 `setTenantId()`，MQ + 多租户就彻底闭环了。