你的判断**完全正确**，而且这是**标准电商建模 + 微信发货的最佳实践路径** 👍

我直接给你一个**“结论 + 正确字段映射 + 可复制代码”**的完整答案。

------

## ✅ 结论（先给你吃定心丸）

> ✅ **`item_desc`应该从 `OmsOrderItem`查**
>
> ✅ **通过 `orderId`查 `oms_order_item`**
>
> ✅ **`OmsOrder`本身不存商品描述是合理的**

------

## 一、字段对应关系（微信 ↔ 你的库）

| 微信字段          | 含义         | 正确来源                               |
| ----------------- | ------------ | -------------------------------------- |
| `item_desc`       | 商品概述     | ✅ `OmsOrderItem.spuName / skuName`     |
| `tracking_no`     | 快递单号     | ✅ `OmsOrderDelivery.deliverySn`        |
| `express_company` | 快递公司     | ✅ `OmsOrderDelivery.deliveryCompany`   |
| `transaction_id`  | 微信支付单号 | ✅ `OmsOrder.transactionId`             |
| `openid`          | 支付人       | ✅ `OmsOrder.buyerOpenid`（你后面会加） |

------

## 二、✅ 正确做法：`orderId → orderItems`

### 1️⃣ Mapper

```
@Mapper
public interface OmsOrderItemMapper
        extends BaseMapper<OmsOrderItem> {
}
```

------

### 2️⃣ Service

```
public interface OmsOrderItemService {

    List<OmsOrderItem> listByOrderId(Long orderId);
}
```

------

### 3️⃣ ServiceImpl（✅ 生产级）

```
@Service
@RequiredArgsConstructor
public class OmsOrderItemServiceImpl
        implements OmsOrderItemService {

    private final OmsOrderItemMapper omsOrderItemMapper;

    @Override
    public List<OmsOrderItem> listByOrderId(Long orderId) {

        if (orderId == null) {
            throw new IllegalArgumentException("订单ID不能为空");
        }

        LambdaQueryWrapper<OmsOrderItem> wrapper =
                new LambdaQueryWrapper<>();

        wrapper
                .eq(OmsOrderItem::getOrderId, orderId)
                .eq(OmsOrderItem::getDeleted, 0);

        return omsOrderItemMapper.selectList(wrapper);
    }
}
```

------

## 三、✅ 生成 `item_desc`（微信推荐格式）

### ✅ 推荐写法（一行搞定）

```
String itemDesc = orderItems.stream()
        .map(i -> i.getSpuName()
                + (StringUtils.isNotBlank(i.getSkuName())
                    ? "【" + i.getSkuName() + "】"
                    : "")
                + " x" + i.getQuantity())
        .collect(Collectors.joining("；"));
```

### ✅ 示例输出

```
iPhone 15 Pro【黑色 256G】 x1；官方硅胶壳【午夜色】 x2
```

✅ **用户看得懂**

✅ **微信审核友好**

------

## 四、✅ 在发货 Service 中完整用法

```
// 1. 查订单
OmsOrder order = orderService.getById(orderId);

// 2. 查发货
OmsOrderDelivery delivery =
        omsOrderDeliveryService.selectByOrderId(orderId);

// 3. 查订单商品
List<OmsOrderItem> orderItems =
        omsOrderItemService.listByOrderId(orderId);

// 4. 生成商品概述
String itemDesc = orderItems.stream()
        .map(i -> i.getSpuName()
                + " x" + i.getQuantity())
        .collect(Collectors.joining("；"));

// 5. 组装微信发货参数
ObjectNode body = weChatApiClient.createObjectNode();
...
shippingItem.put("item_desc", itemDesc);
```

------

## 五、⚠️ 微信对 `item_desc`的限制（必看）

| 限制       | 说明            |
| ---------- | --------------- |
| 最大长度   | 建议 ≤ 100 字符 |
| 禁止 emoji | ❌ 会报 47001    |
| 禁止 HTML  | ❌               |
| 可为空     | ✅ 但不推荐      |

✅ 兜底处理：

```
if (StringUtils.isBlank(itemDesc)) {
    itemDesc = "订单商品";
}
```

------

## ✅ 一句话总结

> **你的建模是对的，现在只是把「商品描述」从“订单维度”升级成“订单明细维度”**

✅ 订单管钱

✅ 发货管物流

✅ 订单项管商品

如果你愿意，下一步我可以帮你：

1️⃣ **拆单发货（一单多包裹）**

2️⃣ **微信发货失败自动重试**

3️⃣ **发货幂等（防止重复同步）**

你直接选一个 👍