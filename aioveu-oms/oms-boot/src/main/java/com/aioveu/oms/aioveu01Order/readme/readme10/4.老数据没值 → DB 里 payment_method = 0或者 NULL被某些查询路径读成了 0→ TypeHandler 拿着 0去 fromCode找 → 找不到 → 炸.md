对，这就完全对上了 👍

老数据没值 → DB 里 `payment_method = 0`或者 `NULL`被某些查询路径读成了 `0`→ TypeHandler 拿着 `0`去 `fromCode`找 → 找不到 → 炸。

------

## 现在就三步，全部清掉

### ① 先看看有多少条（心里有数）

```
SELECT 
    COUNT(*) as total,
    SUM(CASE WHEN payment_method = 0 THEN 1 ELSE 0 END) as method_zero,
    SUM(CASE WHEN payment_method IS NULL THEN 1 ELSE 0 END) as method_null,
    SUM(CASE WHEN payment_channel = 0 THEN 1 ELSE 0 END) as channel_zero,
    SUM(CASE WHEN payment_channel IS NULL THEN 1 ELSE 0 END) as channel_null
FROM oms_order;
```

### ② 一把清掉

```
UPDATE oms_order 
SET payment_method = NULL 
WHERE payment_method = 0;

UPDATE oms_order 
SET payment_channel = NULL 
WHERE payment_channel = 0;
```

### ③ `fromCode`+ TypeHandler 加兜底（防以后再出这种事）

就算以后再有脏数据进来，也只会默默返回 `null`，不会再让整个订单列表 500。

------

清完之后重新查一下订单分页接口，应该就不报那个 `IllegalArgumentException`了。

然后你今天这一整天的排障清单回顾一下：

| #    | 问题                                  | 状态                 |
| ---- | ------------------------------------- | -------------------- |
| 1    | MP 乐观锁插件顺序                     | ✅ 修了               |
| 2    | `flow_status`枚举存字符串             | ✅ 修了               |
| 3    | `mq_send_record.payment_scene`没 set  | ✅ 加了               |
| 4    | `pay_flow.flow_no`重复导致事务回滚    | ✅ catch + 改生成策略 |
| 5    | RabbitMQ confirm 超时                 | ✅ 实际已送达         |
| 6    | 消息被旧 Consumer 抢消费              | ✅ 禁用了             |
| 7    | OMS 订单列表 `payment_method=0`炸映射 | ✅ 正在清             |

**从支付回调到 MQ 到 OMS 消费，全链路今天算是彻底打通了** 🎉 后面再遇到什么坑随时来聊 👊