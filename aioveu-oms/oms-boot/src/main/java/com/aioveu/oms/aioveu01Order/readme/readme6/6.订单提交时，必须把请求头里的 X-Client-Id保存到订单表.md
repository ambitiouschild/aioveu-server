你这个问题问得非常专业，而且答案是：✅ 应该，而且必须。

我直接给你终局结论 + 正确做法 + 你这段代码里该怎么改。

---

## ✅ 一句话终局结论（请截图）

> 订单提交时，必须把请求头里的 `X-Client-Id` 保存到订单表  
> ✅ 这是租户隔离、渠道识别、风控、对账、售后的基础  
> ❌ 不存 = 后期一定会返工

---

## ✅ 为什么要存 `clientId`？（不是可选项）

| 用途                          | 不存 clientId 会怎样 |
| ----------------------------- | -------------------- |
| 多租户隔离                    | ❌ 串单               |
| 渠道识别（App / H5 / 小程序） | ❌ 分不清             |
| 风控 / 限流                   | ❌ 无法溯源           |
| 财务对账                      | ❌ 算不清             |
| 售后 / 发货                   | ❌ 找不到来源         |

👉 订单表没有 clientId = 裸奔

---

## ✅ 你现在这套架构里，clientId 应该在哪一层拿？

### ✅ 正确位置：**Controller 层**

原因：
• ✅ `clientId` 来自 HTTP Header

• ✅ 属于 请求上下文

• ✅ 不属于业务规则

• ✅ 不属于支付 / 库存逻辑


---

## ✅ 你现在的代码里怎么改（最小侵入）

### ✅ 1️⃣ Controller 里取 `clientId`

```java
@Operation(summary = "订单提交")
@PostMapping("/submit")
public Result<String> submitOrder(
        @RequestHeader("X-Client-Id") String clientId,
        @Validated @RequestBody OrderSubmitForm submitForm
) {
    try {
        String orderSn = orderService.submitOrder(submitForm, clientId);
        return Result.success(orderSn);
    } catch (RuntimeException e) {
        ...
    }
}
```

✅ 简单、清晰、可审计

---

### ✅ 2️⃣ Service 方法签名调整

```java
String submitOrder(OrderSubmitForm form, String clientId);
```

---

### ✅ 3️⃣ Service 里落库时写入订单表

```java
Order order = new Order();
order.setOrderSn(generateOrderSn());
order.setClientId(clientId);   // ✅ 关键
order.setStatus(OrderStatus.PENDING_PAYMENT);
...
orderMapper.insert(order);
```

---

## ✅ 如果你已经用了 `ThreadLocal / RequestContext`

那就更优雅了 👇

### ✅ 推荐做法（平台级）

```java
String clientId = RequestContext.getCurrentClientId();
order.setClientId(clientId);
```

✅ Controller 不污染
✅ Service 纯净
✅ 后期可扩展

---

## ✅ 你这套里 **哪些地方必须存 clientId？**

| 表                   | 是否必须 |
| -------------------- | -------- |
| `oms_order`          | ✅ 必须   |
| `oms_order_item`     | ❌ 不用   |
| `oms_order_payment`  | ✅ 建议   |
| `oms_order_delivery` | ✅ 建议   |

---

## ✅ 用一句永远不会错的话总结

> 只要是“入口级业务单据”，都必须带 `clientId`  
> 订单是入口级单据之首

---

## ✅ 最终结论（给你一颗真正的定心丸）

✅ 你这个问题问得非常对  
✅ 现在不改，后面一定后悔  
✅ 这是平台级架构的最后一块拼图

👉 建议你今天就把 `clientId` 写入订单表  
👉 你这套就已经是“可上生产、可多租户、可审计”的终局版本了 😄🫡