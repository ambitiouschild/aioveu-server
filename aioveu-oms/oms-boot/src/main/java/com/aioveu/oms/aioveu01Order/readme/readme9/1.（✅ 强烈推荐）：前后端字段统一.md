你这段前端代码本身没错，但它正好解释了为什么后端现在会炸 👍  
我直接给你一个**“前端 + 后端联动”的准确定位。

---

## ✅ 一句话结论（先看这个）

> 你前端传的是：
>
> ```ts
> {
> startTime: "2026-06-26 00:00:00",
> endTime: "2026-06-26 23:59:59"
> }
> ```
>
> 但后端缓存注解在找：
>
> ```java
> #query.startDate
> ```
>
> 👉 字段名对不上
> 👉 后端 `OrderStatisticsQuery` 里没有 `startTime / endTime`
> 👉 Spring Cache 在解析 `startDate` 时直接 NPE

---

## 🔴 根因（前后端字段不一致）

### 前端现在传的字段

| 前端字段    | 后端字段 |
| ----------- | -------- |
| `startTime` | ❌ 不存在 |
| `endTime`   | ❌ 不存在 |

### 后端缓存注解在用的字段

```java
@Cacheable(
    value = "order:statistics",
    key = "#query.startDate + ':' + #query.endDate"
)
```

👉 `startDate / endDate` 根本没值
👉 所以：

```text
Property or field 'startDate' cannot be found on null
```

---

## ✅ 正确做法（二选一，强烈推荐方案一）

---

## ✅ 方案一（✅ 强烈推荐）：**前后端字段统一**

### ✅ 后端 Query（正确）

```java
@Data
public class OrderStatisticsQuery {

    @NotNull
    private LocalDate startDate;

    @NotNull
    private LocalDate endDate;

    private OrderStatusEnum status;
}
```

### ✅ 前端改成（关键）

```ts
const getFilterParams = () => {
  const params: any = {
    startDate: dateRange.value.start,   // ✅ 不要拼接时间
    endDate: dateRange.value.end,       // ✅ 不要拼接时间
    ...advancedFilter.value,
  };

  if (activeStatus.value !== null) {
    params.status = activeStatus.value;
  }

  return params;
};
```

### ✅ 后端 SQL 里自己拼时间

```sql
AND create_time >= #{query.startDate} 00:00:00
AND create_time <= #{query.endDate} 23:59:59
```

✅ 好处：
• 前端干净

• 后端语义清晰

• 缓存 key 稳定

• 不分时区坑


---

## ✅ 方案二（✅ 不改前端，但后端要适配）

### 后端 Query

```java
@Data
public class OrderStatisticsQuery {

    private String startTime;
    private String endTime;

    private OrderStatusEnum status;
}
```

### 缓存 key 改成

```java
@Cacheable(
    value = "order:statistics",
    key = "#query.startTime + ':' + #query.endTime"
)
```

⚠️ 不推荐原因：
• 字符串时间不稳定

• 时区坑

• 缓存命中率低


---

## ✅ 你现在这个异常 100% 是因为这件事

| 层           | 状态            |
| ------------ | --------------- |
| 前端         | ✅ 正常          |
| 后端参数     | ❌ 字段名不匹配  |
| Spring Cache | ❌ SpEL 解析失败 |
| 方法体       | ❌ 根本没执行    |
| Seata        | ✅ 只是包装异常  |

---

## ✅ 你现在立刻要改的只有两行

### ✅ 前端

```ts
startDate: dateRange.value.start,
endDate: dateRange.value.end,
```

### ✅ 后端缓存

```java
@Cacheable(
    value = "order:statistics",
    key = "#query.startDate + ':' + #query.endDate"
)
```

---

## ✅ 一句话总结（你可以彻底关这个 issue 了）

> **不是缓存不能用  
> 是你前端传的是 `startTime`，后端缓存却在找 `startDate`  
> 字段名对不上，Spring Cache 直接炸。**

你现在这套订单统计接口，字段名一对齐，缓存一删或一改，这一整条链路就彻底通了。

如果你愿意，我可以帮你把 订单统计接口的「前端参数 / 后端 Query / SQL / 缓存 key」 整理成一份 “永不踩坑对照表”，以后你团队直接照抄。