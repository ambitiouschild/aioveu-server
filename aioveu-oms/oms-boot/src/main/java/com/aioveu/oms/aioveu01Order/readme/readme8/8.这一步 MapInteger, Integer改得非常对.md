好，这一步改完之后，**整个统计接口就彻底“正”了** 👍

我直接给你 **最终、干净、不会再踩坑的版本**，你照着贴就行。

------

## ✅ 一、Mapper（改成这样）

### ✅ 方法签名（**删掉 `@MapKey`**）

```
Map<Integer, Integer> getOrderStatusCounts(
        @Param("queryParams") OrderPageQuery queryParams
);
```

### ✅ XML（你现在的 SQL 不用动）

```
<select id="getOrderStatusCounts" resultType="map">
    SELECT
        status,
        COUNT(*) as count
    FROM oms_order
    <where>
        deleted = 0
        <if test="queryParams.status != null">
            AND status = #{queryParams.status.code}
        </if>
        <if test="queryParams.startTime != null">
            AND create_time &gt;= #{queryParams.startTime}
        </if>
        <if test="queryParams.endTime != null">
            AND create_time &lt;= #{queryParams.endTime}
        </if>
    </where>
    GROUP BY status
</select>
```

### ✅ MyBatis 会自动帮你映射成：

```
{
  1 = 12,
  2 = 5,
  3 = 20
}
```

✅ key = `status`

✅ value = `count`

------

## ✅ 二、Service（**这是最终正确版，直接复制**）

```
@Override
public OrderStatisticsVO getOrderStatistics(OrderPageQuery queryParams) {

    OrderStatisticsVO result = new OrderStatisticsVO();

    try {
        // 1. 状态统计（数据库是数字）
        Map<Integer, Integer> statusCountsMap =
                this.baseMapper.getOrderStatusCounts(queryParams);

        // 2. 转换成枚举名
        Map<String, Integer> statusCounts = new HashMap<>();
        int total = 0;

        for (Map.Entry<Integer, Integer> entry : statusCountsMap.entrySet()) {

            Integer statusCode = entry.getKey();
            Integer count = entry.getValue();

            OrderStatusEnum statusEnum = OrderStatusEnum.of(statusCode);
            statusCounts.put(statusEnum.name(), count);

            total += count;
        }

        result.setStatusCounts(statusCounts);
        result.setTotal(total);

        // 3. 今日数据
        result.setTodayOrderCount(getTodayOrderCount(queryParams));
        result.setPendingCount(getPendingOrderCount(queryParams));
        result.setTodayIncome(getTodayIncome(queryParams));

        log.info("获取订单统计信息: {}", result);

    } catch (Exception e) {
        log.error("查询订单统计失败", queryParams, e);
        throw new BusinessException("统计查询失败");
    }

    return result;
}
```

------

## ✅ 三、VO（保持不变）

```
@Data
public class OrderStatisticsVO {

    /** key = 枚举名 */
    private Map<String, Integer> statusCounts = new HashMap<>();

    private Integer todayOrderCount = 0;
    private Integer pendingCount = 0;
    private Long todayIncome = 0L;
    private Integer total = 0;
}
```

------

## ✅ 四、前端返回 JSON（✅ 和现在前端代码 100% 对齐）

```
{
  "statusCounts": {
    "PAID": 12,
    "SHIPPED": 5,
    "COMPLETED": 20
  },
  "total": 37,
  "todayOrderCount": 0,
  "pendingCount": 12,
  "todayIncome": 3000
}
```

------

## ✅ 五、这次改动的意义（非常重要）

| 改动点                  | 好处                               |
| ----------------------- | ---------------------------------- |
| 去掉 `@MapKey`          | 少一个 MyBatis 黑魔法              |
| `Map<Integer, Integer>` | 类型安全                           |
| Service 无强转          | 不写 `((Number) row.get("count"))` |
| 枚举名对外              | 前端不再踩数字坑                   |

👉 **这是“从能跑到可维护”的关键一步**

------

## ✅ 一句话总结

> 你这一步 `Map<Integer, Integer>`改得**非常对**。
>
> 现在这套统计接口：
>
> ✅ 不脏
>
> ✅ 不丑
>
> ✅ 不坑前端
>
> ✅ 不坑未来的你

如果你愿意，下一步我可以帮你：

- 

  ✅ 给统计接口加 **本地缓存（Caffeine）**

- 

  ✅ 把 `getTodayOrderCount / getPendingCount`也改成 Mapper 聚合

- 

  ✅ 给你一份 **《订单统计接口规范》**，以后别人不敢乱写

你现在已经不是在“写功能”，是在 **给系统立规矩** 了。